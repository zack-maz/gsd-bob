---
quick_id: 260706-j81
slug: seed-bob-270k-context-window
description: Seed Bob's 270k context window into the installer config + document it
date: 2026-07-06
status: complete
---

# Quick Task 260706-j81 Summary: Seed Bob's 270k context window

Seeded Bob's real 270k context window into the installer's `.planning/config.json`
merge (top-level `context_window: 270000`, mirroring how `workflow.text_mode` is forced)
so gsd-core's read-depth / advisory scaling matches Bob's actual shared-context budget
instead of the conservative 200k default, and documented the sequential-inline rationale.

## Commits

| Hash | Type | Scope |
| --- | --- | --- |
| `4d25e93` | feat | config merge seeds `context_window: 270000` + tests |
| `c388c93` | docs | ARCHITECTURE.md + README.md document the 270k seeding |

## Files Changed

**Task 1 (`4d25e93`):**
- `src/installer/config-merge.cjs` — added `BOB_CONTEXT_WINDOW = 270000` constant
  (commented with the sequential-inline shared-context rationale); `mergeTextMode` now
  sets top-level `cfg.context_window = BOB_CONTEXT_WINDOW` unconditionally before the
  byte-stable `JSON.stringify` serialization; header doc updated to describe seeding both
  `workflow.text_mode` and `context_window`; export name `mergeTextMode` preserved (added
  `BOB_CONTEXT_WINDOW` to exports). Never-clobber-unparseable, non-object coercion, and
  dryRun discipline unchanged.
- `test/installer/config-merge.test.cjs` — fresh-config and existing-user-config cases now
  assert `context_window === 270000`; idempotency asserts the seeded value persists
  byte-identically; dryRun asserts `bytes` include `"context_window": 270000`; unparseable
  case asserts no `context_window` injected and `bytes === undefined`.

**Task 2 (`c388c93`):**
- `ARCHITECTURE.md` — Axis 2 gains a "Context-window consequence of no isolated subagents"
  bullet linking sequential-inline → one shared window → 270k operative budget → installer
  seeds `context_window: 270000` via `mergeTextMode` / `BOB_CONTEXT_WINDOW`.
- `README.md` — Flagged-gaps sequential-inline paragraph gains a sentence on the 270k window
  and the `context_window: 270000` seeding tied to the real shared budget.

## Test Results

`node --test test/installer/config-merge.test.cjs` — **7 pass / 0 fail**.

Full suite `npm test` — **321 pass / 1 fail (322 total)**. The single failure
(`CORE-02: a produced PLAN.md carries the documented section + frontmatter markers` in
`test/core-loop-contract.test.cjs`) is **pre-existing and unrelated** — it fails on a missing
fixture `.planning/phases/04-core-loop-port/04-01-PLAN.md`, with identical 321/1 counts
before and after this task's changes (verified via stash). Out of scope per the SCOPE
BOUNDARY (not caused by these changes); left untouched.

`node --test test/docs-conformance.test.cjs` — **5 pass / 0 fail** (docs edits break no guard).

## Test-Golden Updates

None required. No test golden pins full `.planning/config.json` bytes — the installer/staging
tests (`install-clean.test.cjs`, `uninstall.test.cjs`) assert on specific keys
(`workflow.text_mode`, user keys) rather than byte-identical config content, and the
`text-mode` fixture (`test/fixtures/text-mode/config.json`) is a static config-loader input,
not `mergeTextMode` output. All continue to pass with the added `context_window` key.

## Deviations from Plan

None — plan executed exactly as written. Design decisions honored: `mergeTextMode` export name
kept, `BOB_CONTEXT_WINDOW = 270000` added, `cfg.context_window` set unconditionally before
serialization, never-clobber/dryRun discipline preserved, docs anchored to the real constant.

## Self-Check: PASSED

- `src/installer/config-merge.cjs` — FOUND (exports `BOB_CONTEXT_WINDOW`, seeds `context_window`)
- `test/installer/config-merge.test.cjs` — FOUND (7/7 green)
- `ARCHITECTURE.md` / `README.md` — FOUND (270k + sequential-inline linkage present)
- Commit `4d25e93` — FOUND in `git log`
- Commit `c388c93` — FOUND in `git log`
