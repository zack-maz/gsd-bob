---
phase: 01-bob-capability-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ACCEPTANCE-CHECKLIST.md
  - .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md
autonomous: true
requirements: [SPIKE-01, SPIKE-02, SPIKE-03, SPIKE-04]

must_haves:
  truths:
    - "CAPABILITY-MAP.md records SPIKE-01 with conservative default 'sequential inline (assume no isolated subagents)', state 'Absence-based', confidence MEDIUM, a bob.ibm.com/docs source URL + verbatim quote, and a reference to AC-01"
    - "CAPABILITY-MAP.md records SPIKE-02 with conservative default 'conversational text_mode (assume no structured-choice primitive)', state 'Absence-based', confidence MEDIUM, a bob.ibm.com/docs source URL + verbatim quote, and a reference to AC-02"
    - "CAPABILITY-MAP.md records SPIKE-03 with conservative default 'command tool group can shell out to node gsd-tools.cjs', state 'Documented', confidence HIGH, a bob.ibm.com/docs source URL + verbatim quote, and a reference to AC-03"
    - "CAPABILITY-MAP.md records SPIKE-04 with config home '~/.bob' (Documented/HIGH), config-home env override 'none documented — ~/.bob fixed, drop override' (UNKNOWN/LOW), and IDE-vs-Shell signal 'BOB_SHELL_CLI_IDE_SERVER_PORT' (MEDIUM), each with source URL + verbatim quote, and a reference to AC-04"
    - "CAPABILITY-MAP.md contains 4 lighter adjacent-surface reference rows (Agent Skill, Slash command, Custom Mode groups, MCP/Rules/AGENTS.md) with documented contract + source URL + confidence and NO per-row AC step (D-14)"
    - "D-10: CAPABILITY-MAP.md explicitly flags every LOW- and MEDIUM-confidence SPIKE row as a Phase 6 'assumption may be wrong' watch-list row (via a `Watch-list (Phase 6)` column carrying `yes` for LOW/MEDIUM rows and `no` for HIGH rows, consistent with the D-02 row schema), so Phase 6 can filter the map to exactly the watch-list rows; SPIKE-01 (MEDIUM), SPIKE-02 (MEDIUM), the SPIKE-04 env-override sub-finding (LOW), and the SPIKE-04 IDE-vs-Shell sub-finding (MEDIUM) are marked `yes`; SPIKE-03 (HIGH) and the SPIKE-04 config-home sub-finding (HIGH) are marked `no`"
    - ".planning/ACCEPTANCE-CHECKLIST.md exists at the planning root (not nested under any phase directory) and contains stepped entries AC-01, AC-02, AC-03, AC-04"
    - "Each AC step uses the D-05 schema with Cmd:, Expect:, Confirms:, and 'Result: [ ] pass  [ ] fail' lines"
    - "Every Cmd: line in ACCEPTANCE-CHECKLIST.md is read-only / side-effect-free (no install, write, delete, or state mutation) so the Phase 6 unattended on-device pass is safe on a real machine"
    - "Every SPIKE source citation in CAPABILITY-MAP.md points at a bob.ibm.com/docs/... URL (never .claude/CLAUDE.md, never ibm.com/us-en/products/...)"
  prohibitions:
    - statement: "No CAPABILITY-MAP.md source cell cites .claude/CLAUDE.md as source-of-truth — it is scaffold only (D-13)"
      status: resolved
      verification: "grep for 'CLAUDE.md' inside source/citation cells of CAPABILITY-MAP.md returns no match"
    - statement: "No adjacent-surface reference row carries an AC-step reference (D-14: adjacent rows get no AC step)"
      status: resolved
      verification: "The 4 adjacent rows contain no AC-0N reference token"
    - statement: "No machine-readable bob runtime descriptor (JSON/code) is built in this phase — it is Phase 2 work (D-03)"
      status: resolved
      verification: "Phase directory contains no .json/.cjs/.js descriptor artifact; only CAPABILITY-MAP.md is added in the phase dir"
    - statement: "No AC Cmd: line performs a write, install, delete, or other state-mutating operation"
      status: resolved
      verification: "grep AC Cmd: lines for install/rm/mkdir/write/>/cp/mv/npm i tokens returns no match; commands are echo/list/read-only query only"
  artifacts:
    - path: ".planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md"
      provides: "Per-primitive decision record: 4 full SPIKE rows + 4 adjacent reference rows; input contract to Phase 2 (its emitter is inspected against this map per Phase 2 SC#4)"
      contains: "SPIKE-01"
      min_lines: 60
    - path: ".planning/ACCEPTANCE-CHECKLIST.md"
      provides: "Root-anchored device-runnable acceptance checklist seeded with AC-01..AC-04; append target for Phases 2-6, run target for Phase 6"
      contains: "AC-01"
      min_lines: 24
  key_links:
    - from: ".planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md"
      to: ".planning/ACCEPTANCE-CHECKLIST.md"
      via: "Each SPIKE row references its device-runnable probe by AC-ID only (D-06); the step bodies live in the root checklist"
      pattern: "AC-0[1-4]"
---

<objective>
Author the two documentation artifacts that resolve Phase 1's four SPIKE primitives from IBM Bob's official docs, recording a conservative lower-bound default for each and a device-runnable verification step the Phase 6 on-device pass will run.

Purpose: This phase grounds the entire adapter design in Bob's most constrained *documented* behavior. Its `CAPABILITY-MAP.md` is the input contract Phase 2 inspects its emitter against (Phase 2 SC#4); its seeded `.planning/ACCEPTANCE-CHECKLIST.md` is the root-anchored, append-only file every later phase extends and Phase 6 runs once on hardware. No live Bob exists, so all resolution is citation-grounded doc-conformance, not empirical demonstration.

Output:
- `.planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md` — 4 full SPIKE rows (D-02 schema) + 4 lighter adjacent-surface reference rows (D-14).
- `.planning/ACCEPTANCE-CHECKLIST.md` — AC-01..AC-04 (D-05 schema), read-only/side-effect-free probes, at the planning root.

The conservative defaults for SPIKE-01 (sequential inline) and SPIKE-02 (text_mode) are LOCKED by ROADMAP — this phase records and sources them, it does not relitigate them.
</objective>

<artifacts_this_phase_produces>
This phase creates only Markdown documentation — no code symbols (no decorators, classes, functions, CLI flags, or struct fields). New file paths introduced:

- `.planning/ACCEPTANCE-CHECKLIST.md` (NEW — planning-root acceptance checklist; D-04)
- `.planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md` (NEW — per-primitive capability decision record; D-01)

New identifiers (acceptance-step IDs) introduced by this phase, to be referenced by later phases:
- `AC-01` (SPIKE-01 — subagent isolation probe)
- `AC-02` (SPIKE-02 — structured prompt primitive probe)
- `AC-03` (SPIKE-03 — command tool group shell-out probe)
- `AC-04` (SPIKE-04 — config home / env override / IDE-vs-Shell signal probe)

Map row `state` field vocabulary established here (D-12): `Documented`, `Absence-based`, `UNKNOWN`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bob-capability-mapping/01-CONTEXT.md
@.planning/phases/01-bob-capability-mapping/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed .planning/ACCEPTANCE-CHECKLIST.md at the planning root with read-only AC-01..AC-04</name>
  <files>.planning/ACCEPTANCE-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/01-bob-capability-mapping/01-RESEARCH.md — §"SPIKE Findings" carries the exact AC-01..AC-04 seed blocks (Cmd/Expect/Confirms) to transcribe; §"Security Domain" carries the read-only-command mandate.
    - .planning/phases/01-bob-capability-mapping/01-CONTEXT.md — D-04 (root location), D-05 (step schema), D-07 (cross-phase append convention), §specifics (the user-endorsed AC step preview).
    - .planning/REQUIREMENTS.md — §"On-Device Verification" (VERIFY-01/02: the acceptance-checklist contract this file seeds).
  </read_first>
  <action>
Create `.planning/ACCEPTANCE-CHECKLIST.md` at the planning ROOT (D-04) — never nested under the phase directory. Give it a short header that states: this is the single consolidated, root-anchored, device-runnable acceptance checklist; it is the append target for Phases 2-6 (each later phase appends its own AC-NN steps using this same schema, per D-07); Phase 6 runs the accumulated file once, unattended, on a real Bob machine.

Seed exactly four steps — AC-01, AC-02, AC-03, AC-04 — each using the D-05 schema with these four fields in order: `Cmd:` (exact command), `Expect:` (expected output), `Confirms:` (which SPIKE/SC it validates), `Result: [ ] pass  [ ] fail`. Transcribe the Cmd/Expect/Confirms content from the RESEARCH.md "AC-0N seed" blocks (AC-01 subagent isolation → confirms SPIKE-01 default; AC-02 structured prompt primitive → confirms SPIKE-02 default; AC-03 command tool group shells out to gsd-tools.cjs → confirms SPIKE-03 default; AC-04 config home / env override / IDE-vs-Shell signal → confirms SPIKE-04 default).

SECURITY MANDATE (high-severity threat T-01-SC, see threat_model): every `Cmd:` line MUST be read-only / side-effect-free so the unattended Phase 6 pass is safe on a user's real machine. Allowed command shapes only: directory listing, `echo $VAR`, and a no-side-effect `node gsd-tools.cjs` / `gsd_run query` (a read-only query). For AC-03's shell-out probe, the command run via Bob's `command` group MUST be a read-only `gsd_run query` (no install, no write). For AC-04, the env-override probe MUST only *test whether* setting `BOB_CONFIG_DIR`/`BOB_HOME` relocates `~/.bob` by reading the resulting resolved path — it must NOT create, move, or delete any `.bob` directory. Forbidden in any Cmd: line: install/`npm i`/`rm`/`mkdir`/`cp`/`mv`/output-redirection-that-writes/any state mutation.

Use the AC-ID tokens exactly as `AC-01`, `AC-02`, `AC-03`, `AC-04` (zero-padded, hyphen form) so CAPABILITY-MAP.md rows can reference them by ID. Do NOT inline any capability-map row content here (D-06: the map references AC by ID; the step bodies live only in this file).
  </action>
  <verify>
    <automated>test -f .planning/ACCEPTANCE-CHECKLIST.md && grep -q 'AC-01' .planning/ACCEPTANCE-CHECKLIST.md && grep -q 'AC-02' .planning/ACCEPTANCE-CHECKLIST.md && grep -q 'AC-03' .planning/ACCEPTANCE-CHECKLIST.md && grep -q 'AC-04' .planning/ACCEPTANCE-CHECKLIST.md && [ "$(grep -c 'Result: \[ \] pass' .planning/ACCEPTANCE-CHECKLIST.md)" -ge 4 ] && ! grep -nE 'Cmd:.*(npm i|npm install|[^a-z]rm |mkdir |[^a-z]cp |[^a-z]mv |>>|[^>]> )' .planning/ACCEPTANCE-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ACCEPTANCE-CHECKLIST.md` exists at the planning root (path is exactly `.planning/ACCEPTANCE-CHECKLIST.md`, not under `phases/`)
    - File contains four step headers identifiable by the tokens `AC-01`, `AC-02`, `AC-03`, `AC-04`
    - Each step contains all four D-05 fields: a `Cmd:` line, an `Expect:` line, a `Confirms:` line, and a `Result: [ ] pass  [ ] fail` line (≥4 `Result: [ ] pass` matches total)
    - `Confirms:` lines reference SPIKE-01 (AC-01), SPIKE-02 (AC-02), SPIKE-03 (AC-03), SPIKE-04 (AC-04) respectively
    - No `Cmd:` line contains any of: `npm i`, `npm install`, ` rm `, `mkdir `, ` cp `, ` mv `, or write redirection (`>`/`>>`) — i.e. every command is read-only (grep guard in `<verify>` returns clean)
    - File header states it is root-anchored and the append target for Phases 2-6 (D-07) — contains the strings `Phase 6` and an append/cumulative statement
  </acceptance_criteria>
  <done>
    `.planning/ACCEPTANCE-CHECKLIST.md` exists at the planning root with four read-only device-runnable steps AC-01..AC-04 in the D-05 schema, each confirming the matching SPIKE default, and a header documenting the D-07 append convention. Every seeded command is side-effect-free.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author CAPABILITY-MAP.md — 4 full SPIKE rows + 4 adjacent reference rows, citing live Bob docs</name>
  <files>.planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md</files>
  <read_first>
    - .planning/phases/01-bob-capability-mapping/01-RESEARCH.md — §"SPIKE Findings" (the D-08 evidence packets: URL + verbatim quote + confidence + state per SPIKE) and §"Adjacent-Surface Reference Rows" (the 4 lighter rows with documented contract + source + confidence).
    - .planning/phases/01-bob-capability-mapping/01-CONTEXT.md — D-01 (single file, phase dir), D-02 (row schema), D-08/D-09 (evidence rigor), D-11/D-12 (UNKNOWN handling + state vocabulary), D-14 (adjacent rows get no AC step), §specifics (user-endorsed row table shape).
    - .planning/ACCEPTANCE-CHECKLIST.md — created in Task 1; this task references its AC-01..AC-04 IDs (do not re-create those steps here).
    - .planning/ROADMAP.md — §"Phase 1" Success Criteria (the locked conservative defaults this map records, must not relitigate).
  </read_first>
  <action>
Create `.planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md` (D-01 — single file in the phase directory). Open with a short purpose header: this is the per-primitive documented capability decision record; it is the input contract to Phase 2 (Phase 2 SC#4 inspects the emitter's output against this map); it does NOT build the machine-readable `bob` runtime descriptor (that is Phase 2 work, D-03).

Section 1 — FOUR FULL SPIKE ROWS. Render each as a fixed-schema block carrying ALL D-02 fields: `primitive` | `conservative default` | `rationale` | `doc source (URL)` | `verbatim quote` | `confidence (HIGH/MEDIUM/LOW)` | `state` | `verification-step ref (AC-ID)`, PLUS a `Watch-list (Phase 6)` field. Table or block layout is Claude's discretion (D-02 fields all present). Transcribe content verbatim from the RESEARCH.md SPIKE Findings:

WATCH-LIST MARKER (D-10): add a `Watch-list (Phase 6)` field to every SPIKE row (and to each SPIKE-04 sub-finding line, since they carry independent confidence tiers). The value is mechanically derived from the row's `confidence` field: LOW or MEDIUM → `yes` (this is the explicit Phase 6 "assumption may be wrong" watch-list); HIGH → `no`. This makes the D-10 watch-list a checkable artifact: Phase 6 filters the map to exactly the `Watch-list (Phase 6): yes` rows. With current confidences this marks SPIKE-01 (MEDIUM) → `yes`, SPIKE-02 (MEDIUM) → `yes`, SPIKE-04 env-override sub-finding (LOW) → `yes`, SPIKE-04 IDE-vs-Shell sub-finding (MEDIUM) → `yes`; and SPIKE-03 (HIGH) → `no`, SPIKE-04 config-home sub-finding (HIGH) → `no`. Add a one-line note under the SPIKE section stating that the `yes` rows are the Phase 6 watch-list per D-10.
  - SPIKE-01 (Subagents): default = "sequential inline (assume NO isolated subagents)"; rationale = docs list only read/write/command/MCP/mode/question tools, no new_task/spawn tool, Orchestrator delegates via in-session mode switch; source = `https://bob.ibm.com/docs/ide/core-concepts/tools` (and `.../features/modes`); verbatim quote from Tools + Modes pages with the D-09 annotation "(no spawn/completion-signal API documented)"; confidence MEDIUM; state `Absence-based`; verify ref `AC-01`.
  - SPIKE-02 (Prompt primitive): default = "conversational text_mode (assume NO structured-choice primitive; numbered text choices)"; rationale = question tool is `ask_followup_question` with no structured/multiple-choice payload; source = `https://bob.ibm.com/docs/ide/core-concepts/tools`; verbatim quote ("Request clarification or additional detail" + the D-09 absence annotation); confidence MEDIUM; state `Absence-based`; verify ref `AC-02`.
  - SPIKE-03 (command tool group): default = "a GSD mode granted the `command` group CAN shell out to `node gsd-tools.cjs` (positively supported, no degrade)"; rationale = `execute_command` "Run CLI commands in your workspace" + `command` group listed in custom-mode `groups`; source = `https://bob.ibm.com/docs/ide/core-concepts/tools` (+ `.../configuration/custom-modes`, `.../shell/configuration/custom-modes-bobshell`); verbatim quote; confidence HIGH; state `Documented`; verify ref `AC-03`. Note the design implication that GSD modes must declare `command` in their `groups`.
  - SPIKE-04 (config home / env override / IDE-vs-Shell): render the three sub-findings (D-08 says "may be one row with three lines, or split" — Claude's discretion). (a) config home = `~/.bob/` (project `<project>/.bob/`), state `Documented`, confidence HIGH, source `https://bob.ibm.com/docs/shell/configuration/configuring`, verbatim quote "~/.bob/settings.json in your home directory" / ".bob/settings.json in your project directory". (b) config-home env override = "none documented — treat ~/.bob as fixed, drop the override" (D-11 buildable default), state `UNKNOWN`, confidence LOW, source = same `configuring` page, verbatim quote of the precedence chain with the D-09 annotation that no `BOB_CONFIG_DIR`/`BOB_HOME` relocation var is documented. (c) IDE-vs-Shell signal = "`BOB_SHELL_CLI_IDE_SERVER_PORT` set under IDE-integrated terminal; else headless/plain Shell" — state `Documented` (signal) but inferred-as-detection-rule, confidence MEDIUM, source `https://bob.ibm.com/docs/shell/troubleshooting/troubleshoot`, verbatim quote "Get the Bob Shell port ... echo $BOB_SHELL_CLI_IDE_SERVER_PORT". All three sub-findings share verify ref `AC-04`.

Section 2 — FOUR ADJACENT-SURFACE REFERENCE ROWS (D-14: lighter — documented contract + source URL + confidence, and NO AC-step reference). Transcribe from RESEARCH.md §"Adjacent-Surface Reference Rows": (1) Agent Skill (`<name>/SKILL.md`, only `name`+`description` read, project/global paths, "skills without descriptions are ignored", strip unsupported keys) — `https://bob.ibm.com/docs/ide/features/skills`, HIGH; (2) Slash command (`<name>.md`, `description`+`argument-hint` only, filename→command, `$1`/`$2` args, hyphen form only) — `https://bob.ibm.com/docs/ide/features/slash-commands`, HIGH; (3) Custom Mode `groups` (`custom_modes.yaml` schema: slug/name/roleDefinition/whenToUse/customInstructions/groups; tool groups read/edit/browser/command/mcp/skill) — `https://bob.ibm.com/docs/ide/configuration/custom-modes` + `.../shell/configuration/custom-modes-bobshell`, HIGH; (4) MCP / Rules / AGENTS.md (MCP configurable; `.bobrules`/`.bob/rules/`/`.bob/rules-{modeSlug}/`; AGENTS.md via `/init`) — `https://bob.ibm.com/docs/ide/configuration/mcp/mcp-in-bob` + `.../shell/configuration/bobshell-custom-rules`, MEDIUM-HIGH. These four rows MUST NOT carry an `AC-0N` reference.

CITATION DISCIPLINE: every SPIKE source cell cites a canonical `bob.ibm.com/docs/...` URL (D-13 — never `.claude/CLAUDE.md`, never an `ibm.com/us-en/products/...` host; see RESEARCH Pitfall 3). Do NOT inline any `Cmd:`/`Expect:` step bodies into the map (D-06: SPIKE rows reference probes by AC-ID only; the bodies live in `.planning/ACCEPTANCE-CHECKLIST.md`). Do NOT relitigate the SPIKE-01/02 locked defaults — record and source them only.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md && for s in SPIKE-01 SPIKE-02 SPIKE-03 SPIKE-04; do grep -q "$s" .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md || { echo "missing $s"; exit 1; }; done && for a in AC-01 AC-02 AC-03 AC-04; do grep -q "$a" .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md || { echo "missing $a ref"; exit 1; }; done && grep -q 'sequential inline' .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md && grep -q 'text_mode' .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md && grep -q 'BOB_SHELL_CLI_IDE_SERVER_PORT' .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md && grep -q 'Absence-based' .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md && grep -q 'UNKNOWN' .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md && [ "$(grep -c 'bob.ibm.com/docs' .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md)" -ge 6 ] && ! grep -qE '(source|quote|citation).*CLAUDE\.md' .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md && grep -qi 'Watch-list' .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md && [ "$(grep -ciE 'Watch-list[^|]*[:|][^|]*yes' .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md)" -ge 4 ] && [ "$(grep -ciE 'Watch-list[^|]*[:|][^|]*no' .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md)" -ge 2 ]
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md` exists
    - Each of SPIKE-01, SPIKE-02, SPIKE-03, SPIKE-04 appears as a row carrying all D-02 fields (primitive, conservative default, rationale, doc source URL, verbatim quote, confidence, state, AC-ID ref)
    - SPIKE-01 row contains `sequential inline` and state `Absence-based` and confidence `MEDIUM` and references `AC-01`
    - SPIKE-02 row contains `text_mode` and state `Absence-based` and references `AC-02`
    - SPIKE-03 row contains `command` group + `gsd-tools.cjs`, state `Documented`, confidence `HIGH`, references `AC-03`
    - SPIKE-04 row contains `~/.bob`, an `UNKNOWN`/LOW env-override sub-finding, `BOB_SHELL_CLI_IDE_SERVER_PORT`, and references `AC-04`
    - The 4 adjacent rows (Agent Skill, Slash command, Custom Mode groups, MCP/Rules/AGENTS.md) are present, carry a `bob.ibm.com/docs` source + confidence, and contain NO `AC-0N` reference token
    - At least 6 distinct `bob.ibm.com/docs` source URLs are cited; no source/quote/citation cell cites `CLAUDE.md`; no source cell uses an `ibm.com/us-en/products/` host
    - No `Cmd:` or `Expect:` step body appears inside the map (D-06 — AC-ID references only)
    - D-10 watch-list marker present: every SPIKE row (and each SPIKE-04 sub-finding) carries a `Watch-list (Phase 6)` value of `yes` or `no`. Every row whose Confidence is LOW or MEDIUM has `Watch-list = yes` (SPIKE-01, SPIKE-02, SPIKE-04 env-override, SPIKE-04 IDE-vs-Shell — ≥4 `yes`); every HIGH row has `Watch-list = no` (SPIKE-03, SPIKE-04 config-home — ≥2 `no`). The map states these `yes` rows are the Phase 6 watch-list per D-10, so Phase 6 can filter to exactly those rows
  </acceptance_criteria>
  <done>
    `CAPABILITY-MAP.md` exists in the phase directory with 4 full SPIKE rows (all D-02 fields, each citing a live `bob.ibm.com/docs` URL + verbatim quote + confidence + state, each referencing its AC-ID) and 4 adjacent reference rows (documented contract + source + confidence, no AC step). The locked SPIKE-01/02 defaults are recorded and sourced, not relitigated; no descriptor JSON/code is produced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded AC command → Phase 6 device (real user machine) | Commands authored in this phase are executed unattended on a real Bob machine in Phase 6. An untrusted (mutating) command crosses into a live user environment. |
| Bob docs → CAPABILITY-MAP.md → Phase 2 design | A doc claim recorded as fact becomes a load-bearing design assumption Phase 2 builds on; a fabricated/mis-grounded claim propagates downstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering / DoS | `.planning/ACCEPTANCE-CHECKLIST.md` seeded `Cmd:` lines run unattended on a real device in Phase 6 | mitigate | Every seeded AC command MUST be read-only / side-effect-free (directory listing, `echo $VAR`, read-only `gsd_run query`). The `<verify>` grep guard in Task 1 fails the build if any `Cmd:` line contains install/`rm`/`mkdir`/`cp`/`mv`/write-redirection. The AC-04 env-override probe only *reads* the resolved path; it never creates/moves/deletes a `.bob` directory. (Per RESEARCH §Security Domain — high-severity for this phase.) |
| T-01-IG | Information disclosure (false design assumption) | CAPABILITY-MAP.md rows recording unverified Bob capabilities as fact | mitigate | D-08 citation rigor enforced: every SPIKE row carries a live `bob.ibm.com/docs` URL + verbatim quote + confidence; absence-based and UNKNOWN rows are tagged loud via the `state` field (D-09/D-12). Task 2 `<verify>` forbids citing `CLAUDE.md` as source. LOW/MEDIUM rows become the Phase 6 watch-list (D-10). |
| T-01-SC2 | Tampering | npm/pip/cargo installs in this phase | accept | No package installs occur — this phase writes only Markdown (RESEARCH: "Package Legitimacy Audit: N/A", Runtime State Inventory all-none). No install task, no legitimacy checkpoint required. |

Note: No human-verify package-legitimacy gate is needed — this phase performs zero package-manager installs (RESEARCH confirms N/A), so no legitimacy checkpoint applies and this plan stays fully autonomous. The single security-relevant control is the read-only mandate on seeded AC commands (T-01-SC), enforced by the Task 1 `<verify>` grep guard.
</threat_model>

<verification>
Phase-level checks (run after both tasks complete):

1. Two new artifacts exist at their D-01/D-04 paths:
   - `test -f .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md`
   - `test -f .planning/ACCEPTANCE-CHECKLIST.md` (planning ROOT — `test ! -f .planning/phases/01-bob-capability-mapping/ACCEPTANCE-CHECKLIST.md` confirms it is NOT nested)
2. All four SPIKE IDs and all four AC IDs are present and cross-linked: each SPIKE-0N row in the map references its AC-0N; each AC-0N step exists in the root checklist.
3. Read-only safety: no `Cmd:` line in the checklist mutates state (grep guard from Task 1 `<verify>`).
4. Citation discipline: every SPIKE source cell cites `bob.ibm.com/docs/...`; none cite `CLAUDE.md` or an `ibm.com/us-en/products/` host.
5. Scope discipline (D-03): no `.json`/`.cjs`/`.js` descriptor artifact added in the phase directory — confirm with `ls .planning/phases/01-bob-capability-mapping/` shows only `*.md`.
6. Adjacent-row discipline (D-14): the 4 adjacent reference rows carry no `AC-0N` token.
</verification>

<success_criteria>
- `CAPABILITY-MAP.md` resolves all four SPIKEs (SPIKE-01..04) as fixed-schema rows with the conservative default, rationale, live `bob.ibm.com/docs` source URL, verbatim quote, confidence, `state`, and AC-ID reference — satisfying ROADMAP Phase 1 Success Criteria 1-4.
- SPIKE-01 default recorded as sequential inline (Absence-based/MEDIUM); SPIKE-02 default recorded as conversational text_mode (Absence-based/MEDIUM) — both locked, sourced not relitigated.
- SPIKE-03 default recorded as command-group-shell-out (Documented/HIGH); SPIKE-04 records config home `~/.bob` (Documented/HIGH), env override UNKNOWN/LOW (~/.bob fixed, override dropped), and IDE-vs-Shell signal `BOB_SHELL_CLI_IDE_SERVER_PORT` (MEDIUM).
- `.planning/ACCEPTANCE-CHECKLIST.md` exists at the planning root, seeded with AC-01..AC-04 in the D-05 schema, every command read-only, with a header documenting the D-07 cross-phase append convention.
- 4 adjacent-surface reference rows present (no AC step), giving Phase 2 a single documented-contract source of truth.
- No machine-readable descriptor built (D-03 deferred to Phase 2).
</success_criteria>

<output>
Create `.planning/phases/01-bob-capability-mapping/01-01-SUMMARY.md` when done.
</output>
