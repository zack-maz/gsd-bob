---
phase: 10-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - MAINTAINING.md
autonomous: true
requirements: [DOCS-04]
must_haves:
  truths:
    - "A maintainer can follow MAINTAINING.md's numbered steps to perform the next gsd-core version bump (D-06)"
    - "The runbook reflects the REAL 1.5.0 → 1.6.1 dance from 07-REVENDOR-NOTES.md, not an aspirational sequence"
    - "The runbook cites the real scripts (apply-bob-patches.cjs, generate-support-roster.cjs) and includes the four real environment caveats (D-06, Pitfall 4)"
  artifacts:
    - path: "MAINTAINING.md"
      provides: "Repeatable, numbered gsd-core version-bump runbook (DOCS-04, D-06)"
      contains: "apply-bob-patches.cjs"
  key_links:
    - from: "MAINTAINING.md"
      to: "scripts/apply-bob-patches.cjs"
      via: "step 5 re-injects the six local Bob deltas and proves idempotency"
      pattern: "apply-bob-patches.cjs"
    - from: "MAINTAINING.md"
      to: "scripts/generate-support-roster.cjs"
      via: "step 7 regenerates the roster"
      pattern: "generate-support-roster.cjs"
    - from: "MAINTAINING.md"
      to: "gsd-core/VERSION"
      via: "step 8 bumps the targeted version marker"
      pattern: "gsd-core/VERSION"
---

<objective>
Author `MAINTAINING.md` (DOCS-04, D-06) — a repeatable, numbered runbook for the gsd-core version-bump procedure, distilled from the real 1.5.0 → 1.6.1 re-vendor recorded in Phase 7 (roadmap mandate: reflect the real dance, not an aspirational one).

Purpose: give a maintainer a checklist they can replay for the next version bump, with exact commands, the six-delta re-injection, idempotency proof, and the real environment gotchas — so the next re-vendor is mechanical, not archaeology.

Output: `MAINTAINING.md` at the repo root (D-01), a checklist (numbered steps + exact commands with `<old>`/`<new>` placeholders), not a narrative retrospective.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-documentation/10-CONTEXT.md
@.planning/phases/10-documentation/10-RESEARCH.md
@.planning/phases/10-documentation/10-PATTERNS.md
@.planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md
@scripts/apply-bob-patches.cjs
@UPSTREAM.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Distill 07-REVENDOR-NOTES.md into the numbered MAINTAINING runbook (DOCS-04, D-06)</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md (esp. the "Runbook seed" L284-296; the recorded baseline L24-53; the environment caveats L273-281, L296)
    - scripts/apply-bob-patches.cjs (the six deltas enumerated L17-24; TARGET_VERSION L46)
    - scripts/generate-support-roster.cjs (the roster regeneration step)
    - gsd-core/VERSION (currently 1.6.1 — the marker step 8 bumps)
    - UPSTREAM.md (the targeted version + file:line pointers step 8 re-verifies)
    - .planning/phases/10-documentation/10-RESEARCH.md (§5 — the 8 distilled steps + the four caveats)
    - .planning/phases/10-documentation/10-PATTERNS.md (MAINTAINING section L135-149)
  </read_first>
  <action>
    Create `MAINTAINING.md` at the repo root (D-01, D-06) distilling `07-REVENDOR-NOTES.md` (esp. the L284-296 runbook seed) into a numbered, replayable runbook that reflects the REAL 1.5.0 → 1.6.1 dance — not an aspirational one. Register: a checklist a maintainer runs — numbered steps with exact commands and `<old>`/`<new>` placeholders (mirror `UPSTREAM.md`'s imperative maintainer voice), NOT a narrative retrospective. The eight ordered steps:
    1. Capture provenance (git HEAD short SHA, current `gsd-core/VERSION`, target version, date) and record the `npm test` baseline BEFORE any mutation.
    2. `npm pack @opengsd/gsd-core@<new>` into a scratch tmp dir, `tar -xzf`, and confirm the payload root `package/gsd-core/{bin,contexts,references,templates,workflows}` by `ls` BEFORE copying (note: the tarball ships NO `VERSION` file — the patch script writes it).
    3. `rm -rf gsd-core/{bin,contexts,references,templates,workflows}` (the tracked curated 5-subdir subset).
    4. `cp -R` the identical 5 subdirs from the extracted tarball.
    5. `node scripts/apply-bob-patches.cjs` to re-inject the six local Bob deltas — open `scripts/apply-bob-patches.cjs` L17-24 and reflect the actual enumeration (colon→hyphen command form, `~/.claude` → `$HOME/.claude` home-path normalization, the `bob` runtime registry block, the Bob converter code block, both aliases, and the local `VERSION` file). Then PROVE idempotency: run the script a 2nd time (every step reports no-op) and `git diff --quiet gsd-core/` (clean).
    6. Re-sync any drifted `commands/gsd/*.md` sources to the target version (the Phase 9 version-consistency lesson).
    7. `node scripts/generate-support-roster.cjs` to regenerate the roster.
    8. Run invariants FIRST (`node --test test/backend-neutrality.test.cjs test/descriptor.test.cjs` — must pass unmodified), then `npm test`, subtract the recorded baseline, classify each non-baseline failure (expected-drift → regenerate that fixture + a one-line justification keyed by the fixture name; regression → fix). Then bump `gsd-core/VERSION` and update `UPSTREAM.md`'s targeted version and re-verify its file:line pointers against the new source (never copy stale line numbers).
    MUST include the FOUR real caveats verbatim in intent (D-06, Pitfall 4): (a) the 3 pre-existing baseline `npm test` failures are environmental noise (they read archived `.planning/` fixtures absent from the working tree) — subtract the baseline, never "fix" them in a bump; (b) the stock `legacy-cleanup.cjs:225` `1.5.0` comment is a permanent expected exception — grep-exclude it, editing it introduces an undocumented 7th delta that breaks idempotency; (c) `npm pack` requires network (registry-signed immutable tarball); (d) the converters are LOCAL hand-edits — a "does the function still exist upstream?" check is meaningless; the re-injection contract (grep-absent in tarball, present post-script) is what matters. Also note the guaranteed golden drift: `test/installer/staged-shim-loads.test.cjs` (the staged `package.json` version fixture flips old→new) — regenerate with a one-line justification. Cite `scripts/apply-bob-patches.cjs`, `scripts/generate-support-roster.cjs`, `gsd-core/VERSION`, `UPSTREAM.md`, and `07-REVENDOR-NOTES.md` as provenance.
    Do NOT add `MAINTAINING.md` to the `package.json` `files` allowlist (D-07) — it stays a repo/GitHub-facing doc.
  </action>
  <verify>
    <automated>test -f MAINTAINING.md && grep -q 'apply-bob-patches.cjs' MAINTAINING.md && grep -q 'generate-support-roster.cjs' MAINTAINING.md && grep -q 'gsd-core/VERSION' MAINTAINING.md && grep -q 'legacy-cleanup.cjs' MAINTAINING.md && grep -q 'npm pack' MAINTAINING.md && grep -qE 'git diff --quiet' MAINTAINING.md</automated>
  </verify>
  <acceptance_criteria>
    - MAINTAINING.md exists at repo root with numbered steps 1–8 in the recorded order.
    - It cites `scripts/apply-bob-patches.cjs` (six-delta re-injection + idempotency) and `scripts/generate-support-roster.cjs` (roster regen).
    - It includes the four real caveats: baseline-noise-subtract, `legacy-cleanup.cjs:225` permanent exception, `npm pack` needs network, converters-are-local-hand-edits — plus the guaranteed `staged-shim-loads.test.cjs` golden drift.
    - Steps use replayable `<old>`/`<new>` placeholders (not the literal `1.5.0`/`1.6.1` hardwired throughout).
    - `package.json` `files` still does not list `MAINTAINING.md` (D-07).
  </acceptance_criteria>
  <done>MAINTAINING.md is a replayable numbered runbook that mirrors the real Phase 7 re-vendor, cites the real scripts, and carries the four environment caveats.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer authoring → repo file | A human authors a markdown runbook distilled from an existing log. No runtime execution, no network, no user input, no secrets at author time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07 | Tampering | an aspirational/incorrect runbook misleads a future re-vendor | low | mitigate | Bind every step to `07-REVENDOR-NOTES.md`; include the four real caveats and the idempotency-proof step so a maintainer can detect a bad state. |
| T-10-08 | Tampering | documented `npm pack` step fetches an unverified tarball | low | accept | `npm pack` fetches the registry-signed immutable tarball; the runbook documents the step, it does not execute it in this phase (test-deferred). |

No high-severity threats: pure documentation authoring — no runtime attack surface, no secrets, no package installs performed by this phase.
</threat_model>

<verification>
- `test -f MAINTAINING.md` and the anchor greps (apply-bob-patches.cjs, generate-support-roster.cjs, gsd-core/VERSION, legacy-cleanup.cjs, npm pack, git diff --quiet) all pass.
- `package.json` `files` unchanged (MAINTAINING.md not shipped).
</verification>

<success_criteria>
- DOCS-04: MAINTAINING.md documents the repeatable gsd-core version-bump procedure, sourced from the actual 1.5.0 → 1.6.1 re-vendor, with concrete steps, the real scripts, and the four environment caveats.
</success_criteria>

<output>
Create `.planning/phases/10-documentation/10-03-SUMMARY.md` when done.
</output>
