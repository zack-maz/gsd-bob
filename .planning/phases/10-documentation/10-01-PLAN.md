---
phase: 10-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - COMMANDS.md
  - scripts/generate-command-reference.cjs
  - test/docs-conformance.test.cjs
autonomous: true
requirements: [DOCS-01, DOCS-02]
must_haves:
  truths:
    - "A maintainer reading README.md sees all 28 supported commands grouped by cluster, plus the 2 flagged gaps (D-01, D-04)"
    - "A maintainer reading COMMANDS.md finds a one-line description for each of the 28 emitted commands, sourced from source frontmatter (D-02)"
    - "Running the doc-conformance test proves README's list and COMMANDS.md both equal the generated roster Supported set exactly, and fails loud on any drift (D-03)"
    - "Only README.md ships in the npm files allowlist; COMMANDS.md/ARCHITECTURE.md/MAINTAINING.md stay out (D-07)"
  artifacts:
    - path: "README.md"
      provides: "Expanded Supported skills section listing all 28 roster commands (DOCS-01, D-04)"
      contains: "gsd-milestone-summary"
    - path: "COMMANDS.md"
      provides: "Per-command reference — 28 gsd- entries, each with frontmatter-sourced description (DOCS-02, D-02)"
      contains: "gsd-"
    - path: "scripts/generate-command-reference.cjs"
      provides: "Directory-derived generator that emits COMMANDS.md from commands/gsd/*.md frontmatter (D-02)"
      contains: "generate-command-reference"
    - path: "test/docs-conformance.test.cjs"
      provides: "Hermetic drift guard: 3 set-equality assertions against SUPPORT-ROSTER.md (D-03)"
      contains: "docs-conformance"
  key_links:
    - from: "test/docs-conformance.test.cjs"
      to: "SUPPORT-ROSTER.md"
      via: "slice the '## Supported' section and collect gsd- tokens into a Set"
      pattern: "Supported"
    - from: "test/docs-conformance.test.cjs"
      to: "commands/gsd/"
      via: "directory-derived stems (readdirSync), single pinned literal 28"
      pattern: "readdirSync"
    - from: "test/docs-conformance.test.cjs"
      to: "README.md"
      via: "section-scoped token extraction from '## Supported skills' to next h2"
      pattern: "Supported skills"
    - from: "COMMANDS.md"
      to: "commands/gsd/*.md"
      via: "frontmatter description: is the source-of-truth blurb per command"
      pattern: "description:"
---

<objective>
Deliver the two roster-derived documentation artifacts (DOCS-01, DOCS-02) and the single hermetic drift guard (D-03) that keeps them honest against the generated `SUPPORT-ROSTER.md`.

Purpose: README currently hand-lists only 10 commands; DOCS-01 grows it to the full 28 sourced from the generated roster, and DOCS-02 adds a per-command reference — both provably pinned to what actually installs so they can never silently drift.

Output:
- `README.md` (expanded in place, D-04) — `## Supported skills` grown 10 → 28, grouped under `###` cluster subheads, links to the three new docs.
- `COMMANDS.md` (new, D-01/D-02) — per-command reference generated from `commands/gsd/*.md` frontmatter `description:`.
- `scripts/generate-command-reference.cjs` (new, D-02) — directory-derived generator mirroring `scripts/generate-support-roster.cjs`.
- `test/docs-conformance.test.cjs` (new, D-03) — three set-equality assertions against the roster Supported set.

## Artifacts this phase produces (whole phase, for reference)
- `README.md` (MODIFY, DOCS-01, this plan)
- `COMMANDS.md` (CREATE, DOCS-02, this plan) + `scripts/generate-command-reference.cjs`
- `test/docs-conformance.test.cjs` (CREATE, D-03, this plan)
- `ARCHITECTURE.md` (CREATE, DOCS-03, plan 10-02)
- `MAINTAINING.md` (CREATE, DOCS-04, plan 10-03)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-documentation/10-CONTEXT.md
@.planning/phases/10-documentation/10-RESEARCH.md
@.planning/phases/10-documentation/10-PATTERNS.md
@README.md
@SUPPORT-ROSTER.md
@scripts/generate-support-roster.cjs
@test/command-expansion.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand README Supported skills to the full 28 (DOCS-01, D-04)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current — esp. `## Supported skills` L53-90, `## Flagged gaps` L76-90, `## Documentation` L116-122; the roster-sourced framing prose L55-59 already exists)
    - SUPPORT-ROSTER.md (the 28 entries under `## Supported (emitted…)`; the 2 under `## Unsupported on Bob`)
    - .planning/phases/10-documentation/10-PATTERNS.md (README section, L99-109)
  </read_first>
  <action>
    Expand `README.md` IN PLACE (D-04, D-01) — do NOT rewrite the Install (L10-17), Scope (L19-33), Modes (L34-51), or Verification-posture (L98-114) sections. Grow the existing `## Supported skills` h2 section's bulleted list from the current 10 to the full 28 supported commands, sourced verbatim from `SUPPORT-ROSTER.md`'s Supported set (D-02) — never hand-invent a command. Group the 28 under FIVE `###` (h3) cluster subheadings within the single `## Supported skills` h2: core loop, quality gates, milestone lifecycle, planning aids, and context & maintenance. Emit each command as a backticked bullet in the existing token style (a leading `- ` then the backticked `gsd-<name>`), so the conformance regex `gsd-[a-z0-9-]+` extracts it cleanly. CRITICAL: cluster subheads MUST be h3 (`###`), never h2 — the next h2 after `## Supported skills` must remain `## Flagged gaps` so the conformance test's heading-to-next-h2 slice captures all 28. Every `gsd-<name>` token that appears inside the Supported skills section must be one of the 28 roster entries. Keep the two flagged gaps (`gsd-autonomous`, `gsd-parallel-fanout`) ONLY in the existing `## Flagged gaps` section (Pitfall 5) — never in Supported skills. In the `## Documentation` section, add GitHub-relative links to the three new repo-root docs: `COMMANDS.md`, `ARCHITECTURE.md`, `MAINTAINING.md`. Extend the existing roster-sourced framing prose rather than replacing it.
  </action>
  <verify>
    <automated>grep -q 'gsd-milestone-summary' README.md && grep -q 'gsd-complete-milestone' README.md && grep -q 'gsd-secure-phase' README.md && [ "$(grep -c '^### ' README.md)" -ge 5 ] && grep -q 'COMMANDS.md' README.md && grep -q 'ARCHITECTURE.md' README.md && grep -q 'MAINTAINING.md' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md `## Supported skills` section lists all 28 roster commands under 5 `###` cluster subheads (`grep -c '^### ' README.md` >= 5).
    - README.md still contains the intact `## Flagged gaps` h2 naming exactly `gsd-autonomous` and `gsd-parallel-fanout`; those 2 do NOT appear in the Supported skills section.
    - README.md `## Documentation` section links `COMMANDS.md`, `ARCHITECTURE.md`, `MAINTAINING.md`.
    - Definitive gate: `node --test test/docs-conformance.test.cjs` (Task 3) assertion 2 passes — README supported set == roster Supported set.
  </acceptance_criteria>
  <done>README's Supported skills section lists exactly the 28 roster commands, clustered under h3 subheads, gaps stay flagged, and the Documentation section links the three new docs.</done>
</task>

<task type="auto">
  <name>Task 2: Generate COMMANDS.md via new directory-derived generator (DOCS-02, D-02, D-07)</name>
  <files>scripts/generate-command-reference.cjs, COMMANDS.md</files>
  <read_first>
    - scripts/generate-support-roster.cjs (the analog — GENERATED banner, fixed repoRoot-relative write path, directory enumeration idiom)
    - commands/gsd/new-project.md, commands/gsd/ship.md, commands/gsd/stats.md (frontmatter `description:` shape — single unquoted line)
    - .planning/phases/10-documentation/10-RESEARCH.md (§2 blurb source, Code Examples L248-255)
    - .planning/phases/10-documentation/10-PATTERNS.md (generator section L64-95)
    - package.json (the `files` allowlist L9-17 — do NOT modify)
  </read_first>
  <action>
    Create `scripts/generate-command-reference.cjs` mirroring `scripts/generate-support-roster.cjs` (D-02, generated-not-hand-edited discipline). Enumerate stems from `commands/gsd/*.md` via `fs.readdirSync` filtered to `.md` (directory-derived, never a hardcoded name list). For each stem, slice the frontmatter (substring between the first `---` and the next `---`, the same fm-slice idiom as command-expansion.test.cjs) and extract the single-line description with the anchored pattern `^description:\s*(.+)$` (multiline) — present and non-empty for all 28 (VERIFIED per RESEARCH §2). The emitted command name is always `gsd-<stem>`; ignore the source's colon `name: gsd:<stem>` field. Write `COMMANDS.md` to the repo root via `path.join(__dirname, '..', 'COMMANDS.md')` — NEVER interpolate a stem into the write path (path-traversal mitigation, mirror generate-support-roster.cjs). Content: a `# Bob Command Reference` header followed by a `> GENERATED — do not hand-edit. Regenerate with node scripts/generate-command-reference.cjs.` banner (mirror the roster banner), then a markdown table with columns `Command | Description`, one row per stem sorted alphabetically, the Command cell being the backticked `gsd-<stem>` and the Description cell the extracted frontmatter description. Use linear, anchored regexes only (ReDoS-safe). Run `node scripts/generate-command-reference.cjs` to produce `COMMANDS.md`. Do NOT touch the `package.json` `files` allowlist (D-07): only `README.md` ships among the docs — leave `COMMANDS.md`, `ARCHITECTURE.md`, `MAINTAINING.md` out of the allowlist (they stay repo-only). Note `scripts/` is already allowlisted, so the generator itself ships (harmless, consistent with generate-support-roster.cjs); the generated `COMMANDS.md` at repo root still won't ship.
  </action>
  <verify>
    <automated>node scripts/generate-command-reference.cjs && node -e "const fs=require('fs'); const c=fs.readFileSync('COMMANDS.md','utf8'); const s=new Set([...c.matchAll(/gsd-[a-z0-9-]+/g)].map(m=>m[0])); if(s.size!==28){console.error('expected 28 unique gsd- tokens, got '+s.size);process.exit(1)} const f=require('./package.json').files; if(f.includes('COMMANDS.md')||f.includes('ARCHITECTURE.md')||f.includes('MAINTAINING.md')){console.error('D-07 violated: doc added to files allowlist');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - COMMANDS.md has exactly 28 unique `gsd-` command tokens (one row per roster command, no missing/extra).
    - Each row's Description equals the corresponding `commands/gsd/<stem>.md` frontmatter `description:` (traceable to source, no invention).
    - COMMANDS.md carries a GENERATED banner and was produced by running the generator, not hand-typed.
    - `package.json` `files` still lists exactly its original 7 entries and none of `COMMANDS.md`/`ARCHITECTURE.md`/`MAINTAINING.md` (D-07 node guard exits 0).
    - Definitive gate: `node --test test/docs-conformance.test.cjs` (Task 3) assertion 3 passes — COMMANDS.md set == roster Supported set.
  </acceptance_criteria>
  <done>`scripts/generate-command-reference.cjs` deterministically emits a 28-row COMMANDS.md whose blurbs trace to source frontmatter, and the packaging allowlist is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Add the hermetic doc-conformance drift guard (D-03)</name>
  <files>test/docs-conformance.test.cjs</files>
  <read_first>
    - test/command-expansion.test.cjs (esp. header + repoRoot + directory-derived stems L40-59; pinned-28 L219-220; roster Supported-section slice L226-234)
    - test/_helpers/vendor.cjs (repoRoot resolution — two levels up from test/_helpers/)
    - SUPPORT-ROSTER.md (the `## Supported` section shape)
    - .planning/phases/10-documentation/10-RESEARCH.md (§3, Code Examples L257-267)
    - .planning/phases/10-documentation/10-PATTERNS.md (test section L20-60)
  </read_first>
  <action>
    Create `test/docs-conformance.test.cjs` as a hermetic `node:test` file using `node:assert/strict`, resolving `repoRoot` from `require('./_helpers/vendor.cjs')` (never raw `process.cwd()` — V5 input-validation control). Enumerate stems from `commands/gsd/*.md` via `fs.readdirSync` (directory-derived spine); assert `stems.length === 28` as the SINGLE pinned literal (matching command-expansion.test.cjs L220) — every other count/list derives from `stems`. Build `rosterSupported` by slicing `SUPPORT-ROSTER.md`'s `## Supported` section (find `^##\s+Supported\b`, slice to the next `^##\s+` heading — reuse the command-expansion.test.cjs L226-234 idiom verbatim so an Unsupported-reason stem cannot falsely satisfy) and collecting `^-\s+(gsd-[a-z0-9-]+)\s*$` matches into a `Set`. Implement three set-equality assertions (RESEARCH §3): (1) `rosterSupported` deepEquals the sorted `stems.map((s) => 'gsd-' + s)`; (2) README's supported set == `rosterSupported` — extract `gsd-[a-z0-9-]+` tokens ONLY from README's `## Supported skills` section (slice from `^##\s+Supported skills` to the next `^##\s+` heading using the SAME slice idiom, so `## Flagged gaps` mentions of `gsd-autonomous`/`gsd-parallel-fanout` do not leak — Pitfall 5); (3) COMMANDS.md's `gsd-[a-z0-9-]+` token set == `rosterSupported` (no missing, no extra). Each assertion uses `assert.deepEqual([...set].sort(), [...expected].sort())` with a descriptive failure message. Use linear, anchored regexes only (ReDoS-safe). The test reads only committed files — no scratch tmpdir or cleanup needed. It is auto-picked by the `test/**/*.test.cjs` glob; no harness change.
  </action>
  <verify>
    <automated>node --test test/docs-conformance.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `test/docs-conformance.test.cjs` exits 0.
    - Stems are directory-derived; only ONE hardcoded literal `28` exists in the file.
    - README token extraction is scoped to the `## Supported skills` section (flagged-gaps entries do not leak into the supported assertion).
    - Deliberately adding a bogus `gsd-fake` bullet to README's Supported skills section makes the test FAIL (drift is caught) — revert after confirming.
    - `npm test` overall stays green (the new file adds no regressions).
  </acceptance_criteria>
  <done>The doc-conformance test passes and provably pins README's and COMMANDS.md's command lists to the generated roster Supported set, failing loud on any drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files → generator write | `scripts/generate-command-reference.cjs` reads `commands/gsd/*.md` and writes `COMMANDS.md` on the local filesystem. No network, no user-supplied runtime input. |
| repo files → test read | `test/docs-conformance.test.cjs` reads committed markdown via fixed `repoRoot`-relative paths. Read-only, hermetic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | generate-command-reference.cjs write path | low | mitigate | Write only to a fixed `path.join(__dirname, '..', 'COMMANDS.md')`; never interpolate a stem into the write path (mirror generate-support-roster.cjs). |
| T-10-02 | Denial of Service | doc/roster regex parsers | low | mitigate | Use the linear, anchored patterns from RESEARCH §3; no nested quantifiers (ReDoS-safe). |
| T-10-03 | Tampering | README/COMMANDS drift from the roster | medium | mitigate | The D-03 conformance test asserts set-equality against the generated roster and fails loud on any drift (Task 3). |
| T-10-04 | Information Disclosure | reading local repo files | low | accept | Only fixed `repoRoot`-relative reads of already-committed repo files; no external/untrusted input (V5 minor per RESEARCH Security Domain). |

No high-severity threats: documentation + one hermetic `node:test` + a node-builtins generator; no network at runtime, no auth surface, no secrets, no package installs (no supply-chain gate needed).
</threat_model>

<verification>
- `node --test test/docs-conformance.test.cjs` exits 0 (all three set-equality assertions pass).
- `node scripts/generate-command-reference.cjs` regenerates a byte-stable COMMANDS.md (28 rows).
- `npm test` stays green (no regressions introduced by the new test file).
- `node -e` D-07 guard confirms `package.json` `files` was not expanded to include the three new docs.
</verification>

<success_criteria>
- DOCS-01: README.md lists all 28 roster commands (clustered), keeps flagged gaps, links the new docs — verified by the conformance test's README assertion.
- DOCS-02: COMMANDS.md covers exactly the 28 emitted commands with frontmatter-sourced blurbs — verified by the conformance test's COMMANDS assertion.
- D-03: the hermetic drift guard passes and catches any injected drift.
- D-07: only README.md remains in the npm files allowlist.
</success_criteria>

<output>
Create `.planning/phases/10-documentation/10-01-SUMMARY.md` when done.
</output>
