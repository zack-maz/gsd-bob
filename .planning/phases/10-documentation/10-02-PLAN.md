---
phase: 10-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ARCHITECTURE.md
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "A maintainer reads ARCHITECTURE.md and understands the Bob adapter vs traditional open-gsd along four axes: converter/descriptor model, capability-map gate, backend-neutrality, and .planning/ interchange (D-05)"
    - "Every architectural claim is anchored to a live file:symbol (src/bob-adapter.cjs, src/installer/stage.cjs, gsd-core/bin/lib/runtime-artifact-conversion.cjs, UPSTREAM.md)"
    - "The doc does not link a live path to the deleted capability-map file (D-05 landmine, Pitfall 2)"
  artifacts:
    - path: "ARCHITECTURE.md"
      provides: "Standalone maintainer-reviewable architecture doc for the Bob adapter (DOCS-03, D-05)"
      contains: "src/bob-adapter.cjs"
  key_links:
    - from: "ARCHITECTURE.md"
      to: "src/bob-adapter.cjs"
      via: "cites gateArtifact / buildSupportRoster / neutralizeModelReferences as the live gate + neutrality authority"
      pattern: "gateArtifact"
    - from: "ARCHITECTURE.md"
      to: "src/installer/stage.cjs"
      via: "cites the roster-agnostic convertible-artifact loop (converter/descriptor axis)"
      pattern: "stage.cjs"
    - from: "ARCHITECTURE.md"
      to: "UPSTREAM.md"
      via: "reuses the 'move, not rewrite' framing for the converter/descriptor model"
      pattern: "UPSTREAM.md"
---

<objective>
Author `ARCHITECTURE.md` (DOCS-03, D-05) — a standalone, maintainer-reviewable document explaining the Bob adapter design versus traditional open-gsd along the four axes the requirement names, each anchored to LIVE code (not the deleted capability-map file).

Purpose: give a gsd-core maintainer a single artifact that explains how the adapter works and why, grounded in real symbols so it stays reviewable and cannot rot into aspirational prose.

Output: `ARCHITECTURE.md` at the repo root (D-01), mirroring `UPSTREAM.md`'s maintainer register and file:line-anchored style.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-documentation/10-CONTEXT.md
@.planning/phases/10-documentation/10-RESEARCH.md
@.planning/phases/10-documentation/10-PATTERNS.md
@UPSTREAM.md
@src/bob-adapter.cjs
@src/installer/stage.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ARCHITECTURE.md across the four axes, anchored to live code (DOCS-03, D-05)</name>
  <files>ARCHITECTURE.md</files>
  <read_first>
    - UPSTREAM.md (audience blockquote + "inventory, not a proposal" register L1-42; the file:line-anchored table L67-74; the "move, not rewrite" framing)
    - src/bob-adapter.cjs (gateArtifact L325-345, buildSupportRoster L356-371, neutralizeModelReferences L103-112, scanModelLiterals L132-147, BOB_SKIP_LIST L298, PRIMITIVE_REASONS L308-313)
    - src/installer/stage.cjs (the roster-agnostic convertible-artifact loop L252-295, BOB_CAPABILITY_DECL L42, the `.planning/` prune guards L305-308)
    - .planning/phases/10-documentation/10-RESEARCH.md (§4 — the four axes with live anchors; the deleted-file LANDMINE at L171-172)
    - .planning/phases/10-documentation/10-PATTERNS.md (ARCHITECTURE section L121-131)
  </read_first>
  <action>
    Create `ARCHITECTURE.md` at the repo root (D-01) explaining the Bob adapter versus traditional open-gsd along the FOUR axes (D-05), each grounded in a LIVE file:symbol anchor and written in `UPSTREAM.md`'s maintainer register (numbered/imperative, a file:line-anchored table where useful):
    Axis 1 — Converter/descriptor model (vendor-as-source, transform-at-emit): cite `src/installer/stage.cjs`'s roster-agnostic convertible-artifact loop (for each `commands/gsd/<stem>.md` it emits a flat command and a nested skill), the two vendored Bob converters `convertClaudeCommandToBobCommand` / `convertClaudeCommandToBobSkill` in `gsd-core/bin/lib/runtime-artifact-conversion.cjs`, and the `bob` runtime registry entry in `gsd-core/bin/lib/capability-registry.cjs`. Reuse `UPSTREAM.md`'s "move, not rewrite" framing.
    Axis 2 — Capability-map gate (conservative lower-bound defaults): cite `src/bob-adapter.cjs` `gateArtifact` / `buildSupportRoster` and the `BOB_CAPABILITY_DECL` lower bound (`isolatedSubagents: false`, `structuredPrompts: false`) plus `PRIMITIVE_REASONS`. Explain the two defaults in prose — no isolated subagents → sequential-inline execution; no structured-choice prompt → text_mode (numbered text choices) — and how they map to the two Unsupported roster entries (`gsd-autonomous`, `gsd-parallel-fanout`).
    Axis 3 — Backend-neutrality: cite `src/bob-adapter.cjs` `neutralizeModelReferences` and `scanModelLiterals`, and the zero-model-literal NEUTRAL-03 invariant test — OPEN `test/model-neutrality.test.cjs` and/or `test/backend-neutrality.test.cjs` before citing a specific assertion (Assumption A1: both files exist; cite whichever you open). State that the adapter itself embeds no bare model-brand literal and that Bob owns model routing.
    Axis 4 — `.planning/` interchange: explain the RUNTIME-03 byte-compatibility contract (artifacts produced under Bob are byte-compatible with Claude Code), that the installer never prunes `.planning/` (`stage.cjs` guards), and the root-anchored `workspaceRoot` (cwd, where `.planning/` lives) kept separate from the package `repoRoot` payload source.
    LANDMINE (D-05, Pitfall 2): the original capability-map document was deleted in commit `459d992` and its live path no longer exists. Source the gate rationale from live `src/bob-adapter.cjs` + `SUPPORT-ROSTER.md` + the ROADMAP/PROJECT decision records. If a verbatim gate-rationale quote is wanted, reference the recovery command `git show 459d992~1:.planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md` written as inline code and explicitly labelled "git-recovered history". Do NOT write a markdown link whose target path resolves to that deleted document — a dead link would itself be a maintainer-standard failure. (The stem/section-reference comments inside `stage.cjs` are historical prose, not file links; do not turn them into a path.)
    Do NOT add `ARCHITECTURE.md` to the `package.json` `files` allowlist (D-07) — it stays a repo/GitHub-facing doc.
    <!-- planner-discipline-allow: CAPABILITY-MAP.md -->
  </action>
  <verify>
    <automated>test -f ARCHITECTURE.md && grep -q 'src/bob-adapter.cjs' ARCHITECTURE.md && grep -q 'gateArtifact' ARCHITECTURE.md && grep -q 'neutralizeModelReferences' ARCHITECTURE.md && grep -q 'stage.cjs' ARCHITECTURE.md && grep -q 'UPSTREAM.md' ARCHITECTURE.md && [ "$(grep -cE '\]\([^)]*CAPABILITY-MAP\.md' ARCHITECTURE.md)" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - ARCHITECTURE.md exists at repo root and covers all four axes (converter/descriptor model, capability-map gate, backend-neutrality, `.planning/` interchange).
    - It cites the live anchors `src/bob-adapter.cjs`, `gateArtifact`, `neutralizeModelReferences`, `stage.cjs`, and references `UPSTREAM.md`.
    - It contains NO markdown link whose target path resolves to the deleted capability-map document (`grep -cE '\]\([^)]*CAPABILITY-MAP\.md' ARCHITECTURE.md` == 0). A bare prose/inline-code mention of the deleted file (labelled git-recovered history) is allowed.
    - `package.json` `files` still does not list `ARCHITECTURE.md` (D-07).
  </acceptance_criteria>
  <done>A standalone ARCHITECTURE.md explains the adapter along the four axes, every claim anchored to a live symbol, with no dead capability-map link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer authoring → repo file | A human authors a markdown doc citing existing code. No runtime execution, no network, no user input, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Information Disclosure | ARCHITECTURE.md integrity (stale/dead anchor) | low | mitigate | Cite live `src/bob-adapter.cjs` symbols; grep-gate against a live link to the deleted capability-map file; label any git-recovered quote as history. |
| T-10-06 | Tampering | doc drifts from the code it describes | low | accept | Anchors are file:symbol references a maintainer re-verifies on the next version bump (documented in MAINTAINING.md step 8); no runtime coupling to enforce. |

No high-severity threats: this is a pure documentation-authoring task — no runtime attack surface, no network, no secrets, no package installs.
</threat_model>

<verification>
- `test -f ARCHITECTURE.md` and the live-anchor greps pass.
- `grep -cE '\]\([^)]*CAPABILITY-MAP\.md' ARCHITECTURE.md` == 0 (no dead capability-map link).
- `package.json` `files` unchanged (ARCHITECTURE.md not shipped).
</verification>

<success_criteria>
- DOCS-03: ARCHITECTURE.md explains the Bob adapter vs traditional open-gsd across the four named axes, anchored to live code, reviewable as a standalone maintainer artifact, with no dead capability-map link.
</success_criteria>

<output>
Create `.planning/phases/10-documentation/10-02-SUMMARY.md` when done.
</output>
