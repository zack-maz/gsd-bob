---
phase: 05-quality-gates-upstream-readiness
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - UPSTREAM.md
  - README.md
  - .planning/ACCEPTANCE-CHECKLIST.md
autonomous: true
requirements: [UP-01, UP-02]
must_haves:
  truths:
    - "All Bob-specific code is confirmed isolated to one adapter component (src/bob-adapter.cjs) + the 5 gsd-core touchpoints, recorded as a file:line upstream-move inventory"
    - "The backend-neutrality grep finds zero model-name literals in the bob registry entry + adapter (stays green)"
    - "The targeted gsd-core version (1.5.0) is recorded in a durable maintainer-visible place"
    - "The package ships a README documenting install, scope/modes (re-run=update, uninstall+install=clean — no invented flags), supported skills sourced from SUPPORT-ROSTER.md, flagged gaps, version, and test-deferred posture"
    - "Each Phase 5 success criterion has a device-runnable AC step (AC-22..) appended to the consolidated acceptance checklist"
  artifacts:
    - path: "UPSTREAM.md"
      provides: "The 5-artifact upstream-move inventory with file:line pointers + gsd-core VERSION 1.5.0"
      contains: "capability-registry.cjs"
    - path: "README.md"
      provides: "Maintainer-standard package README (UP-02 required-content floor)"
      contains: "npx"
    - path: ".planning/ACCEPTANCE-CHECKLIST.md"
      provides: "Device-runnable AC-22.. steps, one per Phase 5 success criterion"
      contains: "AC-22"
  key_links:
    - from: "README.md"
      to: "SUPPORT-ROSTER.md"
      via: "supported-skills list sourced from the generated roster, never hand-typed"
      pattern: "SUPPORT-ROSTER"
    - from: "UPSTREAM.md"
      to: "src/bob-adapter.cjs"
      via: "names the single net-new Bob-specific substance module in the move inventory"
      pattern: "bob-adapter"
    - from: ".planning/ACCEPTANCE-CHECKLIST.md"
      to: "test/quality-gate-equivalence.test.cjs"
      via: "each AC step names the dev-time test it is the on-device complement to"
      pattern: "AC-2[2-6]"
---

<objective>
Discharge the upstream-readiness obligations: AUDIT (do not refactor) that the Bob-specific code is already isolated to one adapter component, record the inventory + targeted gsd-core version (UP-01), ship a maintainer-standard README (UP-02), and append the Phase 5 device-runnable acceptance-checklist steps (D-10).

UP-01 is a verification/documentation obligation, NOT new code (D-07): the backend-neutrality test already passes and the adapter is already a single component — this plan CONFIRMS and RECORDS that, it does not move or rewrite code. The "5 artifacts to upstream" inventory is copied verbatim from RESEARCH §5 with file:line pointers, so the eventual upstream PR diff is pre-scoped.

Purpose: Make the work lift upstream "as a move, not a rewrite" and ship a README a gsd-core maintainer could review.
Output: `UPSTREAM.md`, `README.md`, appended AC-22..AC-26.

## Artifacts this phase produces (this plan)
- `UPSTREAM.md` (NEW — 5-artifact move inventory + version)
- `README.md` (NEW — package README, UP-02)
- `.planning/ACCEPTANCE-CHECKLIST.md` (MODIFIED — AC-22.. appended)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-quality-gates-upstream-readiness/05-CONTEXT.md
@.planning/phases/05-quality-gates-upstream-readiness/05-RESEARCH.md
@.planning/phases/05-quality-gates-upstream-readiness/05-PATTERNS.md

# The upstream-move inventory source (file:line table) + version:
@gsd-core/bin/lib/capability-registry.cjs
@gsd-core/bin/shared/runtime-aliases.manifest.json
@gsd-core/bin/lib/runtime-homes.cjs
@gsd-core/VERSION
@src/bob-adapter.cjs
# The neutrality guard that must stay green + the AC checklist analog:
@test/backend-neutrality.test.cjs
@.planning/ACCEPTANCE-CHECKLIST.md
# The skill-list data source (generated, never hand-typed):
@SUPPORT-ROSTER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: UP-01 isolation audit + UPSTREAM.md inventory (confirm, do not refactor)</name>
  <files>UPSTREAM.md</files>
  <read_first>
    - `gsd-core/bin/lib/capability-registry.cjs` (the `"bob"` entry, ~L3045-3109 — `configHome` `dot-home` `.bob` + `BOB_CONFIG_DIR` env, `artifactLayout` with the 2 converters, `commandStyle: slash-hyphen`)
    - `gsd-core/bin/shared/runtime-aliases.manifest.json` (the `"bob": ["bob","bob-cli"]` alias, ~L79-82)
    - `gsd-core/bin/lib/runtime-homes.cjs` (the generic `dot-home` case, ~L83-91)
    - `gsd-core/VERSION` (`1.5.0`)
    - `src/bob-adapter.cjs` (the single net-new Bob-specific substance module)
    - `test/backend-neutrality.test.cjs` (confirms zero model-name literals in the bob entry + adapter; already green)
    - `.planning/phases/05-quality-gates-upstream-readiness/05-RESEARCH.md` §5 (the verified 5-artifact inventory with exact file:line) and `.planning/phases/05-quality-gates-upstream-readiness/05-PATTERNS.md` §`UPSTREAM.md`
  </read_first>
  <action>
    First CONFIRM the isolation holds (audit, no code change): (a) verify the only net-new Bob-specific substance module in the repo is `src/bob-adapter.cjs`; (b) verify `git grep`-equivalent confirms no Bob branching leaked into other `src/` modules beyond the descriptor/converter touchpoints; (c) verify `node --test test/backend-neutrality.test.cjs` is green (zero model-name literals in the `"bob"` registry block + `bob-adapter.cjs`). If any of these fail, STOP and report — do NOT refactor to make them pass (that would be out of scope for D-07; a failure is a finding, not a task).

    Then write `UPSTREAM.md` recording the upstream-move inventory. Copy the verified 5-artifact table from RESEARCH §5 with exact file:line pointers: (1) the `"bob"` registry entry in `gsd-core/bin/lib/capability-registry.cjs`; (2) the command converter `convertClaudeCommandToBobCommand`; (3) the skill converter `convertClaudeCommandToBobSkill`; (4) the runtime alias in `runtime-aliases.manifest.json`; (5) the configHome/shim resolution via the generic `dot-home` case in `runtime-homes.cjs`. Note explicitly that artifacts 2/3 are gsd-core's GENERIC converters parameterized by the registry entry (so the truly net-new code is the registry entry, the alias, and `src/bob-adapter.cjs`). Record the targeted gsd-core version = `1.5.0` (from `gsd-core/VERSION`). Verify each file:line pointer against the current files before committing them — if a line number drifted from RESEARCH, correct it (the pointers must be accurate for the PR to be a clean move). Frame the doc for a gsd-core maintainer: "what to lift, where it lives, why it's a move not a rewrite."
  </action>
  <acceptance_criteria>
    - SOURCE: `UPSTREAM.md` exists and names all five inventory artifacts: `capability-registry.cjs`, `convertClaudeCommandToBobCommand`, `convertClaudeCommandToBobSkill`, `runtime-aliases.manifest.json`, `runtime-homes.cjs`, plus `src/bob-adapter.cjs` as the net-new module.
    - SOURCE: `UPSTREAM.md` records the targeted gsd-core version `1.5.0`.
    - SOURCE: each file:line pointer in `UPSTREAM.md` resolves to the named construct in the current file (spot-verify the `"bob"` block line range and the alias line range).
    - BEHAVIOR: `node --test test/backend-neutrality.test.cjs` passes (UP-01 zero-model-literal guard green; no code was changed).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/backend-neutrality.test.cjs && grep -q '1.5.0' UPSTREAM.md && grep -q 'bob-adapter' UPSTREAM.md && grep -q 'capability-registry' UPSTREAM.md && echo "UP-01 audit recorded"</automated>
  </verify>
  <done>The isolation audit is confirmed (single adapter component, backend-neutrality green), and `UPSTREAM.md` records the 5-artifact move inventory with accurate file:line pointers and the targeted gsd-core version 1.5.0 — without any code refactor.</done>
</task>

<task type="auto">
  <name>Task 2: README.md to maintainer standard + append AC-22.. acceptance steps</name>
  <files>README.md, .planning/ACCEPTANCE-CHECKLIST.md</files>
  <read_first>
    - `SUPPORT-ROSTER.md` (the regenerated roster from 05-01 — the supported-skills + flagged-gaps DATA SOURCE; never hand-type the skill list)
    - `.planning/ACCEPTANCE-CHECKLIST.md` entries AC-17..AC-21 (the exact `## AC-NN — <title> (<REQ-ID>)` + `Cmd/Expect/Confirms/Result` schema; highest existing = AC-21)
    - `UPSTREAM.md` (from Task 1 — README points to it or summarizes the version)
    - CLAUDE.md §"Installation" + §"What NOT to Use" (the re-run=update / uninstall+install=clean convention; NO invented `--clean`/`--update` flags; `gsd-<cmd>` hyphen form)
    - `.planning/phases/05-quality-gates-upstream-readiness/05-RESEARCH.md` §7 (AC schema + suggested AC-22..AC-26) and §8 (README required-content floor + tone reference); §PATTERNS `README.md` and `.planning/ACCEPTANCE-CHECKLIST.md`
  </read_first>
  <action>
    Write `README.md` (net-new top-level file) to the UP-02 required-content floor (D-08), in maintainer tone (clear, accurate, no marketing fluff — mirror gsd-core README conventions). Required sections (ordering/depth at discretion within the floor):
    - One-line `npx` install command (the gsd-bob installer entry; mirror the gsd-core `npx -y --package=...` UX described in CLAUDE.md).
    - Scope: local (`<project>/.bob/`) vs global (`~/.bob/`) — with the resolved target path printed before writing.
    - Modes: re-run the install command = update; `--uninstall` + install = clean. State explicitly that there are NO `--clean`/`--update` flags (do NOT invent them).
    - Supported skills: list SOURCED FROM `SUPPORT-ROSTER.md` (read the regenerated roster and reflect its Supported list) — do NOT hand-type the list (Pitfall 5: it would drift from what installs). Include the core-loop + the four quality gates as they appear in the roster.
    - Flagged gaps: the `unsupported on Bob: <reason>` entries from the roster (e.g. `gsd-autonomous`, `gsd-parallel-fanout`), reflecting the parity-first flag/skip posture.
    - Targeted gsd-core version: `1.5.0`.
    - Test-deferred / no-local-Bob posture: state that all dev-time verification is doc-conformance / golden / Claude-runtime equivalence, with the on-device run deferred.
    - Pointer to `.planning/ACCEPTANCE-CHECKLIST.md` for the on-device pass, and a pointer to `UPSTREAM.md` for the move inventory.

    Then APPEND device-runnable AC steps to `.planning/ACCEPTANCE-CHECKLIST.md`, continuing from AC-21 (next is AC-22), one per Phase 5 success criterion, mirroring the AC-17..AC-21 schema exactly (`## AC-NN — <title> (<REQ-ID>)` then `Cmd:` mutating `/gsd-*` step + read-only `ls`/`grep` confirm, `Expect:` exact outputs, `Confirms:` `<REQ-ID> — <restated SC> / SC#N. (on-device complement to test/<file>)`, `Result: [ ] pass  [ ] fail`). Append-only — do NOT renumber or edit AC-01..AC-21. Suggested mapping (discretion on copy):
    - AC-22 — code-review (incl. `--fix`) on a fixture diff (QUAL-01) — complement to `test/quality-gate-equivalence.test.cjs` / `test/quality-gate-contract.test.cjs`.
    - AC-23 — debug + `continue <slug>` resume showing restored state (QUAL-02) — complement to `test/debug-state-persistence.test.cjs`.
    - AC-24 — audit-fix native run (QUAL-03) — complement to the contract suite.
    - AC-25 — audit-uat native run (QUAL-03) — complement to the contract suite.
    - AC-26 — README / upstream-readiness doc checks (UP-01/UP-02): roster-sourced skill list present, version 1.5.0 recorded, `UPSTREAM.md` inventory present.
    Each `Cmd` must name the mutating on-Bob `/gsd-*` step AND a read-only confirm (`ls`/`grep`/`test`) that mutates nothing.
  </action>
  <acceptance_criteria>
    - SOURCE: `README.md` exists and contains the install command (`grep -q npx README.md`), scope (local `.bob/` + global `~/.bob/`), modes wording (re-run = update; uninstall + install = clean), and the targeted version `1.5.0`.
    - SOURCE: `README.md` does NOT mention a `--clean` or `--update` flag (`! grep -qE '\-\-clean|\-\-update' README.md`).
    - SOURCE: `README.md`'s supported-skills list matches the Supported entries in `SUPPORT-ROSTER.md` (every skill the README lists as supported appears in the roster's Supported section; no README-only skill).
    - SOURCE: `.planning/ACCEPTANCE-CHECKLIST.md` contains AC-22 through AC-26, each with `Cmd:`/`Expect:`/`Confirms:`/`Result:` and a `<REQ-ID>` of QUAL-01/QUAL-02/QUAL-03/UP-01/UP-02; AC-01..AC-21 unchanged.
    - SOURCE: each new AC `Cmd:` line names a mutating `/gsd-*` step and a read-only confirm.
  </acceptance_criteria>
  <verify>
    <automated>grep -q npx README.md && grep -q '1.5.0' README.md && ! grep -qE '\-\-clean|\-\-update' README.md && grep -qE '^## AC-22 ' .planning/ACCEPTANCE-CHECKLIST.md && grep -qE '^## AC-26 ' .planning/ACCEPTANCE-CHECKLIST.md && echo "README + AC-22..26 present"</automated>
  </verify>
  <done>`README.md` meets the UP-02 content floor with a roster-sourced skill list and no invented flags; AC-22..AC-26 are appended to the acceptance checklist (one per Phase 5 success criterion) in the AC-17..AC-21 schema, with AC-01..AC-21 untouched.</done>
</task>

</tasks>

<verification>
- `node --test test/backend-neutrality.test.cjs` green (UP-01).
- `UPSTREAM.md` records the 5-artifact inventory with accurate file:line pointers + version 1.5.0.
- `README.md` meets the D-08 required-content floor; skill list sourced from `SUPPORT-ROSTER.md`; no invented `--clean`/`--update` flags.
- `.planning/ACCEPTANCE-CHECKLIST.md` has AC-22..AC-26 in the established schema; AC-01..AC-21 byte-unchanged.
</verification>

<success_criteria>
UP-01: Bob-specific code confirmed isolated to one adapter component, backend-neutrality grep green, targeted version 1.5.0 recorded in a durable maintainer-visible place. UP-02: a maintainer-reviewable README ships covering install, scope/modes, roster-sourced supported skills, flagged gaps, version, and test-deferred posture. Plus the Phase 5 device-runnable AC steps (AC-22..) are appended for the Phase 6 on-device pass.
</success_criteria>

<output>
Create `.planning/phases/05-quality-gates-upstream-readiness/05-03-SUMMARY.md` when done
</output>
