---
phase: 05-quality-gates-upstream-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/code-review.md
  - commands/gsd/debug.md
  - commands/gsd/audit-fix.md
  - commands/gsd/audit-uat.md
  - scripts/generate-support-roster.cjs
  - SUPPORT-ROSTER.md
autonomous: true
requirements: [QUAL-01, QUAL-03]
must_haves:
  truths:
    - "The installer emits .bob/commands/gsd-code-review.md + skill from a vendored source (code-review ports by conversion)"
    - "The installer emits .bob/commands/gsd-debug.md, gsd-audit-fix.md, gsd-audit-uat.md + skills from vendored sources"
    - "Every quality-gate candidate degrades cleanly to sequential-inline + text_mode — SUPPORT-ROSTER.md gains NO new unsupported line"
    - "D-06: the roster generator enumerates the full commands/gsd/*.md set (not the Phase-2 representative subset) so any genuine skip surfaces, and SUPPORT-ROSTER.md is regenerated/never hand-maintained — every skip reason is generated from a capability-map primitive"
  artifacts:
    - path: "commands/gsd/code-review.md"
      provides: "Claude command source for code-review (conversion input)"
      contains: "name: gsd:code-review"
    - path: "commands/gsd/debug.md"
      provides: "Claude command source for debug (conversion input)"
      contains: "name: gsd:debug"
    - path: "commands/gsd/audit-fix.md"
      provides: "Claude command source for audit-fix (conversion input)"
      contains: "name: gsd:audit-fix"
    - path: "commands/gsd/audit-uat.md"
      provides: "Claude command source for audit-uat (conversion input)"
      contains: "name: gsd:audit-uat"
    - path: "scripts/generate-support-roster.cjs"
      provides: "Roster generator with full commands/gsd-derived candidate set"
      contains: "commands"
    - path: "SUPPORT-ROSTER.md"
      provides: "Regenerated roster listing quality-gate skills as Supported"
      contains: "gsd-code-review"
  key_links:
    - from: "commands/gsd/code-review.md"
      to: "src/installer/stage.cjs"
      via: "convertible-artifact loop enumerates commands/gsd/*.md (L239-266) — zero installer change"
      pattern: "commands.*gsd"
    - from: "scripts/generate-support-roster.cjs"
      to: "src/bob-adapter.cjs"
      via: "gateArtifact / buildSupportRoster authority drives every roster line"
      pattern: "gateArtifact|buildSupportRoster"
---

<objective>
Vendor the daily-driver quality-gate slash-command sources into `commands/gsd/` so the EXISTING installer convertible loop auto-converts them to Bob artifacts (QUAL-01, QUAL-03), and extend the standalone roster generator to enumerate the full `commands/gsd/*.md` set so any parity-first skip surfaces (QUAL-03 flagged-reason half).

This is a port-by-conversion phase (D-01). No new converter, installer, or gate code is written. `src/installer/stage.cjs` is UNCHANGED — it already enumerates every `.md` under `commands/gsd/`, derives `name = gsd-${stem}`, gates each via `gateArtifact`, and emits `.bob/commands/gsd-<x>.md` + `.bob/skills/gsd-<x>/SKILL.md`. This plan only adds the SOURCE files (mirroring Phase 4 commit `feat(04-01): vendor 6 core-loop command sources`) and widens the roster script's candidate set.

Purpose: Make the quality gates installable as Bob artifacts and make any genuine parity gap loud — the foundation Wave 2's verification suites assert against.
Output: 4 vendored command sources, an extended `generate-support-roster.cjs`, a regenerated `SUPPORT-ROSTER.md`.

## Artifacts this phase produces (this plan)
- `commands/gsd/code-review.md` (NEW — vendored command source)
- `commands/gsd/debug.md` (NEW — vendored command source)
- `commands/gsd/audit-fix.md` (NEW — vendored command source)
- `commands/gsd/audit-uat.md` (NEW — vendored command source)
- `scripts/generate-support-roster.cjs` (MODIFIED — candidate set derived from `commands/gsd/*.md`)
- `SUPPORT-ROSTER.md` (REGENERATED — never hand-edited)

## Scope correction (load-bearing — read before vendoring)
RESEARCH/CONTEXT name FIVE sources, but `code-review-fix` is NOT a standalone slash-command source. It exists ONLY as the vendored workflow `gsd-core/workflows/code-review-fix.md` (already staged wholesale by the installer) and is dispatched internally by `code-review --fix` (the `--fix` sub-flag spawns `gsd-code-fixer` via that workflow). There is NO `~/.claude/skills/gsd-code-review-fix/SKILL.md` (verified: only `gsd-code-review`, `gsd-debug`, `gsd-audit-fix`, `gsd-audit-uat` exist as installed skills). Therefore vendor EXACTLY FOUR command sources. The `--fix` capability of code-review is covered because (a) the `code-review.md` source documents `--fix`/`--all`/`--auto` in its argument-hint + body, and (b) the `code-review-fix.md` workflow is already in the staged payload. Do NOT create `commands/gsd/code-review-fix.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-quality-gates-upstream-readiness/05-CONTEXT.md
@.planning/phases/05-quality-gates-upstream-readiness/05-RESEARCH.md
@.planning/phases/05-quality-gates-upstream-readiness/05-PATTERNS.md

# The conversion-source provenance + transform recipe (the 6 existing core-loop sources):
@commands/gsd/plan-phase.md
@commands/gsd/verify-work.md
# The unchanged installer loop that consumes these sources:
@src/installer/stage.cjs
# The roster generator to extend + the gate authority it calls:
@scripts/generate-support-roster.cjs
@src/bob-adapter.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the 4 quality-gate command sources into commands/gsd/</name>
  <files>commands/gsd/code-review.md, commands/gsd/debug.md, commands/gsd/audit-fix.md, commands/gsd/audit-uat.md</files>
  <read_first>
    - `commands/gsd/plan-phase.md` and `commands/gsd/verify-work.md` (the EXACT transform shape of the 6 already-vendored core-loop sources — the analog)
    - `~/.claude/skills/gsd-code-review/SKILL.md`, `~/.claude/skills/gsd-debug/SKILL.md`, `~/.claude/skills/gsd-audit-fix/SKILL.md`, `~/.claude/skills/gsd-audit-uat/SKILL.md` (the upstream provenance to copy from)
    - `src/installer/stage.cjs` L239-266 (confirm the loop reads `commands/gsd/*.md` and needs no change)
    - `.planning/phases/05-quality-gates-upstream-readiness/05-RESEARCH.md` §1 (transitive enumeration) and Pitfall 1 (sources NOT present yet)
  </read_first>
  <action>
    Create FOUR files: `commands/gsd/code-review.md`, `commands/gsd/debug.md`, `commands/gsd/audit-fix.md`, `commands/gsd/audit-uat.md`. Each is a vendored copy of the corresponding installed skill at `~/.claude/skills/gsd-<cmd>/SKILL.md`, transformed to match the EXACT shape of the 6 existing `commands/gsd/*.md` core-loop sources (this is the Phase 4 D-01 vendoring recipe — verified by diffing `commands/gsd/plan-phase.md` against `~/.claude/skills/gsd-plan-phase/SKILL.md`). Apply this transform to each source's frontmatter and body:

    (1) Frontmatter `name:` uses the legacy colon dialect: `name: gsd:code-review`, `name: gsd:debug`, `name: gsd:audit-fix`, `name: gsd:audit-uat` (the converter translates `gsd:`→`gsd-` on emission — this is the converter's INPUT contract; do NOT pre-hyphenate).
    (2) Frontmatter `description:` unquoted (strip surrounding double-quotes the installed skill uses).
    (3) Preserve `argument-hint:` and `allowed-tools:` exactly as the installed skill has them (the command converter keeps `argument-hint`; the skill converter strips it — both are tested in Wave 2).
    (4) Add a `requires:` frontmatter line listing the prerequisite slugs the workflow names, mirroring the existing sources (e.g. `verify-work.md` has `requires: [execute-phase, phase]`). Derive each `requires` list conservatively from the workflow's documented prerequisites; if none, omit the line (the gate treats `requires: []`). Do NOT add `requires` entries naming any subagent — subagents are runtime-resolved, not artifacts.
    (5) Body `@$HOME/.claude/gsd-core/...` references rewritten to `@~/.claude/gsd-core/...` (the path form the 6 existing sources use).
    (6) Body command-reference dialect: leave the body's `/gsd-<x>` / `/gsd:<x>` references in whatever form the installed skill uses; the body is passed through `convertClaudeCommandToBobContent` which normalizes `gsd:`→`gsd-` on emission. Do NOT hand-rewrite body command refs.

    Each source's `<execution_context>` MUST still reference its workflow via `@~/.claude/gsd-core/workflows/<cmd>.md` (e.g. `code-review.md` → `@~/.claude/gsd-core/workflows/code-review.md`) so the transitive workflow loads. These workflows are already staged wholesale; do NOT vendor or copy the workflow bodies themselves.

    Do NOT create `commands/gsd/code-review-fix.md` — see the Scope correction in the objective. Do NOT touch `src/installer/stage.cjs`, `src/bob-adapter.cjs`, or any converter.
  </action>
  <acceptance_criteria>
    - SOURCE: `commands/gsd/code-review.md`, `commands/gsd/debug.md`, `commands/gsd/audit-fix.md`, `commands/gsd/audit-uat.md` all exist (`ls commands/gsd/` shows 10 files total: 6 core-loop + 4 quality-gate).
    - SOURCE: each new file's frontmatter `name:` matches `^name: gsd:(code-review|debug|audit-fix|audit-uat)$` (colon dialect input).
    - SOURCE: each new file's frontmatter carries a non-empty `description:` line (Bob silently ignores a description-less skill — RESEARCH Pitfall 2).
    - SOURCE: `commands/gsd/code-review-fix.md` does NOT exist (`test ! -f commands/gsd/code-review-fix.md`).
    - SOURCE: `src/installer/stage.cjs` is byte-unchanged from HEAD (`git diff --quiet src/installer/stage.cjs`).
    - BEHAVIOR: running the real installer entry against a scratch `.bob` emits `commands/gsd-code-review.md`, `commands/gsd-debug.md`, `commands/gsd-audit-fix.md`, `commands/gsd-audit-uat.md` and the matching `skills/gsd-<x>/SKILL.md` (asserted in full by Wave 2's contract test; spot-confirm here via the command below).
  </acceptance_criteria>
  <verify>
    <automated>node bin/gsd-bob.cjs --bob --global -c "$(mktemp -d)/.bob" "$(mktemp -d)" >/dev/null 2>&1; echo done; ls commands/gsd/ | grep -cE '^(code-review|debug|audit-fix|audit-uat)\.md$' # expect 4; and test ! -f commands/gsd/code-review-fix.md && echo "no code-review-fix source"</automated>
  </verify>
  <done>4 quality-gate command sources exist in `commands/gsd/` with colon-dialect `name:` and non-empty `description:`; no `code-review-fix.md` source; `stage.cjs` untouched; the real installer emits the 4 commands + 4 skills.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the roster generator to the full commands/gsd set + regenerate SUPPORT-ROSTER.md</name>
  <files>scripts/generate-support-roster.cjs, SUPPORT-ROSTER.md</files>
  <read_first>
    - `scripts/generate-support-roster.cjs` (whole file — the hard-coded Phase-2 candidate set at L29-37 is the gap; header/emission at L44-82 stays)
    - `src/bob-adapter.cjs` exports (`gateArtifact`, `buildSupportRoster`, `BOB_SKIP_LIST`, `PRIMITIVE_REASONS`, `UNSUPPORTED_MARKER`) — the gate authority; do NOT modify
    - `src/installer/stage.cjs` L239-266 (the installer's own enumeration `renderRoster()` is the runtime authority the standalone script must agree with)
    - `.planning/phases/05-quality-gates-upstream-readiness/05-RESEARCH.md` §3 (roster scope GAP) and Open Question 1 (derive from `commands/gsd/*.md`)
    - `.planning/phases/05-quality-gates-upstream-readiness/05-PATTERNS.md` §`scripts/generate-support-roster.cjs (MODIFIED)`
  </read_first>
  <action>
    Modify `scripts/generate-support-roster.cjs` ONLY in its candidate-set construction (currently the hard-coded array at L29-37). Replace the hard-coded representative subset with a candidate set DERIVED from `commands/gsd/*.md` — the same source `src/installer/stage.cjs` iterates — so the standalone script and the installer agree and the list cannot drift. Read the directory `path.join(__dirname, '..', 'commands', 'gsd')`, filter to `.md` files, and map each to `{ name: 'gsd-' + basename-without-ext, requires: [] }`. Preserve the two curated edge-case entries that exercise the gate's skip paths so the roster keeps proving the mechanism: append `{ name: 'gsd-autonomous', requires: [] }` (the curated `BOB_SKIP_LIST` entry) and `{ name: 'gsd-parallel-fanout', requires: ['isolatedSubagents'] }` (the unmet-hard-dependency case). De-duplicate by `name` if any overlap.

    Keep the `gateArtifact`/`buildSupportRoster` call shape and the header/body emission unchanged EXCEPT: update the header/scope comment that currently says "representative candidate set proving the gate mechanism for Phase 2 / Full-roster generation across every GSD skill rides with Phases 4-5" to state that the candidate set is now derived from the emitted `commands/gsd/*.md` set (full-roster generation, Phase 5). Do NOT change the `bobCapabilityDecl` (`{ isolatedSubagents: false, structuredPrompts: false }`).

    Then regenerate the roster: run `node scripts/generate-support-roster.cjs` and let it overwrite `SUPPORT-ROSTER.md`. Do NOT hand-edit `SUPPORT-ROSTER.md` (it carries a GENERATED — do not hand-edit banner): per D-06 the audit roster reasons are always generated from the `gateArtifact`/`buildSupportRoster` capability map, never hand-maintained, so every skip traces to a capability-map primitive. Expected outcome (D-03): all quality-gate candidates appear under Supported; the only `unsupported on Bob:` lines remain `gsd-autonomous` and `gsd-parallel-fanout` — NO new quality-gate skip line.
  </action>
  <acceptance_criteria>
    - SOURCE: `scripts/generate-support-roster.cjs` reads `commands/gsd` via `fs.readdirSync` and constructs candidates from it (`grep -nE "readdirSync|commands" scripts/generate-support-roster.cjs` shows the derivation).
    - SOURCE: the script still imports and calls `adapter.gateArtifact` / `adapter.buildSupportRoster` (gate authority preserved).
    - BEHAVIOR: `node scripts/generate-support-roster.cjs` exits 0 and rewrites `SUPPORT-ROSTER.md`.
    - BEHAVIOR: regenerated `SUPPORT-ROSTER.md` lists `gsd-code-review`, `gsd-debug`, `gsd-audit-fix`, `gsd-audit-uat` under Supported (`grep -c` each ≥1 in a non-`unsupported on Bob:` line).
    - BEHAVIOR: regenerated `SUPPORT-ROSTER.md` contains NO `unsupported on Bob:` line whose subject is a quality-gate name — the only unsupported subjects are `gsd-autonomous` and `gsd-parallel-fanout` (D-03: no new skips).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/generate-support-roster.cjs && for n in gsd-code-review gsd-debug gsd-audit-fix gsd-audit-uat; do grep -q "$n" SUPPORT-ROSTER.md && ! grep -qE "unsupported on Bob:.*$n" SUPPORT-ROSTER.md && echo "$n supported" || { echo "FAIL $n"; exit 1; }; done</automated>
  </verify>
  <done>The roster generator derives its candidate set from `commands/gsd/*.md` plus the two curated edge cases; `SUPPORT-ROSTER.md` is regenerated and lists all four quality gates as Supported with no new unsupported line.</done>
</task>

</tasks>

<verification>
- `ls commands/gsd/` lists exactly 10 sources (6 core-loop + 4 quality-gate); no `code-review-fix.md`.
- `git diff --quiet src/installer/stage.cjs && git diff --quiet src/bob-adapter.cjs` — no converter/installer/gate change.
- `node scripts/generate-support-roster.cjs` regenerates `SUPPORT-ROSTER.md`; the four quality gates are Supported; no new `unsupported on Bob:` line.
- The real installer entry emits the four `.bob/commands/gsd-<x>.md` + `skills/gsd-<x>/SKILL.md`.
</verification>

<success_criteria>
The quality-gate command sources are vendored and auto-converted by the unchanged installer (QUAL-01, QUAL-03 emission half), and the roster generator covers the full `commands/gsd` set so any parity-first skip would surface (QUAL-03 flagged-reason half). Expected D-03 outcome: zero new skips.
</success_criteria>

<output>
Create `.planning/phases/05-quality-gates-upstream-readiness/05-01-SUMMARY.md` when done
</output>
