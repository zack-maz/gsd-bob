---
phase: 05-quality-gates-upstream-readiness
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - test/quality-gate-equivalence.test.cjs
  - test/quality-gate-contract.test.cjs
  - test/debug-state-persistence.test.cjs
  - test/roster-capmap.test.cjs
  - test/fixtures/quality-gates/code-review.command.expected.md
  - test/fixtures/quality-gates/code-review.skill.expected.md
  - test/fixtures/quality-gates/debug.command.expected.md
  - test/fixtures/quality-gates/debug.skill.expected.md
  - test/fixtures/quality-gates/audit-fix.command.expected.md
  - test/fixtures/quality-gates/audit-fix.skill.expected.md
  - test/fixtures/quality-gates/audit-uat.command.expected.md
  - test/fixtures/quality-gates/audit-uat.skill.expected.md
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03]
must_haves:
  truths:
    - "code-review (incl. --fix) reviews changed source under Bob — proven by Claude-runtime equivalence on the converted artifact + a contract assertion on the real installer's emitted command/skill"
    - "D-04: debug persistent state rides the existing .planning/debug/{slug}.md session file (no new state mechanism) — proven by a start→write→reset→continue→restore-from-disk round-trip test, not a 'session starts' assertion"
    - "D-06: audit (audit-fix / audit-uat) runs natively and every parity-first skip carries a generated, capability-map-traceable reason (roster never hand-maintained) — proven by roster-vs-capmap inspection"
    - "D-02: all four quality gates degrade cleanly to sequential-inline — gateArtifact reports supported:true for each (none require concurrent isolated subagents); no quality-gate skip line is emitted"
    - "All four quality-gate sources convert byte-identically to frozen golden fixtures and carry a non-empty description (Bob never silently ignores them)"
  artifacts:
    - path: "test/quality-gate-equivalence.test.cjs"
      provides: "Golden-diff + empty-description + neutralization equivalence suite (QUAL-01/03)"
      contains: "convertClaudeCommandToBobCommand"
    - path: "test/quality-gate-contract.test.cjs"
      provides: "Real-installer e2e: emits the 4 commands+skills and stages the audit/debug workflows wholesale"
      contains: "gsd-bob.cjs"
    - path: "test/debug-state-persistence.test.cjs"
      provides: "QUAL-02 reset→continue→restore round-trip + slug-sanitization edge cases"
      contains: "continue"
    - path: "test/roster-capmap.test.cjs"
      provides: "QUAL-03 every skip line traces to a capability-map primitive; no supported gate missing"
      contains: "gateArtifact"
    - path: "test/fixtures/quality-gates/code-review.command.expected.md"
      provides: "Frozen golden of the converter's command output for code-review"
      contains: "gsd-code-review"
  key_links:
    - from: "test/quality-gate-equivalence.test.cjs"
      to: "test/fixtures/quality-gates/"
      via: "byte-identity assert.equal against frozen .command.expected.md / .skill.expected.md per stem"
      pattern: "fixtures/quality-gates"
    - from: "test/debug-state-persistence.test.cjs"
      to: ".planning/debug/{slug}.md (scratch)"
      via: "write session file in mkdtempSync root, re-read after simulated reset, assert restored fields"
      pattern: "mkdtempSync|debug"
    - from: "test/roster-capmap.test.cjs"
      to: "src/bob-adapter.cjs"
      via: "gateArtifact / buildSupportRoster / PRIMITIVE_REASONS inspection"
      pattern: "PRIMITIVE_REASONS|gateArtifact"
---

<objective>
Build the backend-agnostic verification suites that prove the three quality gates run natively under Bob — all runnable in the Claude/Node runtime with NO live Bob (D-09), mirroring the established `node:test` + frozen-fixture + golden + `mkdtempSync` hermetic pattern.

Four suites:
1. `quality-gate-equivalence.test.cjs` — per-source golden diff (×4) + empty-description / strip-unsupported-keys guard + programmatic neutralization (QUAL-01, QUAL-03).
2. `quality-gate-contract.test.cjs` — drives the REAL installer entry against a scratch `.bob`; asserts the 4 commands + 4 skills emit, and that `audit-fix`/`audit-uat`/`debug`/`code-review-fix` workflows are also staged wholesale under `gsd-core/workflows/` (QUAL-01, QUAL-03).
3. `debug-state-persistence.test.cjs` — the D-05 start→write→reset→`continue`→restore-from-disk round-trip + slug-sanitization edge cases (QUAL-02).
4. `roster-capmap.test.cjs` — every `SUPPORT-ROSTER.md` skip line's reason traces to a `PRIMITIVE_REASONS`/`BOB_SKIP_LIST` entry (D-06: generated, never hand-maintained), and no supported quality-gate artifact is missing; all four gates report `supported:true` under the `isolatedSubagents:false` declaration, proving the sequential-inline lower bound (D-02) (QUAL-03).

Purpose: The Claude/Node runtime is the standing equivalence proxy for the deferred Bob run; these suites are the dev-time gate that the test-deferred port actually works.
Output: 4 test files + 8 frozen golden fixtures under `test/fixtures/quality-gates/`.

## Artifacts this phase produces (this plan)
- `test/quality-gate-equivalence.test.cjs` (NEW)
- `test/quality-gate-contract.test.cjs` (NEW)
- `test/debug-state-persistence.test.cjs` (NEW)
- `test/roster-capmap.test.cjs` (NEW)
- `test/fixtures/quality-gates/{code-review,debug,audit-fix,audit-uat}.command.expected.md` (NEW ×4)
- `test/fixtures/quality-gates/{code-review,debug,audit-fix,audit-uat}.skill.expected.md` (NEW ×4)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-quality-gates-upstream-readiness/05-CONTEXT.md
@.planning/phases/05-quality-gates-upstream-readiness/05-RESEARCH.md
@.planning/phases/05-quality-gates-upstream-readiness/05-PATTERNS.md

# The test analogs to mirror exactly:
@test/core-loop-equivalence.test.cjs
@test/core-loop-contract.test.cjs
@test/_helpers/vendor.cjs
# The vendored debug session model (state-persistence source of truth):
@gsd-core/workflows/debug.md
# The gate API the roster test inspects:
@src/bob-adapter.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Equivalence + contract suites with frozen golden fixtures</name>
  <files>test/quality-gate-equivalence.test.cjs, test/quality-gate-contract.test.cjs, test/fixtures/quality-gates/code-review.command.expected.md, test/fixtures/quality-gates/code-review.skill.expected.md, test/fixtures/quality-gates/debug.command.expected.md, test/fixtures/quality-gates/debug.skill.expected.md, test/fixtures/quality-gates/audit-fix.command.expected.md, test/fixtures/quality-gates/audit-fix.skill.expected.md, test/fixtures/quality-gates/audit-uat.command.expected.md, test/fixtures/quality-gates/audit-uat.skill.expected.md</files>
  <read_first>
    - `test/core-loop-equivalence.test.cjs` (the golden-diff template: per-command loop L63-77, empty-description guard L79-101, programmatic neutralization L52-60/L103-110, hermetic mkdtempSync L167-173)
    - `test/core-loop-contract.test.cjs` (the real-entry e2e template: `runEntry` via execFileSync, command+skill emission asserts, "staged as workflow NOT command" shape)
    - `test/_helpers/vendor.cjs` (`requireVendor`, `repoRoot` — exercise the repo's vendored gsd-core with the `bob` entry, never the global install)
    - `commands/gsd/{code-review,debug,audit-fix,audit-uat}.md` (the vendored sources from 05-01 — the conversion input)
    - `.planning/phases/05-quality-gates-upstream-readiness/05-PATTERNS.md` §`quality-gate-equivalence` and §`quality-gate-contract`
  </read_first>
  <action>
    First FREEZE the golden fixtures. Create `test/fixtures/quality-gates/` and, for each stem in `[code-review, debug, audit-fix, audit-uat]`, generate `<stem>.command.expected.md` by running `convertClaudeCommandToBobCommand(<source>, 'gsd-<stem>')` on the vendored source and committing the EXACT output, and `<stem>.skill.expected.md` by running `convertClaudeCommandToBobSkill(<source>, 'gsd-<stem>')` and committing its EXACT output. The golden IS the converter's own output frozen once (mirrors `test/fixtures/core-loop/`). Produce these with a one-off node invocation requiring the vendored `runtime-artifact-conversion.cjs` through `test/_helpers/vendor.cjs`; do NOT hand-author fixture bodies.

    Then create `test/quality-gate-equivalence.test.cjs`, mirroring `test/core-loop-equivalence.test.cjs` structure, with `QUALITY_GATE_NAMES = ['code-review','debug','audit-fix','audit-uat']`. Per stem:
    - byte-identity: `assert.equal(conv.convertClaudeCommandToBobCommand(source, name), <command.expected>)` and the same for `...Skill` vs `<skill.expected>`.
    - empty-description / strip-unsupported-keys guard (copy L79-101): converted command frontmatter has a non-empty `description:` (`/^description:\s*\S/m`), keeps `argument-hint:`, and does NOT carry `effort:`/`allowed-tools:`/`agent:`; the skill side has `name:` + non-empty `description:` ONLY and `argument-hint:` ABSENT.
    - neutralization (copy L52-60/L103-110): build forbidden tokens PROGRAMMATICALLY via `.join('')` (`claudeHomePath = ['.','claude','/'].join('')`, `colonDialect = ['gsd',':'].join('')`) so this file's prose can't self-trip; assert the converted body excludes the Claude config-home PATH form and the colon dialect, and includes the `.bob` home + hyphen form. <!-- planner-discipline-allow: gsd: -->

    Then create `test/quality-gate-contract.test.cjs`, mirroring `test/core-loop-contract.test.cjs`: define `ENTRY = path.join(repoRoot,'bin','gsd-bob.cjs')` and a `runEntry(args, cwd)` via `execFileSync(process.execPath, [ENTRY, ...args], {cwd, encoding:'utf8'})`; install into a `mkdtempSync` scratch `.bob` with `['--bob','--global','-c',target]`. Assert: `commands/gsd-code-review.md`, `commands/gsd-debug.md`, `commands/gsd-audit-fix.md`, `commands/gsd-audit-uat.md` exist; `skills/gsd-<x>/SKILL.md` exists for each; AND the staged-wholesale workflows exist under `gsd-core/workflows/`: `code-review.md`, `code-review-fix.md`, `debug.md`, `audit-fix.md`, `audit-uat.md` (these are reached transitively — `code-review-fix.md` is workflow-only, never a command, per 05-01 scope correction; assert it is NOT emitted as `commands/gsd-code-review-fix.md`).

    All scratch writes go to `fs.mkdtempSync(path.join(os.tmpdir(), 'gsdbob-qg-...'))` and are `rmSync`'d; never the tracked `.planning/`. Do NOT modify any converter, the installer, or the gate.
  </action>
  <acceptance_criteria>
    - SOURCE: `test/fixtures/quality-gates/` contains 8 files (`<stem>.command.expected.md` + `<stem>.skill.expected.md` for all 4 stems).
    - SOURCE: `test/quality-gate-equivalence.test.cjs` requires modules through `requireVendor` (not the global gsd-core) and builds forbidden tokens via `.join('')` (no literal colon-dialect string in the test prose).
    - TEST COMMAND: `node --test test/quality-gate-equivalence.test.cjs` passes — per-stem byte-identity, non-empty description, argument-hint kept on command / absent on skill, no effort/allowed-tools/agent on command.
    - TEST COMMAND: `node --test test/quality-gate-contract.test.cjs` passes — the real entry emits the 4 commands + 4 skills; the 5 workflows are staged wholesale; `commands/gsd-code-review-fix.md` is NOT emitted.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/quality-gate-equivalence.test.cjs test/quality-gate-contract.test.cjs</automated>
  </verify>
  <done>8 frozen golden fixtures exist; the equivalence suite passes per-stem byte-identity + guards; the contract suite drives the real installer and asserts the 4 commands/skills emit and the workflows stage wholesale (with `code-review-fix` workflow-only).</done>
</task>

<task type="auto">
  <name>Task 2: Debug state-persistence round-trip + roster-vs-capmap suites</name>
  <files>test/debug-state-persistence.test.cjs, test/roster-capmap.test.cjs</files>
  <read_first>
    - `gsd-core/workflows/debug.md` (the `.planning/debug/{slug}.md` session model: frontmatter `status`/`trigger`/`created`/`updated`, `Current Focus` block with `hypothesis`/`test`/`expecting`/`next_action`/`reasoning_checkpoint`/`tdd_checkpoint`, `Evidence`/`Eliminated` sections; `continue <slug>` resume at Step 1c; slug regex `^[a-z0-9][a-z0-9-]*$` max 30 chars rejecting `..`/`/`/`\` in list/status/continue steps)
    - `test/core-loop-equivalence.test.cjs` L167-173 (the hermetic mkdtempSync scratch pattern)
    - `src/bob-adapter.cjs` (`gateArtifact`, `buildSupportRoster`, `PRIMITIVE_REASONS`, `BOB_SKIP_LIST`, `UNSUPPORTED_MARKER` — the gate authority)
    - `scripts/generate-support-roster.cjs` (the extended candidate set from 05-01) and `SUPPORT-ROSTER.md`
    - `.planning/phases/05-quality-gates-upstream-readiness/05-RESEARCH.md` §4 (debug mechanics, the 5 assertions) + Pitfall 3 (false-positive trap); §3 + §Code Examples (roster assertion)
    - `.planning/phases/05-quality-gates-upstream-readiness/05-PATTERNS.md` §`debug-state-persistence` and §`roster-capmap`
  </read_first>
  <action>
    Create `test/debug-state-persistence.test.cjs` (QUAL-02, D-05). The test models the `debug.md` on-disk session contract under plain Node file I/O (the backend-agnostic proxy for the Bob run) in a `mkdtempSync` scratch root used as the workspace; build the session path as `<scratch>/.planning/debug/<slug>.md`. This round-trip asserts the EXISTING file-based session model (D-04: persistence rides `.planning/debug/{slug}.md`, no new state store is introduced) — the test reads/writes that exact path and never stands up an alternative persistence mechanism. The test MUST perform the full round-trip — a "session starts" assertion alone is a FALSE POSITIVE (Pitfall 3):
    (1) START: write `.planning/debug/<slug>.md` with frontmatter `status: investigating`, `trigger` = a verbatim input string, the gathered symptoms, and a `Current Focus` block containing `hypothesis`, `test`, `expecting`, `next_action`, `reasoning_checkpoint`, `tdd_checkpoint`, plus `Evidence` and `Eliminated` sections with a known count of entries each.
    (2) SIMULATE RESET: drop all in-memory references to what was written (no carryover) — re-read ONLY from disk. (Model the reset by reading the file into fresh variables; assert nothing is sourced from the write-time variables.)
    (3) CONTINUE: read `.planning/debug/<slug>.md` back (the `continue <slug>` seam — "the existing file IS the context").
    (4) ASSERT RESTORED VERBATIM FROM DISK: `status`, `hypothesis`, `next_action`, the Evidence entry count, and the Eliminated entry count all equal what was written — proving state survives the reset from disk, not memory.
    (5) SLUG SANITIZATION EDGE CASES: implement (or import, if exported) the slug validation matching `debug.md` (`^[a-z0-9][a-z0-9-]*$`, max 30 chars, reject `..`/`/`/`\`); assert a `../`-bearing slug, a `/`-bearing slug, a `\`-bearing slug, and a >30-char slug are all REJECTED, and a valid slug is accepted. Path-traversal rejection is the only security-relevant control in this phase.
    All writes go to the `mkdtempSync` scratch root and are `rmSync`'d; NEVER write to the tracked `.planning/`.

    Create `test/roster-capmap.test.cjs` (QUAL-03, D-09.3). Require `src/bob-adapter.cjs` and the generator's candidate derivation. Two assertions:
    (a) NO SUPPORTED GATE MISSING / SEQUENTIAL-INLINE LOWER BOUND (D-02): for each of `['gsd-code-review','gsd-debug','gsd-audit-fix','gsd-audit-uat']`, assert `adapter.gateArtifact({name, requires:[]}, {isolatedSubagents:false, structuredPrompts:false}).supported === true` — i.e. under the no-isolated-subagents declaration every gate still emits (degrades to sequential-inline; none carry a concurrency dependency).
    (b) EVERY SKIP LINE TRACES TO A PRIMITIVE (D-06): build the roster lines via `adapter.buildSupportRoster(candidates, decl)` for the full `commands/gsd`-derived candidate set (plus the curated `gsd-autonomous`/`gsd-parallel-fanout` edge cases) and assert each line matches `UNSUPPORTED_MARKER` and ends in a reason that is a value of `PRIMITIVE_REASONS` or is traceable to a `BOB_SKIP_LIST` entry — proving every skip reason is generated from a capability-map primitive (never hand-maintained), no skip line has an untraceable reason. Also assert no quality-gate name appears in any unsupported line (D-03: no new skips). Build any forbidden/brand tokens programmatically if the test prose would otherwise contain them.

    Do NOT modify `src/bob-adapter.cjs` or `gsd-core/workflows/debug.md`.
  </action>
  <acceptance_criteria>
    - SOURCE: `test/debug-state-persistence.test.cjs` contains a `continue`/read-back step AND a post-reset restore assertion (not only a file-creation assertion) — `grep -nE "continue|restore|reset" test/debug-state-persistence.test.cjs` is non-empty.
    - SOURCE: the test writes to a `mkdtempSync` scratch root (`grep -q mkdtempSync test/debug-state-persistence.test.cjs`) and never references the repo's tracked `.planning/debug`.
    - TEST COMMAND: `node --test test/debug-state-persistence.test.cjs` passes — the restored `status`/`hypothesis`/`next_action`/Evidence-count/Eliminated-count match the written values after a simulated reset; `../`, `/`, `\`, and >30-char slugs are rejected; a valid slug accepted.
    - TEST COMMAND: `node --test test/roster-capmap.test.cjs` passes — all four quality gates report `supported:true` under `isolatedSubagents:false` (D-02 sequential-inline lower bound); every roster skip line's reason traces to `PRIMITIVE_REASONS`/`BOB_SKIP_LIST` (D-06); no quality-gate name in any unsupported line.
    - REGRESSION: `node --test test/backend-neutrality.test.cjs` stays green (no adapter/registry edit was made).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/debug-state-persistence.test.cjs test/roster-capmap.test.cjs test/backend-neutrality.test.cjs</automated>
  </verify>
  <done>The debug suite proves state survives a real reset via the `continue`/restore-from-disk round-trip plus slug-sanitization edge cases; the roster suite proves every skip traces to a capability-map primitive and no supported quality gate is missing; backend-neutrality stays green.</done>
</task>

</tasks>

<verification>
- `npm test` (`node --test "test/**/*.test.cjs"`) passes — the 4 new suites plus all pre-existing suites stay green.
- `test/fixtures/quality-gates/` holds 8 frozen goldens produced by the converter.
- No converter/installer/gate/workflow source was modified (`git diff --quiet src/ gsd-core/`).
- No test writes to the tracked `.planning/` (all scratch via `mkdtempSync`).
</verification>

<success_criteria>
QUAL-01 (code-review incl. --fix converts + emits), QUAL-02 (debug persists state across resets via the round-trip), and QUAL-03 (audit runs natively + every skip flagged with a capability-map-traceable reason) are each proven by a dedicated backend-agnostic `node:test` suite, with no live Bob.
</success_criteria>

<output>
Create `.planning/phases/05-quality-gates-upstream-readiness/05-02-SUMMARY.md` when done
</output>
