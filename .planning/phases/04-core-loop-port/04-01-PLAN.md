---
phase: 04-core-loop-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/new-project.md
  - commands/gsd/plan-phase.md
  - commands/gsd/discuss-phase.md
  - commands/gsd/execute-phase.md
  - commands/gsd/verify-work.md
  - commands/gsd/progress.md
  - src/installer/stage.cjs
  - test/core-loop-equivalence.test.cjs
  - test/fixtures/core-loop/
autonomous: false
requirements: [CORE-01, CORE-02, CORE-03, CORE-04, CORE-05]
user_setup:
  - service: "@opengsd/gsd-core@1.5.0 npm distribution"
    why: "The 6 core-loop Claude slash-command source files (commands/gsd/*.md) are the converter INPUT (D-01 port-by-conversion). They are ABSENT from this repo, from ~/.claude/commands/gsd, and from any local cache — they must be fetched from the published gsd-core 1.5.0 tarball's commands/gsd/ tree (the same dir gsd-core's stageCommandsForRuntimeFlat reads). Hand-authoring is forbidden (violates D-01/UP-01 move-not-rewrite). NOTE (checkpoint correction 2026-06-19): execute-plan and verify-phase are WORKFLOWS, not slash commands — they are not in commands/gsd/; they ship in gsd-core/workflows/ (already vendored) and are staged wholesale with the gsd-core payload (Structural piece 2). CORE-03 is covered by the execute-phase command → execute-plan workflow; CORE-04 by the verify-work command → verify-phase workflow."
    dashboard_config:
      - task: "If the automated `npm pack @opengsd/gsd-core@1.5.0` fetch in Task 1 cannot reach the registry non-interactively, the human supplies the 6 source files (or approves a network fetch) at the checkpoint."
        location: "npm registry (registry.npmjs.org) — package @opengsd/gsd-core@1.5.0, path package/commands/gsd/*.md"

must_haves:
  truths:
    - "The 6 core-loop Claude slash-command sources (new-project, plan-phase, discuss-phase, execute-phase, verify-work, progress) exist under commands/gsd/ in the gsd-bob package root, vendored verbatim from gsd-core 1.5.0 (not hand-authored). The execute-plan + verify-phase WORKFLOWS are NOT commands — they ride along in the wholesale-staged gsd-core/workflows/ payload."
    - "The installer's convertible loop runs each vendored source through the bob artifactLayout converters, emitting a Bob-conformant .bob/commands/gsd-<name>.md AND a .bob/skills/gsd-<name>/SKILL.md per command (CORE-01/02/05 direct; CORE-03/04 via the execute-phase→execute-plan and verify-work→verify-phase transitive workflows)."
    - "Each emitted command/skill carries a non-empty description and frontmatter reduced to the Bob-supported keys only (unsupported keys stripped) — never silently ignored by Bob."
    - "Each emitted artifact body is neutralized: no Claude config-home path ref, no colon-dialect command ref; .bob home + hyphen command form present."
    - "All 6 core-loop commands gate supported:true — none routed to SUPPORT-ROSTER.md (the only existing skip, gsd-autonomous, is out of core-loop scope)."
  artifacts:
    - path: "commands/gsd/new-project.md"
      provides: "new-project converter input (CORE-01)"
    - path: "commands/gsd/plan-phase.md"
      provides: "plan-phase converter input (CORE-02)"
    - path: "commands/gsd/discuss-phase.md"
      provides: "transitive discuss-phase converter input (CORE-02)"
    - path: "commands/gsd/execute-phase.md"
      provides: "execute-phase converter input (CORE-03; transitively drives the vendored execute-plan workflow)"
    - path: "commands/gsd/verify-work.md"
      provides: "verify-work converter input (CORE-04; transitively drives the vendored verify-phase workflow)"
    - path: "commands/gsd/progress.md"
      provides: "progress converter input (CORE-05)"
    - path: "src/installer/stage.cjs"
      provides: "convertible loop wired to the declared converters (emit command + skill)"
      contains: "convertClaudeCommandToBobCommand"
    - path: "test/core-loop-equivalence.test.cjs"
      provides: "per-command converter golden diff + empty-description guard + real-answer guard + byte-compat proxy"
    - path: "test/fixtures/core-loop/"
      provides: "frozen per-command *.expected.md (command + skill) and golden .planning/ reference subtree"
  key_links:
    - from: "src/installer/stage.cjs"
      to: "gsd-core/bin/lib/runtime-artifact-conversion.cjs"
      via: "convertible loop calls convertClaudeCommandToBobCommand + convertClaudeCommandToBobSkill (the converters named in the bob artifactLayout)"
      pattern: "convertClaudeCommandToBob(Command|Skill)"
    - from: "src/installer/stage.cjs"
      to: "src/bob-adapter.cjs"
      via: "gateArtifact(candidate, BOB_CAPABILITY_DECL) decides supported before emit"
      pattern: "gateArtifact"
    - from: "test/core-loop-equivalence.test.cjs"
      to: "commands/gsd/*.md"
      via: "requireVendor('runtime-artifact-conversion.cjs') run on the vendored source, byte-compared to the frozen expected fixture"
      pattern: "requireVendor"
---

<objective>
Close the load-bearing emission gap for the core loop. Vendor the 6 core-loop Claude slash-command source files under `commands/gsd/`, wire the installer's already-present convertible loop to the bob `artifactLayout` converters (so it emits Bob-conformant `.bob/commands` + `.bob/skills` instead of raw-copying), and prove per-command conversion correctness with a golden-diff + real-answer equivalence suite.

Purpose: Today `src/installer/stage.cjs:231-243` sources convertibles from `repoRoot/commands/gsd/` — a directory that does not exist — so the installer emits ZERO `.bob/commands`/`.bob/skills`. Even once the source is vendored, the loop currently RAW-COPIES the source (`stageFile(destRel, fs.readFileSync(...))` at stage.cjs:238) rather than running the converter named in the bob `artifactLayout`, so it would stage non-conformant artifacts (frontmatter not stripped, paths/`gsd:` not neutralized, no SKILL.md). This plan supplies the missing input AND the minimal wiring to make the already-built converters fire — honoring D-01 (port-by-conversion, reuse the built converters; never hand-rewrite a skill).

Output: 6 vendored `commands/gsd/*.md`; a converter-wired convertible loop in `stage.cjs`; `test/core-loop-equivalence.test.cjs`; and the frozen `test/fixtures/core-loop/` golden tree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-loop-port/04-RESEARCH.md
@.planning/phases/04-core-loop-port/04-PATTERNS.md

# Load-bearing source files this plan reads/edits
@src/installer/stage.cjs
@src/bob-adapter.cjs
@test/command-golden.test.cjs
@test/planning-bytecompat.test.cjs
@test/_helpers/vendor.cjs
@test/fixtures/command/input.md
@test/fixtures/command/expected.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Vendor the 6 core-loop command sources from gsd-core 1.5.0 (acquisition checkpoint)</name>
  <files>commands/gsd/new-project.md, commands/gsd/plan-phase.md, commands/gsd/discuss-phase.md, commands/gsd/execute-phase.md, commands/gsd/verify-work.md, commands/gsd/progress.md</files>
  <read_first>
    - .planning/phases/04-core-loop-port/04-RESEARCH.md (Open Question 1, Assumption A2, Environment Availability — the source must be FETCHED from the @opengsd/gsd-core@1.5.0 tarball; absent from repo and ~/.claude)
    - .planning/phases/04-core-loop-port/04-PATTERNS.md ("CRITICAL acquisition note" — fetch from the tarball's commands/gsd/ tree, vendor verbatim, do NOT hand-author and do NOT pre-neutralize)
    - test/fixtures/command/input.md (the canonical Claude-command shape: frontmatter description/argument-hint + extra keys, $ARGUMENTS body refs, Claude-config-home path refs, colon-dialect cmd refs — confirms what a valid source file looks like)
  </read_first>
  <action>
    The executor attempts the acquisition non-interactively first, then surfaces the result for human confirmation (this is a blocking-human checkpoint — the network fetch and the vendored bytes are human-approved before commit). Steps:
    1. Run `npm pack @opengsd/gsd-core@1.5.0 --pack-destination /tmp/gsd-core-pack` (or equivalent registry fetch) and extract `package/commands/gsd/` from the tarball.
    2. Copy exactly these 6 slash-command files verbatim into `commands/gsd/` at the gsd-bob package root: new-project.md, plan-phase.md, discuss-phase.md, execute-phase.md, verify-work.md, progress.md. (execute-plan and verify-phase are NOT slash commands — they are workflows in gsd-core/workflows/, already vendored and staged wholesale with the gsd-core payload; they get NO command conversion. CORE-03 is covered transitively via execute-phase→execute-plan; CORE-04 via verify-work→verify-phase.)
    3. Vendor them AS gsd-core ships them — do NOT pre-neutralize (the config-home rewrite, the colon-to-hyphen command rewrite, and $ARGUMENTS to $1 are the converter's job, applied later at conversion time, NOT in the source).
    4. Report: the 6 file paths, each file's `description:` frontmatter line, and a note that no body was edited.
    If the registry fetch cannot complete non-interactively (offline/sandboxed), STOP and ask the human to either approve a network fetch or supply the 6 files. Do NOT hand-author substitute files (violates D-01/UP-01 move-not-rewrite) and do NOT fall back to feeding gsd-core/workflows/*.md to the converter (those are workflow bodies with no `description:` frontmatter — RESEARCH Pitfall 2).
  </action>
  <verify>
    <human-check>
      1. `ls commands/gsd/` lists exactly the 6 expected files.
      2. For each file, the frontmatter block contains a non-empty `description:` line (the converter emits an empty description otherwise, which Bob silently ignores — RESEARCH Pitfall 2/4): `grep -L '^description:' commands/gsd/*.md` returns nothing.
      3. Confirm the sources are unmodified gsd-core 1.5.0 content (bodies may still contain config-home / colon-dialect / $ARGUMENTS refs — that is correct; neutralization is the converter's job in Task 2's tests).
    </human-check>
  </verify>
  <done>
    commands/gsd/ contains exactly the 6 named core-loop slash-command sources, copied verbatim from @opengsd/gsd-core@1.5.0's commands/gsd/ tree; no source was hand-authored or pre-neutralized; each source carries a non-empty `description:` frontmatter line. The execute-plan + verify-phase workflows are intentionally NOT vendored as commands (they ride in the gsd-core/workflows/ payload).
  </done>
  <resume-signal>Type "approved" once the 6 sources are vendored verbatim, or describe the acquisition blocker (e.g. "offline — supplying files manually").</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the convertible loop in stage.cjs to the declared bob converters (emit command + skill)</name>
  <files>src/installer/stage.cjs, test/installer/stage.test.cjs</files>
  <read_first>
    - src/installer/stage.cjs (lines 213-243 — Structural piece 2 payload copy as the stageFile pattern; the convertible loop at 227-243 that currently RAW-COPIES and must be rewired)
    - src/bob-adapter.cjs (gateArtifact signature + BOB_CAPABILITY_DECL usage already imported into stage.cjs)
    - test/_helpers/vendor.cjs (requireVendor — but stage.cjs is production code: require the converters via the same vendored path stage.cjs already uses for the payload, i.e. relative to repoRoot's gsd-core/bin/lib)
    - test/installer/stage.test.cjs (line 176 — the existing "empty convertible roster" test that must still pass; the new behavior is additive when the source IS present)
    - gsd-core/bin/lib/runtime-artifact-conversion.cjs (convertClaudeCommandToBobCommand at ~763, convertClaudeCommandToBobSkill at ~735 — both signature (content, name); already exported)
  </read_first>
  <behavior>
    - Test: when commands/gsd/<name>.md is present and gates supported, stage() emits commands/gsd-<name>.md whose bytes equal convertClaudeCommandToBobCommand(source, 'gsd-<name>') — NOT the raw source.
    - Test: stage() also emits skills/gsd-<name>/SKILL.md whose bytes equal convertClaudeCommandToBobSkill(source, 'gsd-<name>').
    - Test: the emitted command frontmatter contains description + argument-hint and does NOT contain effort/allowed-tools/agent (unsupported keys stripped by the converter).
    - Test: the emitted command/skill body contains no Claude config-home path ref and no colon-dialect command ref (built programmatically so the test prose cannot self-trip).
    - Test (regression): when commands/gsd/ is absent, stage() still completes without throwing and emits no commands/skills (existing stage.test.cjs:176 stays green).
    - Test: both emitted paths are recorded as manifest file entries (so update/orphan logic continues to track them).
  </behavior>
  <action>
    Rewire the convertible-artifact loop in stage.cjs (currently src/installer/stage.cjs:227-243). Require the two converters named in the bob artifactLayout from the vendored gsd-core lib (the same payload source stage.cjs already trusts: resolve through `path.join(repoRoot, 'gsd-core', 'bin', 'lib', 'runtime-artifact-conversion.cjs')`). For each source file under repoRoot/commands/gsd/: derive the bare stem and the prefixed name `gsd-<stem>`; build the gate candidate `{ name: 'gsd-<stem>', requires: [] }` and keep the existing gateArtifact(candidate, BOB_CAPABILITY_DECL).supported guard (do NOT add core-loop skip entries — A3 confirms all 6 degrade cleanly). When supported, read the source content once, then emit TWO artifacts via the existing stageFile helper: the flat command at `commands/gsd-<stem>.md` (stageFile receives the Buffer of convertClaudeCommandToBobCommand(content, 'gsd-<stem>')) and the nested skill at `skills/gsd-<stem>/SKILL.md` (Buffer of convertClaudeCommandToBobSkill(content, 'gsd-<stem>')). Match the bob artifactLayout exactly: commands are flat with the gsd- prefix; skills are nested under skills/gsd-<stem>/SKILL.md with the gsd- prefix. Do NOT raw-copy the source any longer; do NOT introduce a new converter or a degrade*.cjs (out of scope — RESEARCH Anti-Patterns/Pitfall 3). Per D-04 (reuse the TRANS-03 text_mode seam — no new degradation code): text_mode is already config-driven in the workflow bodies and forced by the Phase 3 installer, so the wiring here touches only conversion/emission, never degradation logic. Preserve the existsSync no-op-when-absent guard so the empty-source regression stays green. Reference D-01 (reuse the built converters), D-02 (sequential-inline is a runtime property — no code), D-03 (gate, don't break — gateArtifact unchanged).
  </action>
  <verify>
    <automated>node --test test/installer/stage.test.cjs</automated>
  </verify>
  <done>
    With commands/gsd/ present, stage() emits converted .bob/commands/gsd-<name>.md AND .bob/skills/gsd-<name>/SKILL.md per source (bytes equal the respective converter output, unsupported frontmatter keys stripped, body neutralized), both tracked in the manifest; with commands/gsd/ absent it still completes cleanly emitting neither. No converter logic is reimplemented; gateArtifact remains the sole support authority.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Equivalence suite — per-command converter golden diff, empty-description guard, real-answer guard, byte-compat proxy</name>
  <files>test/core-loop-equivalence.test.cjs, test/fixtures/core-loop/</files>
  <read_first>
    - test/command-golden.test.cjs (lines 10-64 — requireVendor + byte-identical golden diff + frontmatter-keys-stripped + neutralization-via-programmatic-tokens patterns; copy structure verbatim)
    - test/planning-bytecompat.test.cjs (lines 52-93 — resolveConfigHomeFromDescriptor bob-vs-claude byte-identity + "config home leaks nowhere" + "bob artifactLayout enumerates no .planning/" patterns)
    - test/_helpers/vendor.cjs (requireVendor, repoRoot)
    - .planning/phases/04-core-loop-port/04-PATTERNS.md ("test/core-loop-equivalence.test.cjs" section — Analog A/B/C exact code shapes)
    - .planning/phases/04-core-loop-port/04-RESEARCH.md (Code Examples — real-answer SENTINEL guard; Pitfall 2/4 — empty-description silent-ignore)
  </read_first>
  <behavior>
    - Test (per-command golden, ×6): for each commands/gsd/<name>.md, convertClaudeCommandToBobCommand(source, 'gsd-<name>') equals the frozen test/fixtures/core-loop/<name>.command.expected.md (byte-identical), and convertClaudeCommandToBobSkill(source, 'gsd-<name>') equals <name>.skill.expected.md. (The 6 names: new-project, plan-phase, discuss-phase, execute-phase, verify-work, progress.)
    - Test (empty-description guard, ×6): each converted command's frontmatter matches a non-empty description and an argument-hint, and does NOT match effort/allowed-tools/agent.
    - Test (neutralization, ×6): each converted body contains no Claude config-home path ref and no colon-dialect command ref (forbidden tokens built programmatically, mirroring command-golden.test.cjs:54-64); contains the .bob home and hyphen command form.
    - Test (real-answer guard, D-05): a frozen golden .planning/PROJECT.md reference under test/fixtures/core-loop/ contains the sentinel project-name string and does NOT contain TODO/placeholder/{{ markers — the explicit guard against a structurally-valid-but-empty artifact.
    - Test (byte-compat proxy, RUNTIME-03/CORE-01 equivalence): the runtime-agnostic write path produces byte-identical output under the bob vs claude config home, and the config home leaks nowhere into the artifact body (assert !bytes.includes('.bob')).
  </behavior>
  <action>
    Create test/core-loop-equivalence.test.cjs as a node:test suite. Require the vendored converter via requireVendor('runtime-artifact-conversion.cjs') and use repoRoot from _helpers/vendor.cjs. Iterate the 6 core-loop names (new-project, plan-phase, discuss-phase, execute-phase, verify-work, progress); for each, read commands/gsd/<name>.md and byte-compare both converter outputs to frozen fixtures under test/fixtures/core-loop/ (one *.command.expected.md and one *.skill.expected.md per command). FREEZE those expected fixtures by running the vendored converters on the vendored sources once and capturing the exact output (do this as part of authoring the fixture, not in the test). Add the empty-description guard (assert.match on a non-empty description line, assert.match on argument-hint, assert.doesNotMatch on effort/allowed-tools/agent) per the command-golden.test.cjs:25-35 shape. Add the neutralization assertions with programmatically-built forbidden tokens (claudeHome=['.', 'claude'].join(''), colonDialect=['gsd', ':'].join(''), bobHome=['.', 'bob'].join(''), hyphenForm=['gsd', '-'].join('')) — NEVER write the literal '.claude'/'gsd:' tokens in the test prose. For the D-05 real-answer guard, freeze a golden PROJECT.md (with a recognizable sentinel project-name string) under test/fixtures/core-loop/ and assert.match it appears and assert.doesNotMatch(/\bTODO\b|\bplaceholder\b|\{\{/i). For the byte-compat proxy, mirror planning-bytecompat.test.cjs:52-79 (resolveConfigHomeFromDescriptor for bob vs claude, drive the same write path, assert byte-identity and no config-home leak). All scratch writes go to mkdtempSync temp dirs — never the tracked .planning/. Do NOT touch the converters or stage.cjs here (Task 2 owns wiring).
  </action>
  <verify>
    <automated>node --test test/core-loop-equivalence.test.cjs</automated>
  </verify>
  <done>
    All 6 core-loop commands convert byte-identically to frozen command + skill fixtures; every converted artifact carries a non-empty description with unsupported keys stripped and a neutralized body; the D-05 golden PROJECT.md proves a real sentinel answer lands with no placeholder markers; the runtime-agnostic byte-compat proxy holds with no config-home leak. Suite green via `node --test test/core-loop-equivalence.test.cjs` and the full `npm test`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → repo (vendoring) | The 6 command sources cross from the published gsd-core 1.5.0 tarball into the gsd-bob package — supply-chain ingress. |
| repoRoot/commands/gsd source → converter → .bob (staging) | Untrusted-shaped source content is read and written under the scope `.bob/` during the conversion loop. |
| test scratch dirs → filesystem | Tests write only to mkdtempSync temp dirs; the tracked workspace `.planning/` must never be written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | vendored command sources (supply-chain) | mitigate | Source pinned to `@opengsd/gsd-core@1.5.0` and vendored verbatim via the Task 1 checkpoint; provenance asserted (frontmatter `description:` present, no hand-authoring). A wrong/tampered source surfaces as a golden-diff mismatch in Task 3. |
| T-04-02 | Tampering | stage.cjs convertible loop writing under `.bob/` | mitigate | The loop reuses the existing stageFile path which routes deletes/writes through the manifest + safeJoin containment guard (Phase 3 CR-01, stage.cjs:261); no new write path is introduced and `.planning/` is never written by the loop. |
| T-04-03 | Tampering | test suites writing into the real workspace `.planning/` | mitigate | All scratch writes use `fs.mkdtempSync(os.tmpdir(), …)`; no test writes to the tracked `.planning/` (security V12, D-07 sweep guard). |
| T-04-04 | Information disclosure | config-home leak into emitted artifacts | mitigate | Byte-compat proxy asserts `!bytes.includes('.bob')` — the resolved config home must not leak into the `.planning/`/artifact body. |
| T-04-SC | Tampering | npm `npm pack` fetch of the gsd-core tarball | mitigate | Task 1 is a `checkpoint:human-verify` gated `blocking-human` — the install/fetch is human-approved and the vendored bytes are inspected before commit; never auto-approved. |
</threat_model>

<verification>
- `node --test test/installer/stage.test.cjs` — convertible loop emits converted command + skill; empty-source regression green.
- `node --test test/core-loop-equivalence.test.cjs` — 6 per-command golden diffs, empty-description guard, neutralization, D-05 real-answer guard, byte-compat proxy.
- `npm test` — full suite green (no existing suite regressed by the stage.cjs rewire).
- `ls commands/gsd/` — exactly 6 verbatim sources.
- `grep -c convertClaudeCommandToBob src/installer/stage.cjs` — convertible loop now calls both declared converters (non-zero).
</verification>

<success_criteria>
- The 6 core-loop command sources are vendored verbatim from gsd-core 1.5.0 under commands/gsd/ (execute-plan + verify-phase are workflows, staged in the gsd-core/workflows/ payload, not converted as commands).
- stage()'s convertible loop emits Bob-conformant .bob/commands/gsd-<name>.md AND .bob/skills/gsd-<name>/SKILL.md per source via the declared converters (no raw-copy), both manifest-tracked.
- All 6 gate supported:true; SUPPORT-ROSTER.md gains no core-loop line.
- test/core-loop-equivalence.test.cjs proves per-command byte-identity, non-empty descriptions, neutralized bodies, the D-05 real-answer guard, and the byte-compat equivalence proxy.
- npm test is green.
</success_criteria>

<output>
Create `.planning/phases/04-core-loop-port/04-01-SUMMARY.md` when done.
</output>
