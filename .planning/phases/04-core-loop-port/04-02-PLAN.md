---
phase: 04-core-loop-port
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - test/core-loop-contract.test.cjs
  - test/core-loop-root-anchor.test.cjs
  - .planning/ACCEPTANCE-CHECKLIST.md
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04, CORE-05]
user_setup: []

must_haves:
  truths:
    - "A scratch end-to-end install (real bin/gsd-bob.cjs) emits .bob/commands/gsd-plan-phase.md (+ the other 7) and .bob/skills/gsd-plan-phase/SKILL.md, proving the core-loop commands stage natively (CORE-01..05 emission)."
    - "A produced PLAN.md / PROJECT.md matches the documented section/frontmatter contract (CORE-02)."
    - "execute-plan produces one atomic commit per task with the {type}({phase}-{plan}) message shape under the sequential-inline model (CORE-03)."
    - "After a full loop run there is exactly one .planning/ at the workspace root next to .bob/, with no nested second .planning/ under the scope dir (CORE-05)."
    - "The bob artifactLayout enumerates no .planning/ destination — .planning/ is root-anchored, never an emitted target (CORE-05)."
    - "AC-17..AC-21 are appended to .planning/ACCEPTANCE-CHECKLIST.md, one per core-loop success criterion, with read-only Cmd lines (mutating in-Bob loop runs wrapped in read-only confirms), continuing from AC-16 (CORE-01..05 on-device steps)."
  artifacts:
    - path: "test/core-loop-contract.test.cjs"
      provides: "e2e install emission assertion + PLAN.md/PROJECT.md structural contract (CORE-02) + atomic-commit assertion (CORE-03)"
    - path: "test/core-loop-root-anchor.test.cjs"
      provides: "single root-anchored .planning/ assertion + bob artifactLayout no-.planning/ assertion (CORE-05)"
    - path: ".planning/ACCEPTANCE-CHECKLIST.md"
      provides: "AC-17..AC-21 device-runnable steps (CORE-01..05)"
      contains: "AC-21"
  key_links:
    - from: "test/core-loop-contract.test.cjs"
      to: "bin/gsd-bob.cjs"
      via: "execFileSync(process.execPath, [ENTRY, '--bob', '--global', '-c', target], { cwd }) drives the real installer; reads back .bob/commands + .bob/skills"
      pattern: "execFileSync"
    - from: "test/core-loop-root-anchor.test.cjs"
      to: "gsd-core/bin/lib/capability-registry.cjs"
      via: "bob.runtime.artifactLayout asserted to enumerate no .planning/ destSubpath"
      pattern: "artifactLayout"
    - from: ".planning/ACCEPTANCE-CHECKLIST.md"
      to: "test/core-loop-*.test.cjs"
      via: "each AC-17..AC-21 Confirms field names its hermetic test complement (mirrors AC-05/AC-12 phrasing)"
      pattern: "AC-1[7-9]|AC-2[01]"
---

<objective>
Prove the core loop runs natively end-to-end and document the on-device steps. Drive the real installer against a scratch target and assert it emits the converted core-loop command + skill artifacts; assert the produced PLAN.md/PROJECT.md match the artifact contract; assert atomic commits under the sequential-inline model; assert exactly one root-anchored `.planning/` with no nesting; and append AC-17..AC-21 to the consolidated acceptance checklist.

Purpose: Plan 01 made the converters fire and proved per-command byte-identity in isolation. This plan completes the D-06 verification harness (three complementary checks — equivalence/golden in Plan 01, plus the artifact-contract structural + atomic-commit tests here, plus the D-07 root-anchoring assertion) and, per D-08, appends the device-runnable AC steps the Phase 6 on-device pass will run.

Output: `test/core-loop-contract.test.cjs`, `test/core-loop-root-anchor.test.cjs`, and AC-17..AC-21 appended to `.planning/ACCEPTANCE-CHECKLIST.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-loop-port/04-RESEARCH.md
@.planning/phases/04-core-loop-port/04-PATTERNS.md

# Analog test files + the append target
@test/installer/install-clean.test.cjs
@test/planning-bytecompat.test.cjs
@test/_helpers/vendor.cjs
@.planning/ACCEPTANCE-CHECKLIST.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contract suite — e2e install emission, PLAN.md/PROJECT.md structure (CORE-02), atomic commits (CORE-03)</name>
  <files>test/core-loop-contract.test.cjs</files>
  <read_first>
    - test/installer/install-clean.test.cjs (lines 17-62 — the ENTRY + scratch(prefix) + runEntry(execFileSync) harness; copy verbatim, then assert .bob/commands/gsd-plan-phase.md AND .bob/skills/gsd-plan-phase/SKILL.md exist after install)
    - test/_helpers/vendor.cjs (repoRoot, requireVendor)
    - .planning/phases/04-core-loop-port/04-PATTERNS.md ("test/core-loop-contract.test.cjs" section — scratch e2e + atomic-commit assertion shapes)
    - .planning/phases/04-core-loop-port/04-RESEARCH.md (Code Examples — atomic-commit /^\w+\(\d+-\d+\)/ shape; the protocol lives in agents/gsd-executor.md and is runtime-resolved, NOT file-loaded)
  </read_first>
  <behavior>
    - Test (e2e emission): after `execFileSync(ENTRY, ['--bob','--global','-c',target], {cwd})`, .bob/commands/gsd-plan-phase.md exists AND .bob/skills/gsd-plan-phase/SKILL.md exists (and at least the other core-loop command names are present) — proving the Plan-01 wiring emits the core loop through the real entry.
    - Test (structure CORE-02): a representative produced/fixture PLAN.md contains the documented section/frontmatter headers (e.g. the frontmatter fence and the documented section markers from the artifact contract); a produced/fixture PROJECT.md contains its documented top-level headers.
    - Test (atomic commits CORE-03): given a git fixture repo driven through the execute-plan flow (or a frozen git-log fixture capturing the sequential-inline run), every commit subject matches /^\w+\(\d+-\d+\)/ — the {type}({phase}-{plan}) atomic-commit shape; assert one commit per task.
  </behavior>
  <action>
    Create test/core-loop-contract.test.cjs as a node:test suite. Copy the ENTRY/scratch/runEntry harness from install-clean.test.cjs verbatim. For the emission assertion, run the real entry into a scratch `.bob` target from a scratch cwd, then assert fs.existsSync on `commands/gsd-plan-phase.md` and `skills/gsd-plan-phase/SKILL.md` under the target (and assert the remaining 7 command names are present). For CORE-02, read a produced or frozen PLAN.md/PROJECT.md and assert the documented section/frontmatter markers exist via assert.match on the heading/frontmatter patterns (structural only — content depth is covered by the Plan-01 D-05 real-answer guard, so do NOT duplicate that here). For CORE-03, assert the atomic-commit message shape: drive a small fixture through the execute-plan path OR read a frozen git-log fixture and assert every subject line matches /^\w+\(\d+-\d+\)/ with one commit per task. Do NOT assert wall-clock or parallelism (D-02 — output equivalence is the contract, not speed). All scratch writes go to mkdtempSync temp dirs; never the tracked .planning/.
  </action>
  <verify>
    <automated>node --test test/core-loop-contract.test.cjs</automated>
  </verify>
  <done>
    The real installer emits converted core-loop .bob/commands + .bob/skills (asserted via existsSync after execFileSync); a representative PLAN.md/PROJECT.md matches the documented section/frontmatter contract (CORE-02); execute-plan's commits all match the atomic {type}({phase}-{plan}) shape with one per task (CORE-03). Suite green via `node --test test/core-loop-contract.test.cjs`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Root-anchoring suite — exactly one .planning/ at workspace root, none nested (CORE-05)</name>
  <files>test/core-loop-root-anchor.test.cjs</files>
  <read_first>
    - test/installer/install-clean.test.cjs (lines 64-79 — the existsSync(path.join(cwd,'.planning')) predicate and the "no stray .planning/ in a non-project cwd" assertion)
    - test/planning-bytecompat.test.cjs (lines 81-93 — bob.runtime.artifactLayout enumerates no .planning/ destSubpath; extend this for the post-loop walk)
    - test/_helpers/vendor.cjs (requireVendor for capability-registry)
    - .planning/phases/04-core-loop-port/04-PATTERNS.md ("test/core-loop-root-anchor.test.cjs" section — Analog A/B + post-loop walk shapes)
    - .planning/phases/04-core-loop-port/04-RESEARCH.md (Pitfall 5 — nested second .planning/ failure mode; Code Examples — fs walk collecting .planning dirs)
  </read_first>
  <behavior>
    - Test: bob's artifactLayout (global + local) enumerates no destSubpath matching /\.planning/ — .planning/ is never an emitted target.
    - Test: after a scratch install + a simulated loop run that creates a workspace-root .planning/, a recursive walk of the scope dir finds exactly one .planning/ directory, and it sits at the workspace root (path.dirname === workspaceRoot) adjacent to .bob/.
    - Test: no nested .planning/ exists under the scope dir (assert !fs.existsSync(path.join(scopeDir, '.planning')) and the walk count === 1).
  </behavior>
  <action>
    Create test/core-loop-root-anchor.test.cjs as a node:test suite. Require the vendored capability-registry via requireVendor and assert that for both runtimes.bob.runtime.artifactLayout.global and .local, no entry's destSubpath matches /\.planning/ (mirror planning-bytecompat.test.cjs:81-93). Then, in a scratch workspace, run the real entry (or stage) to produce .bob/ and create a workspace-root .planning/ (the loop's natural output anchor), walk the scope dir collecting every directory named .planning, and assert the collected list has length 1 with path.dirname(found) === workspaceRoot. Assert !fs.existsSync(path.join(scopeDir, '.planning')) to prove nothing nested under the scope dir. Reuse the scratch(prefix) mkdtempSync helper; never write to the tracked .planning/. Reference D-07/CORE-05 (single root-anchored .planning/, no nesting) and Pitfall 5.
  </action>
  <verify>
    <automated>node --test test/core-loop-root-anchor.test.cjs</automated>
  </verify>
  <done>
    bob's artifactLayout enumerates no .planning/ target; after a loop run exactly one .planning/ exists, anchored at the workspace root next to .bob/, with no nested second .planning/ under the scope dir (CORE-05). Suite green via `node --test test/core-loop-root-anchor.test.cjs`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append AC-17..AC-21 device-runnable steps to the acceptance checklist (CORE-01..05)</name>
  <files>.planning/ACCEPTANCE-CHECKLIST.md</files>
  <read_first>
    - .planning/ACCEPTANCE-CHECKLIST.md (head lines 1-30 — the schema + T-01-SC read-only safety invariant; tail AC-13..AC-16 — the exact row format and the "mutating install/loop step wrapped in read-only confirms" precedent)
    - .planning/phases/04-core-loop-port/04-PATTERNS.md (".planning/ACCEPTANCE-CHECKLIST.md" section — exact AC-NN schema, AC-05 "on-device complement to test/..." phrasing, highest existing AC-ID = AC-16)
    - .planning/phases/04-core-loop-port/04-RESEARCH.md (Q5 — append AC-17..AC-21, one per core-loop SC, each pointing at its hermetic test complement)
  </read_first>
  <action>
    Per D-08 (append device-runnable AC steps continuing the Phase 1 D-07 convention), append five rows AC-17..AC-21 to .planning/ACCEPTANCE-CHECKLIST.md, continuing from the highest existing AC-ID (AC-16), one per core-loop success criterion: AC-17 → new-project (CORE-01), AC-18 → plan-phase incl. transitive discuss-phase (CORE-02), AC-19 → execute-phase incl. execute-plan atomic commits (CORE-03), AC-20 → verify incl. verify-work/verify-phase (CORE-04), AC-21 → progress + root-anchoring (CORE-05). Use the exact four-field schema in order: `## AC-NN — <title> (CORE-0N)`, then `Cmd:`, `Expect:`, `Confirms:`, `Result: [ ] pass  [ ] fail`. Honor the T-01-SC safety invariant: every `Cmd:` is read-only / side-effect-free — the in-Bob `/gsd-<command>` run itself is the mutating Phase-6 step (allowed, exactly like AC-13's install), wrapped in read-only read-backs (`ls`, `grep -c`, `gsd-tools query`, `find … -name .planning`). For each Confirms field, name the matching CORE-0N criterion AND its hermetic test complement (e.g. "on-device complement to test/core-loop-equivalence.test.cjs" for CORE-01, test/core-loop-contract.test.cjs for CORE-02/03, test/core-loop-root-anchor.test.cjs for CORE-05), mirroring AC-05/AC-12 phrasing. Do NOT renumber or edit any existing AC row; append only. Do NOT introduce a write/delete/move command as a standalone Cmd (mutating loop runs are the Phase-6 step, surrounded by read-only confirms).
  </action>
  <verify>
    <automated>grep -c '^## AC-1[7-9]\|^## AC-2[01]' .planning/ACCEPTANCE-CHECKLIST.md</automated>
  </verify>
  <done>
    .planning/ACCEPTANCE-CHECKLIST.md gains exactly AC-17..AC-21 (one per CORE-01..05), each with the four-field schema, a read-only Cmd (mutating in-Bob loop run wrapped in read-only confirms), and a Confirms field naming the CORE criterion plus its hermetic test complement. Existing AC-01..AC-16 untouched. The grep returns 5.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real installer (execFileSync) | The contract/root-anchor suites spawn the real bin/gsd-bob.cjs against a scratch target — process boundary, scratch-confined. |
| scratch dirs → filesystem | All e2e writes confined to mkdtempSync temp dirs; the tracked workspace `.planning/` must never be written. |
| ACCEPTANCE-CHECKLIST Cmd lines → on-device execution (Phase 6) | The appended `Cmd:` lines run unattended on a real machine — they must be read-only / side-effect-free. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | e2e tests writing into the real workspace `.planning/` | mitigate | Every test uses `fs.mkdtempSync(os.tmpdir(), …)` scratch dirs; the install runs with `--global -c <scratch>` so nothing touches the tracked `.planning/` (V12, D-07). |
| T-04-06 | Tampering | a mutating command leaking into an AC `Cmd:` line | mitigate | Task 3 enforces the T-01-SC read-only invariant — the in-Bob loop run is the Phase-6 mutating step wrapped in read-only confirms (AC-13 precedent); no standalone write/delete/move Cmd is added. |
| T-04-07 | Elevation of privilege | execFileSync invoking the entry with unexpected args | accept | Args are fixed literals (`--bob --global -c <scratch>`); no user-controlled input flows into the spawned command line. Low risk, scratch-confined. |
| T-04-08 | Tampering | nested second `.planning/` under the scope dir | mitigate | Task 2 asserts exactly one root-anchored `.planning/` and `!existsSync(scopeDir/.planning)` after a loop run (CORE-05 / Pitfall 5). |
</threat_model>

<verification>
- `node --test test/core-loop-contract.test.cjs` — e2e emission of core-loop command + skill, PLAN.md/PROJECT.md structure, atomic commits.
- `node --test test/core-loop-root-anchor.test.cjs` — single root-anchored .planning/, no nesting, no .planning/ artifactLayout target.
- `grep -c '^## AC-1[7-9]\|^## AC-2[01]' .planning/ACCEPTANCE-CHECKLIST.md` returns 5.
- `npm test` — full suite green.
- `grep -c 'Result: \[ \] pass' .planning/ACCEPTANCE-CHECKLIST.md` increased by 5 from the pre-plan count.
</verification>

<success_criteria>
- The real installer emits converted .bob/commands/gsd-<name>.md + .bob/skills/gsd-<name>/SKILL.md for the core loop (asserted e2e).
- PLAN.md/PROJECT.md structural contract holds (CORE-02); execute-plan commits are atomic {type}({phase}-{plan}) (CORE-03).
- Exactly one root-anchored .planning/ next to .bob/, no nested second, no .planning/ artifactLayout target (CORE-05).
- AC-17..AC-21 appended, read-only Cmd lines, each naming its CORE criterion + hermetic test complement, AC-01..AC-16 untouched.
- npm test is green.
</success_criteria>

<output>
Create `.planning/phases/04-core-loop-port/04-02-SUMMARY.md` when done.
</output>
