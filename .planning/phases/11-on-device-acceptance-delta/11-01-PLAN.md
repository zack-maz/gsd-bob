---
phase: 11-on-device-acceptance-delta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ACCEPTANCE-CHECKLIST.md
  - test/fixtures/acceptance/frozen-ac01-26.md
  - test/acceptance-delta-coverage.test.cjs
  - test/acceptance-insert-only.test.cjs
autonomous: true
requirements:
  - ACCEPT-01
  - ACCEPT-02
must_haves:
  truths:
    - "AC-28..AC-45 exist: 18 read-only emission-recognition steps, one per newly added Phase 9 command, each Cmd references .bob/commands/gsd-<name>.md via ls/cat only (no functional /gsd- run)"
    - "AC-27's single Confirms line references ACCEPT-02 alongside NEUTRAL-03 (one-token amend; AC-27 sits outside the frozen AC-01..AC-26 range)"
    - "The AC-01..AC-26 step blocks are byte-identical to the committed snapshot test/fixtures/acceptance/frozen-ac01-26.md (insert-only guarantee, SC#3)"
    - "test/acceptance-delta-coverage.test.cjs derives the 28-command set from SUPPORT-ROSTER.md at run time and passes (each gsd-<name>.md referenced by a Cmd line; ACCEPT-01 and ACCEPT-02 both on a Confirms line)"
    - "test/acceptance-insert-only.test.cjs passes (live frozen slice equals the committed fixture, header-anchored)"
    - "test/acceptance-coverage.test.cjs stays green (27->45 AC headers, exactly one Confirms per block, every referenced token declared)"
    - "npm test runs the full suite green (node --test exits 0)"
  artifacts:
    - .planning/ACCEPTANCE-CHECKLIST.md
    - test/fixtures/acceptance/frozen-ac01-26.md
    - test/acceptance-delta-coverage.test.cjs
    - test/acceptance-insert-only.test.cjs
  key_links:
    - "SUPPORT-ROSTER.md `## Supported` bullets -> traceability derivation (drift-proof; never a frozen literal list of 18 names)"
    - "Header anchors `## AC-01 — Subagent isolation` (inclusive) and `## AC-27 — model-neutrality zero-literal grep (NEUTRAL-03)` (exclusive) -> the frozen-slice boundaries"
    - "REQUIREMENTS.md `## Future Requirements` boundary -> declared-id set that keeps test/acceptance-coverage.test.cjs green (ACCEPT-01/ACCEPT-02/CMD-01 all above it)"
---

<objective>
Extend the root-anchored `.planning/ACCEPTANCE-CHECKLIST.md` — insert-only — with device-runnable steps for the two things v2.0 added that the checklist does not yet cover: (1) the 18 newly added Phase 9 commands, as read-only emission-recognition steps `AC-28..AC-45` (ACCEPT-01), and (2) the model-neutrality invariant, discharged by amending the already-present `AC-27` to also cite `ACCEPT-02` (ACCEPT-02 — no duplicate grep). Lock the v1 `AC-01..AC-26` freeze with a hermetic byte-diff test against a committed snapshot fixture (SC#3), and add a drift-proof presence/traceability guard for the 28-command coverage — both without a live Bob.

Purpose: Make ACCEPT-01 and ACCEPT-02 provably discharged and mechanically enforced, while proving the frozen v1 items were never touched. This is a checklist-authoring + `node:test` guard phase — zero runtime code, zero new npm dependencies.
Output: Edited checklist (AC-28..AC-45 + roll-up rows AC-27..AC-45 + execution-order note + AC-27 Confirms amend), one committed snapshot fixture, and two new hermetic `node:test` guards.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-on-device-acceptance-delta/11-CONTEXT.md
@.planning/phases/11-on-device-acceptance-delta/11-RESEARCH.md
</context>

## Artifacts this phase produces

New AC step IDs (in `.planning/ACCEPTANCE-CHECKLIST.md`):
- `AC-28` new-milestone, `AC-29` complete-milestone, `AC-30` milestone-summary, `AC-31` quick, `AC-32` fast, `AC-33` ship, `AC-34` explore, `AC-35` spec-phase, `AC-36` mvp-phase, `AC-37` map-codebase, `AC-38` ui-phase, `AC-39` secure-phase, `AC-40` extract-learnings, `AC-41` docs-update, `AC-42` health, `AC-43` stats, `AC-44` resume-work, `AC-45` pause-work.

New files:
- `test/fixtures/acceptance/frozen-ac01-26.md` — committed snapshot of the frozen AC-01..AC-26 slice.
- `test/acceptance-delta-coverage.test.cjs` — ACCEPT-01 presence/traceability guard (roster-derived).
- `test/acceptance-insert-only.test.cjs` — SC#3 insert-only freeze guard (header-anchored byte-diff).

Edited (in place):
- `.planning/ACCEPTANCE-CHECKLIST.md` — append AC-28..AC-45; add Results Roll-Up rows AC-27..AC-45; add an Execution-Order/How-to-Run read-only note; amend AC-27's `Confirms:` to add `ACCEPT-02`.

## Multi-Source Coverage Audit

| Source | Item | Covered by |
|--------|------|------------|
| GOAL (ROADMAP §11) | Insert-only device-runnable steps for the new commands + neutrality; v1 AC-01..AC-26 frozen | Task 1 (fixture), Task 2 (steps + amend), Task 3 (guards) |
| REQ | ACCEPT-01 — device-runnable steps for the newly added commands | Task 2 (AC-28..AC-45), Task 3 (traceability guard) |
| REQ | ACCEPT-02 — device-runnable model-neutrality step (NEUTRAL-03) | Task 2 (AC-27 Confirms +ACCEPT-02), Task 3 (traceability guard asserts ACCEPT-02) |
| RESEARCH | 18 read-only emission-recognition steps (AC-07/AC-08 pattern, D-01/D-07) | Task 2 |
| RESEARCH | Amend AC-27 Confirms (D-03), extend roll-up/execution-order (D-06) | Task 2 |
| RESEARCH | Committed snapshot fixture + freeze test (D-04), header anchors | Task 1, Task 3 |
| RESEARCH | Drift-proof roster-derived traceability test (D-05), keep coverage green | Task 3 |
| CONTEXT | D-01..D-07, F-01..F-05 | Tasks 1-3 (see task actions) |

No unplanned items. Deferred long-tail commands (transition/ai-integration-phase/autonomy cluster/full parity) are not emitted → correctly no AC step (CONTEXT §Deferred).

<tasks>

<task type="auto">
  <name>Task 1: Capture the pristine AC-01..AC-26 snapshot fixture (SC#3, D-04)</name>
  <files>test/fixtures/acceptance/frozen-ac01-26.md</files>
  <read_first>
    - .planning/ACCEPTANCE-CHECKLIST.md — the file being sliced; note `## AC-01 — Subagent isolation` (currently line 85) and `## AC-27 — model-neutrality zero-literal grep (NEUTRAL-03)` (currently line 267).
    - .planning/phases/11-on-device-acceptance-delta/11-RESEARCH.md §"Pattern 2: Header-anchored deterministic slicing" and §"Insert-only freeze test idiom" — the exact anchors and the capture-first ordering rule.
    - .planning/phases/11-on-device-acceptance-delta/11-CONTEXT.md D-04 — snapshot fixture over git HEAD-diff (hermetic).
  </read_first>
  <action>
    Run this FIRST, before any checklist edit in Task 2, so the fixture captures the pristine AC-01..AC-26 slice (D-04). This is the highest-value guard: capturing before edits means a later accidental edit to the frozen slice is caught by Task 3's freeze test rather than baked in.

    From the repository root, extract the slice between the two header anchors (start inclusive, end exclusive) and write it verbatim to `test/fixtures/acceptance/frozen-ac01-26.md`. Use a Node one-liner so the bytes are exact (locate anchors by `indexOf`, never by hard-coded line offsets — line numbers drift):

    `node -e "const fs=require('fs'),p='.planning/ACCEPTANCE-CHECKLIST.md',md=fs.readFileSync(p,'utf8'),S='## AC-01 — Subagent isolation',E='## AC-27 — model-neutrality zero-literal grep (NEUTRAL-03)',s=md.indexOf(S),e=md.indexOf(E);if(s<0||e<=s)throw new Error('anchors not found or out of order');fs.mkdirSync('test/fixtures/acceptance',{recursive:true});fs.writeFileSync('test/fixtures/acceptance/frozen-ac01-26.md',md.slice(s,e));console.log('fixture bytes:',md.slice(s,e).length);"`

    Both anchor strings use an em-dash (`—`, U+2014) — match the existing headers exactly. The slice captures `AC-01..AC-26` plus the trailing blank line(s) before `## AC-27`; it must NOT include the `## AC-27` header. Do not hand-edit the fixture after writing.
  </action>
  <verify>
    <automated>test -f test/fixtures/acceptance/frozen-ac01-26.md && [ "$(grep -c '^## AC-' test/fixtures/acceptance/frozen-ac01-26.md)" -eq 26 ] && head -1 test/fixtures/acceptance/frozen-ac01-26.md | grep -q '^## AC-01 — Subagent isolation' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test/fixtures/acceptance/frozen-ac01-26.md` exists.
    - It contains exactly 26 `## AC-` headers (AC-01..AC-26); the `## AC-27` header is excluded.
    - Its first line is the start anchor `## AC-01 — Subagent isolation`.
    - It was captured from the checklist BEFORE any Task 2 edit.
  </acceptance_criteria>
  <done>The committed snapshot of the pristine frozen slice exists and is header-bounded to AC-01..AC-26.</done>
</task>

<task type="auto">
  <name>Task 2: Append AC-28..AC-45, extend roll-up/execution-order, amend AC-27 Confirms (ACCEPT-01, ACCEPT-02)</name>
  <files>.planning/ACCEPTANCE-CHECKLIST.md</files>
  <read_first>
    - .planning/ACCEPTANCE-CHECKLIST.md — the insert target: the four-field schema (`Cmd:`/`Expect:`/`Confirms:`/`Result:`), the T-01-SC read-only safety invariant (line 15), AC-07/AC-08 (the read-only emission-recognition template, lines 127-139), the Results Roll-Up table (lines 54-81), the Execution Order (lines 38-48) and How to Run (lines 19-36) sections, and AC-27's `Confirms:` line (line 271).
    - SUPPORT-ROSTER.md — the drift-proof 28-command Supported source; confirm all 18 target commands are present.
    - .planning/phases/11-on-device-acceptance-delta/11-RESEARCH.md §"Code Examples" (the per-command AC shape and the exact AC-27 before/after amend, lines 218-243) and §"Common Pitfalls" (Pitfall 1: exactly one Confirms per block, declared IDs only).
    - .planning/phases/11-on-device-acceptance-delta/11-CONTEXT.md D-01, D-02, D-03, D-06, D-07 and F-01, F-02, F-03.
  </read_first>
  <action>
    Three insert-only edits to `.planning/ACCEPTANCE-CHECKLIST.md`. Do NOT touch any line inside the AC-01..AC-26 step blocks (SC#3 — the frozen slice). All edits below land outside that slice (after AC-27, or in the roll-up/execution-order sections above `## AC-01`).

    EDIT 1 — Append 18 per-command AC blocks (AC-28..AC-45) after AC-27's `Result:` line (currently end of file), each blank-line separated, following the AC-07/AC-08 read-only emission-recognition pattern (D-01, D-07). Each block has EXACTLY these four fields in order (mirror AC-07 verbatim structure; per Pitfall 1 there must be exactly one `Confirms:` line per block or `test/acceptance-coverage.test.cjs` parity breaks):
      - Header: `## AC-<NN> — <name> command emitted + recognized under Bob (read-only) (ACCEPT-01)` — use an em-dash `—`.
      - `Cmd:` — read-only ONLY: `On a real Bob machine, read-only inspect the emitted Bob slash command for the ` + "`<name>`" + ` command and confirm Bob recognizes it. List and read (no edits): ` + "`ls .bob/commands/gsd-<name>.md`" + ` then ` + "`cat .bob/commands/gsd-<name>.md`" + `, and observe the command in Bob's slash-command palette / listing. No file is written, moved, or deleted.`
      - `Expect:` — "`.bob/commands/gsd-<name>.md`" + ` exists with ` + "`description:`" + ` (and, where applicable, ` + "`argument-hint:`" + `) frontmatter and a ` + "`$1`" + ` positional-arg body; Bob lists the command and shows its description as ` + "`/gsd-<name>`" + ` (hyphen form).`
      - `Confirms:` (exactly one line): `Confirms: ACCEPT-01, CMD-01 — device-runnable emission/recognition step for the newly added Phase 9 command ` + "`<name>`" + `.`
      - `Result: [ ] pass  [ ] fail`

      The `Cmd:` line MUST use only `ls`/`cat` (read-only). NEVER a functional `/gsd-<name>` invocation, `npx`, `--uninstall`, or any write/install/delete verb — several of these commands mutate irreversibly or open PRs (F-03, T-01-SC).

      AC-number -> command-name mapping (roster/CMD-01 order — use exactly this): AC-28 new-milestone, AC-29 complete-milestone, AC-30 milestone-summary, AC-31 quick, AC-32 fast, AC-33 ship, AC-34 explore, AC-35 spec-phase, AC-36 mvp-phase, AC-37 map-codebase, AC-38 ui-phase, AC-39 secure-phase, AC-40 extract-learnings, AC-41 docs-update, AC-42 health, AC-43 stats, AC-44 resume-work, AC-45 pause-work. The `.md` filename in each `Cmd:` line must be `gsd-<name>.md` (full filename — the traceability test matches on the filename to avoid prefix collisions).

    EDIT 2 — Extend the Results Roll-Up table (lines 54-81, above `## AC-01`, outside the frozen slice). Append rows `AC-27` through `AC-45` (AC-27 is currently missing from the roll-up — a Phase 8 omission — add it too so every AC step has a row). Each row: `| AC-<NN> | [ ] pass  [ ] fail | |`. After this edit the table holds 45 data rows (AC-01..AC-45), matching the 45 AC step headers.

    EDIT 3a — Execution Order note (lines 38-48, outside the frozen slice, D-06). Extend the "Read-only first (any order)" item so it also lists the new read-only steps, e.g. add: `, AC-27 (model-neutrality grep), and AC-28..AC-45 (per-command emission-recognition, ls/cat only)`. Keep it a one-line note; do not reorder the mutating-steps list.

    EDIT 3b — How to Run "Read-only by default (T-01-SC preserved)" sentence (line 36, outside the frozen slice). Extend the enumerated read-only steps to include `AC-27..AC-45` for consistency (they add no new mutating `Cmd:`), preserving the read-only-by-default posture wording.

    EDIT 4 — Amend AC-27's single `Confirms:` line to add `ACCEPT-02` (D-03, F-01, F-02 — AC-27 is outside the frozen range, so this one-token edit is permitted; do NOT add a duplicate grep step). Change the leading token list from `NEUTRAL-03` to `NEUTRAL-03, ACCEPT-02`, leaving the rest of the line byte-identical. Result: `Confirms: NEUTRAL-03, ACCEPT-02 — zero model literals across the emitted ` + "`.bob/`" + ` converted set, guarding regressions / SC#3. (on-device complement to test/model-neutrality.test.cjs, which enforces the same invariant hermetically via the real staging path)`. Keep AC-27 to exactly one `Confirms:` line.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && [ "$(grep -c '^## AC-' .planning/ACCEPTANCE-CHECKLIST.md)" -eq 45 ] && [ "$(grep -cE '^\| AC-[0-9]' .planning/ACCEPTANCE-CHECKLIST.md)" -eq 45 ] && grep -q '^Confirms: NEUTRAL-03, ACCEPT-02' .planning/ACCEPTANCE-CHECKLIST.md && grep -q 'gsd-new-milestone.md' .planning/ACCEPTANCE-CHECKLIST.md && grep -q 'gsd-pause-work.md' .planning/ACCEPTANCE-CHECKLIST.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## AC-' .planning/ACCEPTANCE-CHECKLIST.md` returns 45 (AC-01..AC-45).
    - `grep -cE '^\| AC-[0-9]' .planning/ACCEPTANCE-CHECKLIST.md` returns 45 (roll-up rows AC-01..AC-45).
    - AC-27 has exactly one `Confirms:` line and it now reads `Confirms: NEUTRAL-03, ACCEPT-02 — ...` (rest byte-identical).
    - All 18 `gsd-<name>.md` filenames (gsd-new-milestone.md .. gsd-pause-work.md) appear in an AC `Cmd:` line.
    - No AC-28..AC-45 `Cmd:` line contains a functional `/gsd-` invocation, `npx`, `--uninstall`, or a write/install/delete verb (T-01-SC held) — verify with the phase `<verification>` read-only guard.
    - No line inside the AC-01..AC-26 step blocks changed (verified by Task 3's freeze test).
  </acceptance_criteria>
  <done>The checklist carries AC-28..AC-45 (read-only, one Confirms each), a complete 45-row roll-up, read-only execution-order/how-to-run notes, and an AC-27 Confirms that cites ACCEPT-02 — all insert-only.</done>
</task>

<task type="auto">
  <name>Task 3: Add the two hermetic guards and prove the whole suite green (SC#1, SC#3)</name>
  <files>test/acceptance-delta-coverage.test.cjs, test/acceptance-insert-only.test.cjs</files>
  <read_first>
    - .planning/phases/11-on-device-acceptance-delta/11-RESEARCH.md §"Code Examples" — the "Traceability test harness idiom" (lines 245-286) and the "Insert-only freeze test idiom" (lines 288-316); mirror these exactly (they are the verified source).
    - test/acceptance-coverage.test.cjs — the derive-never-freeze convention to mirror and the suite that must stay green (parity: one Confirms per AC block; declared-id-only tokens).
    - test/_helpers/vendor.cjs — the `repoRoot` helper both new tests import.
    - SUPPORT-ROSTER.md — the `## Supported` / `## Unsupported` section boundaries the traceability test slices between.
    - test/fixtures/acceptance/frozen-ac01-26.md — the snapshot the freeze test compares against (from Task 1).
    - .planning/phases/11-on-device-acceptance-delta/11-CONTEXT.md D-05, D-04, F-04.
  </read_first>
  <action>
    Add two builtin-only (`node:test` / `node:assert/strict` / `node:fs` / `node:path` + `repoRoot`) test files. No new npm dependency. Both derive their inputs at run time (anti-drift, D-05) — never freeze a literal list of the 18 names.

    FILE A — `test/acceptance-delta-coverage.test.cjs` (ACCEPT-01 presence/traceability, D-05). Mirror the RESEARCH §"Traceability test harness idiom". Constants: `ROSTER = repoRoot/SUPPORT-ROSTER.md`, `CHECKLIST = repoRoot/.planning/ACCEPTANCE-CHECKLIST.md`. Implement `supportedCommands()`: read the roster, slice from `## Supported` to `## Unsupported`, fail-closed assert both markers present and ordered, then match bullet lines against `/^- (gsd-[a-z0-9-]+)\s*$/` and return the names (28 today). Test 1 ("every Supported command has >=1 AC step referencing its emitted artifact"): assert `cmds.length >= 28`, then for each `c` assert `checklist.includes(c + '.md')` with a message naming the missing command (match the full `.md` filename to avoid prefix collisions — Pitfall 3). Test 2 ("both phase reqs referenced by a Confirms line"): for `id` in `['ACCEPT-01', 'ACCEPT-02']` assert `checklist.includes(id)`. This composes with — does not fork — `acceptance-coverage.test.cjs`.

    FILE B — `test/acceptance-insert-only.test.cjs` (SC#3 freeze, D-04). Mirror the RESEARCH §"Insert-only freeze test idiom". Constants: `CHECKLIST = repoRoot/.planning/ACCEPTANCE-CHECKLIST.md`, `FROZEN = repoRoot/test/fixtures/acceptance/frozen-ac01-26.md`, `START = '## AC-01 — Subagent isolation'`, `END = '## AC-27 — model-neutrality zero-literal grep (NEUTRAL-03)'` (em-dashes exact). Implement `frozenSlice(md)`: `indexOf` both anchors, fail-closed assert `s >= 0 && e > s`, return `md.slice(s, e)`. Test ("AC-01..AC-26 step blocks are byte-unchanged (insert-only)"): `assert.equal(frozenSlice(liveChecklist), frozenFixture, 'the AC-01..AC-26 frozen slice diverged from the committed snapshot')`.

    Then run the full suite and confirm nothing regressed: `acceptance-coverage.test.cjs` (27->45 headers, still exactly one Confirms per block, ACCEPT-01/ACCEPT-02/CMD-01 all declared) and `model-neutrality.test.cjs` must stay green.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node --test test/acceptance-delta-coverage.test.cjs && node --test test/acceptance-insert-only.test.cjs && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/acceptance-delta-coverage.test.cjs` exits 0 (all 28 Supported commands have a `.md` reference; ACCEPT-01 and ACCEPT-02 both present).
    - `node --test test/acceptance-insert-only.test.cjs` exits 0 (live frozen slice equals the committed fixture).
    - `npm test` exits 0 — the full suite is green, including `test/acceptance-coverage.test.cjs` and `test/model-neutrality.test.cjs` (neither forked, neither regressed).
    - Both new tests derive their sets at run time (roster bullets / header anchors), with no frozen literal list of the 18 names.
  </acceptance_criteria>
  <done>Two hermetic guards mechanically enforce ACCEPT-01 coverage and the AC-01..AC-26 insert-only freeze, and the whole `npm test` suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checklist `Cmd:` line -> user's real machine shell | The checklist is run unattended on real hardware; any mutating `Cmd:` would execute with real, possibly irreversible side effects (PR creation, roadmap rewrite, uninstall). |
| committed markdown/fixtures -> CI `node:test` runner | Trusted, repo-local, read-only doc parsing; no external/untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | AC-28..AC-45 `Cmd:` lines in `.planning/ACCEPTANCE-CHECKLIST.md` | high | mitigate | Every new step is read-only emission-recognition (`ls`/`cat` only), never a functional `/gsd-<name>` run or any write/install/delete verb (T-01-SC, D-07, F-03). Enforced by the phase `<verification>` read-only guard that scopes to the AC-28..EOF region and fails on any mutating token. |
| T-11-02 | Tampering | Frozen AC-01..AC-26 step blocks (SC#3) | high | mitigate | `test/acceptance-insert-only.test.cjs` byte-diffs the header-anchored live slice against the committed `frozen-ac01-26.md` fixture; any edit (even whitespace) inside the frozen slice fails CI. Fixture captured from the pristine slice BEFORE any edit (Task 1 first). |
| T-11-03 | Spoofing | Traceability coverage claim (ACCEPT-01) | medium | mitigate | The presence test derives the 28-command set from the generated `SUPPORT-ROSTER.md` at run time and matches full `gsd-<name>.md` filenames (Pitfall 3), so a missing or renamed step cannot pass as a false green. |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs ZERO packages — builtin `node:test`/`node:assert`/`node:fs`/`node:path` + the existing `repoRoot` helper only (RESEARCH §Package Legitimacy Audit: N/A). No supply-chain surface introduced; no `[ASSUMED]`/`[SUS]`/`[SLOP]` packages. |
</threat_model>

<verification>
Run from the repository root after all three tasks:

1. Full suite green: `npm test` exits 0 (runs `node --test "test/**/*.test.cjs"`), including the two new guards and the unchanged `acceptance-coverage.test.cjs` + `model-neutrality.test.cjs`.
2. Structure: `grep -c '^## AC-' .planning/ACCEPTANCE-CHECKLIST.md` == 45 and `grep -cE '^\| AC-[0-9]' .planning/ACCEPTANCE-CHECKLIST.md` == 45.
3. Freeze: `grep -c '^## AC-' test/fixtures/acceptance/frozen-ac01-26.md` == 26; `node --test test/acceptance-insert-only.test.cjs` exits 0.
4. ACCEPT-02 discharge: `grep -q '^Confirms: NEUTRAL-03, ACCEPT-02' .planning/ACCEPTANCE-CHECKLIST.md` succeeds; AC-27 still has exactly one `Confirms:` line.
5. T-01-SC read-only guard (no mutating `Cmd:` in the delta region): run
   `node -e "const fs=require('fs');const md=fs.readFileSync('.planning/ACCEPTANCE-CHECKLIST.md','utf8');const i=md.indexOf('## AC-28');if(i<0)process.exit(1);const bad=md.slice(i).split('\n').filter(l=>l.startsWith('Cmd:')&&/(\/gsd-[a-z]|npx |--uninstall|\brm \b|\bmv \b|\bcp \b|>\s|install\b)/.test(l));if(bad.length){console.error('mutating Cmd in delta steps:',bad);process.exit(1);}console.log('delta read-only OK, Cmd lines:',md.slice(i).split('\n').filter(l=>l.startsWith('Cmd:')).length);"`
   exits 0 (18 read-only `Cmd:` lines, zero mutating).
</verification>

<success_criteria>
- SC#1 (ACCEPT-01): AC-28..AC-45 add insert-only device-runnable read-only steps for each of the 18 newly added Phase 9 commands, appended after AC-27; `test/acceptance-delta-coverage.test.cjs` proves every Supported command (all 28) has a referencing AC step.
- SC#2 (ACCEPT-02): the checklist's model-neutrality step (AC-27, the NEUTRAL-03 zero-literal grep) is made traceable to ACCEPT-02 via the one-token Confirms amend; the traceability test asserts ACCEPT-02 is present. No duplicate grep step added.
- SC#3 (insert-only): the v1 AC-01..AC-26 step blocks are byte-unchanged, mechanically enforced by `test/acceptance-insert-only.test.cjs` against the committed snapshot fixture.
- `test/acceptance-coverage.test.cjs` stays green; `npm test` full suite exits 0. Zero runtime code changed, zero new npm dependencies.
</success_criteria>

<output>
Create `.planning/phases/11-on-device-acceptance-delta/11-01-SUMMARY.md` when done.
</output>
