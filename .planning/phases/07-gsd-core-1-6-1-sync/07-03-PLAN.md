---
phase: 07-gsd-core-1-6-1-sync
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - test/installer/staged-shim-loads.test.cjs
  - UPSTREAM.md
  - README.md
  - src/installer/stage.cjs
  - .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md
autonomous: true
requirements: [SYNC-01, SYNC-02, SYNC-03]
must_haves:
  truths:
    - "node --test test/backend-neutrality.test.cjs test/descriptor.test.cjs passes 10/10 UNMODIFIED (D-09 invariants; the shim still resolves the bob home under the 1.6.1 bin)"
    - "`npm test` is green, OR every failure beyond the recorded 186/3 baseline is classified per D-08 as an expected-drift with a one-line justification in 07-REVENDOR-NOTES.md (keyed by fixture name), never a blanket regenerate"
    - "test/installer/staged-shim-loads.test.cjs asserts the staged package.json version is 1.6.1 (the one guaranteed expected-drift)"
    - "UPSTREAM.md targets gsd-core 1.6.1, all 5 pointers are re-verified against the 1.6.1 source, the runtime-name-policy alias is ADDED as a documented artifact, and the false 'no new converter code / already exist upstream' framing is corrected to reflect the converters are a vendored hand-edit"
    - "No 1.5.0 residue remains in README.md, src/installer/stage.cjs (comment), or UPSTREAM.md (SYNC-01 full sweep outside the payload)"
  artifacts:
    - UPSTREAM.md (updated to 1.6.1 + re-pointed + corrected framing + 6th alias artifact)
    - test/installer/staged-shim-loads.test.cjs (version fixture 1.6.1)
    - .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md (finalized)
  key_links:
    - "07-REVENDOR-NOTES.md drift justifications keyed by fixture name + the SYNC-03 pointer re-verification seed the Phase 10 MAINTAINING runbook (DOCS-04)"
    - "test/backend-neutrality.test.cjs brace-walks the re-injected bob registry block; test/descriptor.test.cjs resolves the .bob home — both must pass unmodified"
---

<objective>
Re-validate the Bob integration against the 1.6.1 bin layer using the EXISTING test harness, apply the golden-drift policy, correct UPSTREAM.md to 1.6.1 with re-verified pointers and honest converter framing, and sweep the last 1.5.0 residue from outside the payload — finalizing the live re-vendor notes that seed the Phase 10 runbook.

Purpose: This closes SYNC-02 (suites green or drift-justified; invariants pass unmodified; shim resolves the bob home) and SYNC-03 (UPSTREAM.md → 1.6.1, 5 pointers re-verified + the undocumented alias added + the "stock converter" framing corrected), and completes the SYNC-01 version sweep outside gsd-core/.

Output: updated UPSTREAM.md, the one guaranteed golden-fixture update (staged-shim-loads version 1.6.1), README/stage.cjs 1.5.0 sweep, and finalized 07-REVENDOR-NOTES.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gsd-core-1-6-1-sync/07-CONTEXT.md
@.planning/phases/07-gsd-core-1-6-1-sync/07-RESEARCH.md
@.planning/phases/07-gsd-core-1-6-1-sync/07-PATTERNS.md
@.planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md
@UPSTREAM.md
</context>

<artifacts_produced>
Files this plan creates/updates (exclude from drift/source-grounding as newly-produced):
- `test/installer/staged-shim-loads.test.cjs` — version fixture bumped 1.5.0→1.6.1 (the one guaranteed golden drift)
- `UPSTREAM.md` — 1.6.1 target, re-pointed 5-artifact inventory + added name-policy alias + corrected converter framing
- `README.md`, `src/installer/stage.cjs` — 1.5.0→1.6.1 residue sweep (README L93 text; stage.cjs L239 comment)
- finalized `.planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Run the suites unchanged, classify drift (D-08), and confirm the invariants pass unmodified (D-09)</name>
  <files>test/installer/staged-shim-loads.test.cjs, .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md</files>
  <read_first>
    - test/installer/staged-shim-loads.test.cjs (L65 asserts staged package.json version === '1.5.0' — the guaranteed drift)
    - test/backend-neutrality.test.cjs, test/descriptor.test.cjs (the D-09 invariants — never regenerated)
    - .planning/phases/07-gsd-core-1-6-1-sync/07-RESEARCH.md (§"Golden-Fixture Drift Map", §6 baseline invariant run, Pitfall 3, D-08/D-09)
    - .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md (the recorded 186/3 baseline from Plan 01)
  </read_first>
  <action>
    Run the existing suites UNCHANGED against the 1.6.1 payload FIRST, then classify (D-08 — never blanket-regenerate, that would mask a real converter break, Pitfall 3):

    (1) Run the D-09 invariants first: `node --test test/backend-neutrality.test.cjs test/descriptor.test.cjs`. These MUST pass 10/10 UNMODIFIED — backend-neutrality brace-walks the re-injected bob registry block (confirming zero brand literals survived the re-injection) and descriptor confirms the shim resolves the .bob home under the 1.6.1 bin (the SYNC-02 shim path). If either fails, that is a REGRESSION to fix in this phase (a broken re-injection), NOT a fixture to edit.

    (2) Run the full suite: `npm test`. Diff the result against the recorded 186/3 baseline. For EACH failure beyond the baseline, classify:
    - Expected drift (1.6.1 source content flowing correctly through an unchanged converter, or a verified-correct helper-behavior change) → regenerate ONLY that fixture and record a one-line justification in 07-REVENDOR-NOTES.md keyed by fixture name.
    - Regression (converter/descriptor behavior actually broke) → fix it here; do not paper over with a fixture edit.
    The 3 pre-existing baseline failures (acceptance-coverage.test.cjs, core-loop-contract.test.cjs) are environmental noise — do NOT touch them (Pitfall 5).

    (3) Apply the ONE guaranteed expected-drift (RESEARCH Drift Map): update test/installer/staged-shim-loads.test.cjs L65 so it asserts the staged package.json version is `1.6.1` (was `1.5.0`). Record the justification: "vendored gsd-core version legitimately bumped 1.5.0→1.6.1 (SYNC-01)". Per RESEARCH the other goldens (skill-golden, command-golden, text-mode-golden) should stay GREEN because the converters were re-injected verbatim and their helpers (yamlQuote, transformContentToHyphen, readCmdNames) survive — if one of those DOES drift, classify it (diff the helper: correct new behavior = expected-drift + regenerate + justify; otherwise regression + fix). Note test/installer/stage.test.cjs writes its OWN hermetic VERSION fixture — that is NOT drift.

    Record in 07-REVENDOR-NOTES.md: the post-vendor `npm test` totals, the invariant result (10/10), and every drift classification with its justification keyed by fixture name (D-11).
  </action>
  <verify>
    <automated>node --test test/backend-neutrality.test.cjs test/descriptor.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/backend-neutrality.test.cjs test/descriptor.test.cjs` passes 10/10 with those two files UNMODIFIED.
    - `npm test` was run unchanged first; every failure beyond the 186/3 baseline is classified as expected-drift (regenerated + one-line justification) or regression (fixed) — no blanket regeneration.
    - test/installer/staged-shim-loads.test.cjs asserts version 1.6.1.
    - 07-REVENDOR-NOTES.md records the post-vendor totals, the invariant 10/10 result, and drift justifications keyed by fixture name.
  </acceptance_criteria>
  <done>The 1.6.1 payload passes the existing suites (or every drift is justified per D-08), the D-09 invariants pass unmodified, and the guaranteed version-fixture drift is applied — all recorded in the re-vendor notes.</done>
</task>

<task type="auto">
  <name>Task 2: SYNC-03 — update UPSTREAM.md to 1.6.1 with re-verified pointers, the added alias artifact, and corrected converter framing</name>
  <files>UPSTREAM.md, .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md</files>
  <read_first>
    - UPSTREAM.md (L9 targeted version; L21–27 "No new converter code" claim; L49–83 the 5-artifact table + "What a maintainer actually lifts")
    - gsd-core/bin/lib/capability-registry.cjs (locate the re-injected bob block's new line range in the 1.6.1 file, inside `const runtimes`)
    - gsd-core/bin/lib/runtime-artifact-conversion.cjs (locate the re-injected converter impls + export lines in the 1.6.1 file)
    - gsd-core/bin/shared/runtime-aliases.manifest.json, gsd-core/bin/lib/runtime-name-policy.cjs (the two alias sites), gsd-core/bin/lib/runtime-homes.cjs (the dot-home case)
    - .planning/phases/07-gsd-core-1-6-1-sync/07-RESEARCH.md (§8 UPSTREAM re-verification table, State-of-the-Art converter correction)
  </read_first>
  <action>
    Update UPSTREAM.md for SYNC-03 — this is MORE than a line-number refresh (RESEARCH §8). Do all four:
    1. Bump the "Targeted gsd-core version" line (L9) from `1.5.0` to `1.6.1`, and the "(gsd-core `1.5.0`)" mention at L51.
    2. Re-verify and re-point ALL 5 inventory pointers to their ACTUAL new file:line locations in the re-vendored 1.6.1 source (grep/inspect each site — the registry block is now inside `const runtimes`; the converter impls + exports relocated; the alias JSON tail; the dot-home case in runtime-homes.cjs). Record the actual observed line numbers; do not copy the 1.5.0 numbers.
    3. ADD the undocumented 4th data patch to the inventory: `runtime-name-policy.cjs` `bob: ['bob','bob-cli']` (the FALLBACK_ALIASES entry) — currently missing from the move inventory (RESEARCH §1/§8).
    4. CORRECT the false framing (current L21–24 "No new converter code" and L73–74 "They already exist upstream"): the Bob converters are LOCAL ~105-line hand-edited code (in-file banner `gsd-bob HAND-EDIT to this GENERATED file`), NEVER stock upstream. Reframe honestly: the data patches (registry + two aliases) are moves; the converters are a small parameterized rewrite a maintainer would fold into gsd-core's per-runtime converter family. Keep the backend-neutrality guarantee section accurate.

    Log the SYNC-03 pointer re-verification results (each artifact's old → new file:line) into 07-REVENDOR-NOTES.md (D-11) — this seeds the Phase 10 runbook.
  </action>
  <verify>
    <automated>grep -q '1\.6\.1' UPSTREAM.md && ! grep -q '1\.5\.0' UPSTREAM.md && grep -q 'runtime-name-policy' UPSTREAM.md && echo "upstream ok"</automated>
  </verify>
  <acceptance_criteria>
    - UPSTREAM.md targeted version reads 1.6.1; no 1.5.0 remains in the file.
    - All 5 inventory pointers show re-verified 1.6.1 file:line locations (not the stale 1.5.0 numbers).
    - runtime-name-policy.cjs bob alias is added to the inventory as a documented artifact.
    - The "no new converter code / already exist upstream" claim is replaced with the accurate hand-edited-converter framing.
    - 07-REVENDOR-NOTES.md records the old→new pointer re-verification for each artifact.
  </acceptance_criteria>
  <done>UPSTREAM.md accurately records gsd-core 1.6.1 with re-verified pointers, the previously-undocumented name-policy alias, and an honest converter framing — reviewable as a maintainer artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Final 1.5.0 sweep outside the payload + finalize the re-vendor notes</name>
  <files>README.md, src/installer/stage.cjs, .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md</files>
  <read_first>
    - README.md (L93 "gsd-core `1.5.0`" text)
    - src/installer/stage.cjs (L239 comment mentioning "gsd-core/VERSION → 1.5.0"; the code at L242 reads VERSION dynamically — the comment is cosmetic)
    - .planning/phases/07-gsd-core-1-6-1-sync/07-RESEARCH.md (§7 version-consistency surface, Open Question 2 — sweep stage.cjs comment + README/UPSTREAM; leave historical .tgz alone)
  </read_first>
  <action>
    Complete the SYNC-01 version-consistency sweep OUTSIDE the payload (the payload itself was cleared in Plan 02):
    - README.md L93: change the "gsd-core `1.5.0`" reference to `1.6.1`.
    - src/installer/stage.cjs L239: update the stale `1.5.0` in the comment to `1.6.1` (hygiene only — the code reads VERSION dynamically at L242, so this is non-functional but keeps the tree consistent).
    - Do NOT touch the historical root `.tgz` publish artifacts (out of scope, RESEARCH Open Question 2 / Runtime State Inventory) and do NOT touch test/installer/stage.test.cjs's hermetic self-written VERSION fixture (not drift).

    Assert the SYNC-01 full check one more time: `gsd-core/VERSION`=1.6.1 and `grep -rn '1\.5\.0' gsd-core/` empty (payload), plus no 1.5.0 in README.md / UPSTREAM.md / stage.cjs comment (outside-payload). Note the remaining 1.5.0 mentions that are LEGITIMATELY out of scope (historical .tgz filenames, archived .planning/ phase docs) so a reviewer knows they are intentional.

    Finalize 07-REVENDOR-NOTES.md (D-10/D-11): ensure it contains the complete chronological command log, all gotchas/dead-ends encountered, the D-03 converter re-verification verdict, the D-08 fixture-drift justifications keyed by fixture name, and the SYNC-03 pointer re-verification — the raw source Phase 10 turns into the MAINTAINING runbook (DOCS-04). Add a short closing "Runbook seed" summary listing the replayable recipe: npm pack → nuke → restage → run apply-bob-patches.cjs → run suites + drift policy → update UPSTREAM.md → version-consistency check.
  </action>
  <verify>
    <automated>! grep -q '1\.5\.0' README.md && ! grep -Rn '1\.5\.0' gsd-core/ && test "$(cat gsd-core/VERSION)" = "1.6.1" && grep -q 'apply-bob-patches' .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md && echo "sweep + notes ok"</automated>
  </verify>
  <acceptance_criteria>
    - README.md and src/installer/stage.cjs no longer carry a 1.5.0 version reference (README to 1.6.1; stage.cjs comment to 1.6.1).
    - `grep -rn '1\.5\.0' gsd-core/` is empty and gsd-core/VERSION=1.6.1 (SYNC-01 fully clean in the payload).
    - Any remaining 1.5.0 mentions (historical .tgz, archived phase docs) are noted as intentionally out of scope.
    - 07-REVENDOR-NOTES.md is finalized with command log, gotchas, D-03 verdict, D-08 drift justifications, SYNC-03 re-verification, and a closing replayable-recipe summary.
  </acceptance_criteria>
  <done>All 1.5.0 version residue outside the payload is swept to 1.6.1 (excluding intentional historical artifacts), and 07-REVENDOR-NOTES.md is a complete live record ready to seed the Phase 10 runbook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| re-injected registry block → backend-neutrality invariant | The invariant must confirm no brand literal survived the re-injection |
| documentation (UPSTREAM.md) → maintainer | Inaccurate pointers/framing would mislead an upstream reviewer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering | golden-drift blanket regeneration | medium | mitigate | D-08 classify-not-regenerate policy; invariants (D-09) never regenerated; each drift carries a keyed justification |
| T-07-06 | Repudiation | UPSTREAM.md pointer accuracy | low | mitigate | Every pointer re-verified against the actual re-vendored source and logged old→new in the notes |
| T-07-07 | Information disclosure | backend-neutrality regression | high | mitigate | test/backend-neutrality.test.cjs must pass unmodified — confirms zero brand literals in the re-injected bob block (RUNTIME-04 cross-cutting invariant) |
</threat_model>

<verification>
- `node --test test/backend-neutrality.test.cjs test/descriptor.test.cjs` passes 10/10 unmodified (D-09; shim resolves bob home).
- `npm test` green or every non-baseline failure justified per D-08; staged-shim-loads asserts 1.6.1.
- UPSTREAM.md → 1.6.1, 5 pointers re-verified, name-policy alias added, converter framing corrected (SYNC-03).
- No 1.5.0 residue in gsd-core/, README.md, UPSTREAM.md, or the stage.cjs comment (SYNC-01 full sweep).
- 07-REVENDOR-NOTES.md finalized as the Phase 10 runbook seed.
</verification>

<success_criteria>
- SYNC-02: descriptor + converter suites re-validated green (or drift-justified) against the 1.6.1 bin; invariants pass unmodified; shim resolves the bob home.
- SYNC-03: UPSTREAM.md records 1.6.1 with re-verified pointers, the added alias artifact, and honest converter framing.
- SYNC-01: one consistent version everywhere — no 1.5.0 residue in the payload or its adjacent docs/tests.
</success_criteria>

<output>
Create `.planning/phases/07-gsd-core-1-6-1-sync/07-03-SUMMARY.md` when done.
</output>
