---
phase: 07-gsd-core-1-6-1-sync
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - gsd-core/
  - gsd-core/VERSION
  - gsd-core/bin/lib/capability-registry.cjs
  - gsd-core/bin/lib/runtime-artifact-conversion.cjs
  - gsd-core/bin/lib/runtime-name-policy.cjs
  - gsd-core/bin/shared/runtime-aliases.manifest.json
  - .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md
autonomous: true
requirements: [SYNC-01, SYNC-02]
must_haves:
  truths:
    - "gsd-core/VERSION reads 1.6.1 and `grep -rn '1\\.5\\.0' gsd-core/` returns nothing (SYNC-01 payload version-consistency, D-07)"
    - "1.6.1-only new files exist under gsd-core/ (e.g. workflows/list-seeds.md), proving a wholesale replace not a partial merge"
    - "require('./gsd-core/bin/lib/runtime-artifact-conversion.cjs') exposes convertClaudeCommandToBobSkill AND convertClaudeCommandToBobCommand (D-03 converter re-injection verified)"
    - "require('./gsd-core/bin/lib/capability-registry.cjs') resolves with a bob runtime entry; runtime-aliases.manifest.json and runtime-name-policy.cjs both carry the bob alias"
    - "A second run of apply-bob-patches.cjs produces zero file changes (git diff clean on gsd-core/) — the D-02 idempotency contract is proven at runtime"
    - "The colon→hyphen normalization is at a fixed point — a second `apply-bob-patches.cjs` run leaves the gsd-core/ doc tree byte-identical, so no real command-form `gsd:<cmd>` survives (intentional `gsd:...`/`gsd:X` placeholders and stock `bin/` colon strings are expected, NOT failures) — AND `grep -rn '~/\\.claude' gsd-core/workflows gsd-core/references gsd-core/templates gsd-core/contexts` returns nothing (SYNC-01 normalization guarantee, .md doc-tree scoped)"
  artifacts:
    - gsd-core/ (restaged pristine 1.6.1 payload + re-injected local deltas)
    - gsd-core/VERSION (1.6.1)
  key_links:
    - "src/installer/stage.cjs L242 reads gsd-core/VERSION — the write must exist or stage.cjs throws ENOENT"
    - "src/installer/stage.cjs L268 require-destructures convertClaudeCommandToBobCommand + convertClaudeCommandToBobSkill — both must be re-exported or the convertible loop crashes"
---

<objective>
Execute the destructive re-vendor: obtain the immutable 1.6.1 tarball, nuke-and-restage the curated `gsd-core/` payload wholesale, run the Plan-01 script to re-inject all six local deltas, and re-verify the two SYNC-02 integration seams (converter existence + shim/require resolution) plus the SYNC-01 payload version-consistency — logging every command and result live into 07-REVENDOR-NOTES.md.

Purpose: This is the SYNC-01 core (one consistent 1.6.1 payload, no 1.5.0/1.6.1 mix) and the SYNC-02 integration re-validation at the seam level. Nuke-and-restage (D-05) is the only mechanic that guarantees "no residual mix"; the curated-subset boundary stays unchanged (D-06).

Output: the restaged 1.6.1 `gsd-core/` payload with local deltas re-applied, `gsd-core/VERSION`=1.6.1, and the live command log + D-03 result in 07-REVENDOR-NOTES.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gsd-core-1-6-1-sync/07-CONTEXT.md
@.planning/phases/07-gsd-core-1-6-1-sync/07-RESEARCH.md
@.planning/phases/07-gsd-core-1-6-1-sync/07-PATTERNS.md
@.planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md
@src/installer/stage.cjs
@scripts/apply-bob-patches.cjs
</context>

<artifacts_produced>
Files this plan creates/regenerates (exclude from drift/source-grounding as newly-produced):
- restaged `gsd-core/` payload at 1.6.1 (all subdirs: bin, contexts, references, templates, workflows + new 1.6.1 files)
- `gsd-core/VERSION` (1.6.1) — written by apply-bob-patches.cjs (tarball ships none)
- re-injected local deltas in capability-registry.cjs, runtime-artifact-conversion.cjs, runtime-name-policy.cjs, runtime-aliases.manifest.json
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Pack and nuke-and-restage the pristine 1.6.1 curated subset</name>
  <files>gsd-core/, .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md</files>
  <read_first>
    - src/installer/stage.cjs (L129 payloadSrc = repoRoot/gsd-core; L217 recursive whole-dir copy — confirms the curated subset IS whatever lives under gsd-core/)
    - scripts/generate-support-roster.cjs (confirm it derives from commands/gsd/*.md, NOT the gsd-core payload — the roster input is unchanged by the re-vendor)
    - .planning/phases/07-gsd-core-1-6-1-sync/07-RESEARCH.md (§5 curated subset, §"Don't Hand-Roll" npm pack, §"Environment Availability", D-04/D-05/D-06)
  </read_first>
  <action>
    Per D-04, obtain the immutable payload with `npm pack @opengsd/gsd-core@1.6.1` into a scratch dir (e.g. a temp dir under the OS tmp), then extract the tarball. Inspect the extracted layout to confirm the payload root path (the tarball extracts under `package/`; gsd-core's own payload lives at `package/gsd-core/{bin,contexts,references,templates,workflows}` — confirm with a directory listing before copying, do not assume).

    Per D-05 (nuke-and-restage, the only mechanic guaranteeing SYNC-01 no-mix): delete the tracked vendored subdirs under gsd-core/ (bin, contexts, references, templates, workflows) — the whole curated subset — then recursively copy the identical curated subset from the extracted tarball into gsd-core/. Per D-06 keep the curated-subset boundary unchanged: restage the same five subdirs; only include a NEW top-level dir if 1.6.1 adds one the shim/workflows actually reference (RESEARCH §5 verified the directory structure is identical — no new top-level or bin/ subdir — so no new dir is expected). Do NOT restage gsd-core/VERSION from the tarball (it ships none); the Plan-01 script writes it in Task 2.

    Log EVERY exact command live into 07-REVENDOR-NOTES.md as-it-happens (D-10) under the Command Log: the npm pack command + resolved tarball filename, the extract command, the confirmed payload root path, the nuke commands, and the restage copy command. Record any gotcha/dead-end in the moment (e.g. an unexpected tarball layout).

    Verify the wholesale replace landed: confirm at least one 1.6.1-only new file is present (e.g. gsd-core/workflows/list-seeds.md) and that the five subdirs are populated. At THIS point (before the patch script runs) the payload is pristine 1.6.1: expect the colon command form and ~/.claude to be present (they are re-normalized in Task 2) and expect the two Bob converters + VERSION to be ABSENT (re-injected in Task 2).
  </action>
  <verify>
    <automated>test -f gsd-core/workflows/list-seeds.md && for d in bin contexts references templates workflows; do test -d "gsd-core/$d" || { echo "missing gsd-core/$d"; exit 1; }; done && echo "restage ok"</automated>
  </verify>
  <acceptance_criteria>
    - `npm pack @opengsd/gsd-core@1.6.1` succeeded and the exact command + tarball name are logged in 07-REVENDOR-NOTES.md.
    - The five curated subdirs (bin, contexts, references, templates, workflows) exist and are populated from the 1.6.1 tarball.
    - At least one 1.6.1-only new file (workflows/list-seeds.md) is present.
    - No new top-level dir was added unless it is referenced by the shim/workflows (none expected per RESEARCH §5).
    - The command log in 07-REVENDOR-NOTES.md captures pack/extract/nuke/restage commands live.
  </acceptance_criteria>
  <done>The vendored gsd-core/ subset is replaced wholesale with pristine 1.6.1 content (new files present, no partial merge), with every command captured live in the re-vendor notes.</done>
</task>

<task type="auto">
  <name>Task 2: Re-inject the six local deltas, prove idempotency, and re-verify the SYNC-02 seams + SYNC-01 consistency</name>
  <files>gsd-core/VERSION, gsd-core/bin/lib/capability-registry.cjs, gsd-core/bin/lib/runtime-artifact-conversion.cjs, gsd-core/bin/lib/runtime-name-policy.cjs, gsd-core/bin/shared/runtime-aliases.manifest.json, .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md</files>
  <read_first>
    - scripts/apply-bob-patches.cjs (the tool built in Plan 01)
    - src/installer/stage.cjs (L242 VERSION read; L268–270 converter require destructure — the two seams re-verified here)
    - .planning/phases/07-gsd-core-1-6-1-sync/07-RESEARCH.md (§3 converter survival / D-03 interpretation, §6 shim resolution, §7 version-consistency, Pitfalls 1/2/4)
  </read_first>
  <action>
    Run `node scripts/apply-bob-patches.cjs` against the freshly-restaged pristine 1.6.1 tree. It reproduces the full six-delta set (D-02): colon→hyphen + ~/.claude→$HOME normalizations, the `"bob"` registry block, the ~105-line Bob converter block + exports, both alias entries, and the gsd-core/VERSION=1.6.1 write. Log the exact command + its per-step output into 07-REVENDOR-NOTES.md live (D-10).

    Reconfirm the ~/.claude→$HOME normalization scope against the live diff during this run (RESEARCH A2, target ≈129 lines): after the run, `grep -rn '~/\.claude' gsd-core/workflows gsd-core/references gsd-core/templates gsd-core/contexts` should return nothing (all .md doc-tree occurrences rewritten to $HOME/.claude). Do NOT assert anything about `~/.claude` in `bin/`: RESEARCH §1 scopes the normalization delta to the .md doc tree, and stock upstream `.cjs` under `bin/` legitimately retains ~/.claude in comments/string literals (~20 occurrences) that are NEVER normalized. If unexpected `~/` occurrences remain in the doc tree or the pattern over-matched, note it and refine the pattern in scripts/apply-bob-patches.cjs (this is the only place the exact pattern is finalized).

    Prove the D-02 idempotency contract AT RUNTIME by comparing run-1 output vs run-2 output DIRECTLY — do NOT diff against HEAD. HEAD still holds the 1.5.0 payload, so a working-tree-vs-HEAD diff (`git diff --quiet gsd-core/` before any staging) reflects the ENTIRE re-vendor delta (nuke + restage + six patches), not the second-run delta, and would false-fail even when the second run is a perfect no-op. Instead: after the first `node scripts/apply-bob-patches.cjs` run completes, stage the fully-patched tree as the comparison baseline with `git add gsd-core/`, then run `node scripts/apply-bob-patches.cjs` a SECOND time and assert `git diff --quiet gsd-core/` (now working tree vs the staged post-run-1 tree) reports zero changes — the second run is a pure no-op relative to the first. Log the result. (The `git add` is a staging step for the idempotency proof only; the executor still commits the final tree normally at plan end.)

    Re-verify the two SYNC-02 integration seams (this is the D-03 re-verification — NOT a stock-function existence check; per RESEARCH §3 the converters are local code that must have been re-injected):
    - Converter existence: `node -e "const c=require('./gsd-core/bin/lib/runtime-artifact-conversion.cjs'); if(typeof c.convertClaudeCommandToBobSkill!=='function'||typeof c.convertClaudeCommandToBobCommand!=='function') process.exit(1)"` must succeed. Record the D-03 result verbatim in 07-REVENDOR-NOTES.md ("converters re-injected; require resolves both — no upstream rename break, converters were never stock upstream").
    - Registry + shim resolution: `node -e "require('./gsd-core/bin/lib/capability-registry.cjs')"` loads clean and the runtimes object carries a bob entry; confirm runtime-aliases.manifest.json and runtime-name-policy.cjs both carry the bob alias. (The generic dot-home resolver that resolves the .bob home is exercised by the descriptor invariant suite in Plan 03.)

    Assert SYNC-01 payload version-consistency (D-07): `gsd-core/VERSION` reads exactly `1.6.1` AND `grep -rn '1\.5\.0' gsd-core/` returns nothing (RESEARCH §7 — post-restage the only place 1.5.0 could live is VERSION, which is now 1.6.1). Do NOT assert a raw `grep -rn '/gsd:' gsd-core/` count of 0 — that is UNSATISFIABLE: stock `bin/` retains ~38 upstream colon strings and the .md doc tree intentionally retains 3 non-command placeholders (`gsd:...`, `gsd:X` in workflows/help/modes/topic.md) that `transformContentToHyphen` correctly never rewrites (its regex only matches `gsd:<REAL-command-name>`). Instead prove colon→hyphen normalization completeness via the fixed-point already established by the idempotency check above: a second `apply-bob-patches.cjs` run leaves the doc tree byte-identical, so no real command-form `gsd:<cmd>` survived normalization. Log the version-consistency result. Note: sweeping 1.5.0 residue OUTSIDE the payload (README.md, stage.cjs comment, UPSTREAM.md, the shim-loads test fixture) is deferred to Plan 03.
  </action>
  <verify>
    <automated>git add gsd-core/ && node scripts/apply-bob-patches.cjs >/dev/null 2>&1; git diff --quiet gsd-core/ && echo "IDEMPOTENT: re-run vs staged post-run-1 tree made no changes (colon->hyphen normalization at a fixed point — no real gsd:<cmd> survived)" || { echo "NOT idempotent: re-run changed files"; exit 1; }; test "$(cat gsd-core/VERSION)" = "1.6.1" || { echo "VERSION not 1.6.1"; exit 1; }; if grep -rn '1\.5\.0' gsd-core/ ; then echo "residual 1.5.0 in payload"; exit 1; fi; if grep -rn '~/\.claude' gsd-core/workflows gsd-core/references gsd-core/templates gsd-core/contexts ; then echo "residual ~/.claude in .md doc tree"; exit 1; fi; node -e "const c=require('./gsd-core/bin/lib/runtime-artifact-conversion.cjs'); if(typeof c.convertClaudeCommandToBobSkill!=='function'||typeof c.convertClaudeCommandToBobCommand!=='function'){console.error('converters missing');process.exit(1)}"; node -e "require('./gsd-core/bin/lib/capability-registry.cjs')"; grep -q '"bob"' gsd-core/bin/shared/runtime-aliases.manifest.json && grep -q 'bob:' gsd-core/bin/lib/runtime-name-policy.cjs && echo "seams ok"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/apply-bob-patches.cjs` ran; after `git add gsd-core/` stages the post-run-1 tree, a SECOND run leaves `git diff gsd-core/` clean (idempotency proven by comparing run-1 output to run-2 output, NOT working-tree-vs-HEAD).
    - `gsd-core/VERSION` reads exactly `1.6.1`.
    - `grep -rn '1\.5\.0' gsd-core/` returns nothing (SYNC-01 payload consistency).
    - `grep -rn '~/\.claude' gsd-core/workflows gsd-core/references gsd-core/templates gsd-core/contexts` returns nothing (~/.claude→$HOME normalization reproduced across the .md doc tree; stock `bin/` retains upstream ~/.claude untouched — RESEARCH §1). The colon→hyphen normalization is at a fixed point: the second `apply-bob-patches.cjs` run leaves the doc tree byte-identical, so no real command-form `gsd:<cmd>` survives (the 3 intentional `gsd:...`/`gsd:X` placeholders and stock `bin/` colon strings are expected, NOT failures — do NOT assert a raw `/gsd:` count of 0).
    - `require` of runtime-artifact-conversion.cjs exposes both Bob converter functions; require of capability-registry.cjs loads clean with a bob runtime entry; both alias files carry bob.
    - 07-REVENDOR-NOTES.md records the apply command output, the idempotency result, the D-03 converter re-verification verdict, and the SYNC-01 version-consistency result.
  </acceptance_criteria>
  <done>The restaged 1.6.1 payload carries all six re-injected local deltas idempotently, VERSION is 1.6.1 with no 1.5.0 residue in the payload, and both SYNC-02 seams (converter require + registry/alias load) resolve — captured live in the re-vendor notes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local vendored tree | The 1.6.1 tarball crosses into gsd-core/; integrity depends on registry immutability |
| patch script → vendored files | apply-bob-patches.cjs rewrites gsd-core/ files in place; a mis-applied anchor could corrupt JS syntax |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering | nuke-and-restage fs ops | high | mitigate | Restage exclusively from the immutable extracted tarball; confirm payload root path before copy; verify a 1.6.1-only file landed and all five subdirs are populated |
| T-07-04 | Tampering | anchor-based patch insertion | high | mitigate | Each patch site is require-smoke-tested post-run (capability-registry.cjs + runtime-artifact-conversion.cjs load without throwing); idempotency double-run guards against double-insertion |
| T-07-SC | Tampering | npm pack @opengsd/gsd-core@1.6.1 | high | mitigate | Immutable registry-signed tarball (D-04); package verdict Approved in RESEARCH Package Legitimacy Audit (not ASSUMED/SUS) — no blocking human checkpoint required |
</threat_model>

<verification>
- The payload is wholesale 1.6.1 (a 1.6.1-only file exists; all five curated subdirs populated).
- `gsd-core/VERSION`=1.6.1 and `grep -rn '1\.5\.0' gsd-core/` is empty (SYNC-01, D-07).
- Both normalizations reproduced: `grep -rn '~/\.claude' gsd-core/workflows gsd-core/references gsd-core/templates gsd-core/contexts` is empty, and the colon→hyphen normalization is at a fixed point (a second apply-bob-patches.cjs run leaves the doc tree byte-identical — no real `gsd:<cmd>` survives). Stock `bin/` retains upstream colon/tilde strings and is never normalized (RESEARCH §1).
- A second apply-bob-patches.cjs run leaves gsd-core/ unchanged (D-02 idempotency).
- Both Bob converters are requireable and the registry + both alias files carry bob (SYNC-02 seams).
- The re-vendor command log, D-03 result, and version-consistency result are recorded live in 07-REVENDOR-NOTES.md.
</verification>

<success_criteria>
- SYNC-01 payload replaced wholesale at 1.6.1 on one consistent version with no residual mix.
- SYNC-02 integration seams (converters, registry, aliases) re-validated at the require level against the 1.6.1 bin layer; the shim-resolution invariant is confirmed by the descriptor suite in Plan 03.
- apply-bob-patches.cjs proven idempotent at runtime — the executable core of the Phase 10 runbook.
</success_criteria>

<output>
Create `.planning/phases/07-gsd-core-1-6-1-sync/07-02-SUMMARY.md` when done.
</output>