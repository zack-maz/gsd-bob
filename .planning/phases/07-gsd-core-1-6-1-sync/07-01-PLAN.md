---
phase: 07-gsd-core-1-6-1-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/apply-bob-patches.cjs
  - .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md
autonomous: true
requirements: [SYNC-01, SYNC-02]
must_haves:
  truths:
    - "07-REVENDOR-NOTES.md records the pre-vendor `npm test` baseline (186 pass / 3 fail) with the 3 failures named as pre-existing environmental noise (acceptance-coverage.test.cjs:114/:128, core-loop-contract.test.cjs:126)"
    - "scripts/apply-bob-patches.cjs loads clean (node --check passes) and reuses transformContentToHyphen + readCmdNames imported from scripts/fix-slash-commands.cjs — no hand-rolled colon→hyphen regex, never runs fix-slash-commands.cjs as main"
    - "The inlined `\"bob\"` registry block and the ~105-line Bob converter block embedded in the script are byte-identical to the current vendored blocks captured from the tree BEFORE the Plan-02 nuke"
    - "The script defines all six local-delta steps (colon→hyphen, ~/.claude→$HOME, registry, converter, 2 aliases, VERSION write) each guarded to be an idempotent no-op on re-run"
  artifacts:
    - scripts/apply-bob-patches.cjs
    - .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md
  key_links:
    - "apply-bob-patches.cjs imports { transformContentToHyphen, readCmdNames } from ./fix-slash-commands.cjs (the exported pure transforms, NOT the require.main forward direction)"
    - "The canonical Bob-block constants are captured from gsd-core/bin/lib/capability-registry.cjs (bob block L3041–3109) and gsd-core/bin/lib/runtime-artifact-conversion.cjs (Bob block L674–~790) before those files are destroyed in Plan 02"
---

<objective>
Prepare the two prerequisites the destructive re-vendor depends on, WITHOUT touching the vendored payload yet:

1. **Capture the pre-vendor baseline** into a new live re-vendor log so post-vendor deltas are attributable (D-10, D-11).
2. **Author `scripts/apply-bob-patches.cjs`** — the idempotent, node-builtins-only script that reproduces the FULL local-delta set over a freshly-restaged pristine 1.6.1 payload (D-02). Per RESEARCH this is SIX deltas, not the two D-01 originally claimed (D-01's premise is corrected here): colon→hyphen normalization, ~/.claude→$HOME normalization, the `"bob"` registry block, the ~105-line Bob converter code block, two alias entries, and the local `gsd-core/VERSION` write.

Purpose: The script is the executable core of the Phase 10 MAINTAINING runbook (a bump = nuke → restage clean 1.6.1 → run this script → validate). Building it BEFORE the nuke is mandatory: it must inline the canonical `"bob"` registry block and Bob converter block captured verbatim from the current vendored tree, which Plan 02 destroys.

Output: `scripts/apply-bob-patches.cjs` (new) and `.planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md` (new, live log).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gsd-core-1-6-1-sync/07-CONTEXT.md
@.planning/phases/07-gsd-core-1-6-1-sync/07-RESEARCH.md
@.planning/phases/07-gsd-core-1-6-1-sync/07-PATTERNS.md
@scripts/fix-slash-commands.cjs
@scripts/generate-support-roster.cjs
@src/installer/stage.cjs
</context>

<artifacts_produced>
Files/symbols this plan creates (exclude from any drift/source-grounding check as newly-created):
- `scripts/apply-bob-patches.cjs` — new idempotent re-injection script
- `.planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md` — new live re-vendor log (seeds Phase 10 DOCS-04)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Capture the pre-vendor baseline into 07-REVENDOR-NOTES.md</name>
  <files>.planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md</files>
  <read_first>
    - .planning/phases/07-gsd-core-1-6-1-sync/07-RESEARCH.md (§"Golden-Fixture Drift Map" baseline note; §"Common Pitfalls" Pitfall 5)
    - .planning/phases/07-gsd-core-1-6-1-sync/07-CONTEXT.md (D-10, D-11 — notes obligation and required contents)
  </read_first>
  <action>
    Create the live re-vendor log at .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md per D-11. This file is written live as-the-work-happens (D-10), NOT reconstructed later; Plans 02 and 03 append to it. Seed it now with:
    (a) a dated header and a chronological "Command Log" section (empty, to be appended live in Plans 02/03);
    (b) the pre-vendor test baseline: run `npm test`, record the exact totals, and assert the baseline is 186 pass / 3 fail. Name the 3 failures verbatim as PRE-EXISTING ENVIRONMENTAL NOISE unrelated to the re-vendor — acceptance-coverage.test.cjs (two assertions, ~:114 and ~:128, read archived .planning/ fixtures absent from the working tree) and core-loop-contract.test.cjs (~:126, reads a missing archived 04-01-PLAN.md). Per Pitfall 5, do NOT attempt to fix these three in this phase;
    (c) record the current git HEAD short SHA and the current `gsd-core/VERSION` value (1.5.0) as the pre-vendor provenance anchor;
    (d) placeholder sub-sections to be filled later: "D-03 converter re-verification result", "D-08 fixture-drift justifications (keyed by fixture name)", and "SYNC-03 pointer re-verification".
    If the observed baseline differs from 186/3, record the ACTUAL observed numbers and the actual failing test IDs verbatim (the ACTUAL run is the source of truth) and flag the discrepancy for the drift analysis in Plan 03 — do not silently assert 186/3.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md && grep -q "baseline" .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md && grep -Eq "186|pass" .planning/phases/07-gsd-core-1-6-1-sync/07-REVENDOR-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - 07-REVENDOR-NOTES.md exists with a dated header and a Command Log section.
    - The actual `npm test` totals are recorded; if 186/3, the 3 failing tests are named as pre-existing environmental noise; if different, the actual numbers + failing IDs are recorded and flagged.
    - Current git HEAD short SHA and `gsd-core/VERSION`=1.5.0 are recorded as pre-vendor provenance.
    - Placeholder sub-sections for D-03 result, D-08 drift justifications, and SYNC-03 pointer re-verification are present.
  </acceptance_criteria>
  <done>The pre-vendor baseline (test totals + the 3 known env failures + git SHA + VERSION 1.5.0) is captured in 07-REVENDOR-NOTES.md so every post-vendor delta is attributable.</done>
</task>

<task type="auto">
  <name>Task 2: Author scripts/apply-bob-patches.cjs (full six-delta idempotent re-injection)</name>
  <files>scripts/apply-bob-patches.cjs</files>
  <read_first>
    - scripts/fix-slash-commands.cjs (reuse target: `transformContentToHyphen`, `readCmdNames`, `processDir`/`processFile` walk pattern, SKIP_DIRS, fail-loud ENOENT discipline)
    - scripts/generate-support-roster.cjs (script header / CLI / derive-from-source style)
    - gsd-core/bin/lib/capability-registry.cjs (capture the `"bob"` block verbatim: banner L3041–3044 + `"bob": {` L3045 through its closing brace ~L3109; note it currently sits inside `const runtimes = {` which opens at L2591)
    - gsd-core/bin/lib/runtime-artifact-conversion.cjs (capture the Bob converter block verbatim: banner L674 + `convertClaudeToBobContent` L698 + `convertClaudeCommandToBobSkill` L735 + `convertClaudeCommandToBobCommand` L763, through ~L790 before `convertClaudeCommandToCursorCommand` L896; and the 3 export lines L2014–2016)
    - gsd-core/bin/lib/runtime-name-policy.cjs (FALLBACK_ALIASES map L22, `cline` L38, `bob` L39)
    - gsd-core/bin/shared/runtime-aliases.manifest.json (`cline` L75–78, `bob` L79–82)
    - src/installer/stage.cjs (L242 VERSION read; L268 converter require destructure — the two hard dependencies this script must satisfy)
    - .planning/phases/07-gsd-core-1-6-1-sync/07-RESEARCH.md (§4 anchors table, §"Recommended apply-bob-patches.cjs Design", Pitfalls 1/2/4)
    - .planning/phases/07-gsd-core-1-6-1-sync/07-PATTERNS.md (all `apply-bob-patches.cjs` pattern assignments)
  </read_first>
  <action>
    Create scripts/apply-bob-patches.cjs implementing D-02: a node-builtins-only (`node:fs`, `node:path` only — CLAUDE.md: no third-party deps, no network, no exec; writes only under gsd-core/) idempotent script that restores ALL local deltas over a freshly-restaged pristine gsd-core/ payload. This corrects D-01's "two data-only edits" premise: per RESEARCH §1 the true delta set is SIX items, and dropping any of them leaves the payload broken under Bob (Pitfall 1). Implement each as a guarded no-op-on-re-run step:

    1. Colon→hyphen normalization — reuse the exported pure transform: `const { transformContentToHyphen, readCmdNames } = require('./fix-slash-commands.cjs')`. Walk ONLY the `.md` documentation subdirs — `workflows`, `references`, `templates`, `contexts` — and process only `*.md` files, with a recursive readdirSync walk mirroring fix-slash-commands.cjs's processDir/processFile pattern (skip SKIP_DIRS, read → transform(src, readCmdNames()) → write-if-changed, log per-file count). Do NOT include `bin/lib` (or any of `bin/`) in this normalization walk: RESEARCH §1 (rows 1a/1b) verified both normalization deltas — colon→hyphen (~995 lines) and ~/.claude→$HOME (~129 lines) — live exclusively in the `.md` doc tree, and running a text transform over `bin/` `.cjs` files would corrupt legitimate code content (e.g. runtime-slash.cjs's legacy-colon input parser and command-string literals that intentionally retain those tokens). `bin/lib` stays a patch-only target for steps 3/4/5b below — never a normalization target. Do NOT hand-roll a regex and do NOT invoke fix-slash-commands.cjs as a script — its require.main runs the WRONG (hyphen→colon) direction (Pitfall 4). Naturally idempotent (no colon command form left after the first pass).
    2. ~/.claude→$HOME normalization — a second pure replace over the SAME `.md` doc-subdir walk from step 1 (`workflows`, `references`, `templates`, `contexts` only, `*.md` files; NOT `bin/`): `content.replace(/(^|[^\w$])~\/\.claude/g, '$1$HOME/.claude')` (idempotent; also covers the `@~/.claude/...` mandatory-read forms). Per RESEARCH A2 the exact pattern/scope should be reconfirmed against the live diff during Plan 02 execution (target delta ≈129 lines).
    3. Registry patch — anchor-based string insertion of the captured `"bob"` block into `const runtimes = {` in gsd-core/bin/lib/capability-registry.cjs. Anchor on a stable sibling runtime key line (e.g. the `"claude": {` entry inside `const runtimes`); object key order is cosmetic. Guard: skip if a `"bob"` key already exists inside the runtimes object. Do NOT insert into the earlier `const capabilities` object (RESEARCH §4).
    4. Converter patch — anchor-based string insertion of the captured ~105-line Bob converter block (banner + convertClaudeToBobContent + convertClaudeCommandToBobSkill + convertClaudeCommandToBobCommand) into gsd-core/bin/lib/runtime-artifact-conversion.cjs before `module.exports = {`, plus the three export lines (`convertClaudeToBobContent,` `convertClaudeCommandToBobSkill,` `convertClaudeCommandToBobCommand,`) inserted inside the exports object anchored on the existing `convertClaudeCommandToCursorCommand,` export line. Function declarations are hoisted so block placement is flexible. Guard: skip if `convertClaudeCommandToBobSkill` is already declared. This corrects D-03: the converters are LOCAL hand-edited code (in-file banner `gsd-bob HAND-EDIT`), absent from the pristine tarball — they must be re-injected wholesale, not treated as stock functions. All block dependencies (`yamlQuote`, `command-roster.cjs`) survive in 1.6.1 (RESEARCH §3).
    5a. Alias manifest patch — structured JSON edit of gsd-core/bin/shared/runtime-aliases.manifest.json: JSON.parse → add `bob: ['bob','bob-cli']` if absent → write JSON.stringify(obj, null, 2) + trailing '\n' (preserve 2-space indent + trailing newline). Drift-proof; no line fragility.
    5b. Name-policy alias patch (the UNDOCUMENTED 4th data patch, RESEARCH §1/§8) — anchor-based string insertion of `bob: ['bob', 'bob-cli'],` after the `cline: ['cline', 'cline-cli'],` line inside FALLBACK_ALIASES in gsd-core/bin/lib/runtime-name-policy.cjs. Guard: skip if a `bob:` key is present.
    6. VERSION write — write `gsd-core/VERSION` = the target version string `1.6.1` (per D-05/D-07; the tarball ships NO VERSION file per RESEARCH §2, and stage.cjs L242 throws ENOENT without it). Match the current file's shape (no trailing newline). This step is idempotent by content-equality (writing the same string twice yields the same file).

    Keep the canonical registry-block and converter-block text as inline JS template-string constants (self-contained; best for the Phase-10 runbook narrative — RESEARCH Open Question 1). Fail-loud on every filesystem read: swallow ONLY ENOENT, re-throw everything else (mirror fix-slash-commands.cjs readCmdNames). Guard each of the six steps to be a no-op when already applied so a re-run is clean (the D-02 idempotency contract). Resolve gsd-core/ from the repo root (`path.join(__dirname, '..')`), matching stage.cjs's repoRoot anchoring. Log a concise per-step summary to stdout.
    Do NOT run the script against the live tree in this task (that would prematurely bump VERSION and mutate the payload) — the runtime idempotency proof and first real run happen in Plan 02 against the restaged pristine 1.6.1 tree.
  </action>
  <verify>
    <automated>node --check scripts/apply-bob-patches.cjs && node -e "const s=require('fs').readFileSync('scripts/apply-bob-patches.cjs','utf8'); const bad=/require\(['\"]\.\/fix-slash-commands\.cjs['\"]\)/.test(s)?0:1; if(bad){console.error('missing reuse import'); process.exit(1)} if(!/transformContentToHyphen/.test(s)){console.error('missing transformContentToHyphen'); process.exit(1)} if(!/convertClaudeCommandToBobSkill/.test(s)||!/convertClaudeCommandToBobCommand/.test(s)){console.error('missing converter block'); process.exit(1)} if(!/1\.6\.1/.test(s)){console.error('missing VERSION target'); process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/apply-bob-patches.cjs` passes (valid CJS).
    - The script requires only `node:fs` and `node:path` (no third-party deps, no child_process/network) and imports `transformContentToHyphen` + `readCmdNames` from ./fix-slash-commands.cjs.
    - All six delta steps are present, each with an idempotency guard (skip-if-already-applied for the four patches; content-equality for VERSION and the two normalizations).
    - The inlined `"bob"` registry block and Bob converter block are byte-identical to the current vendored blocks (verify by capturing the current blocks and comparing).
    - The VERSION target string is `1.6.1`.
    - The script does NOT execute fix-slash-commands.cjs as a subprocess/main.
  </acceptance_criteria>
  <done>scripts/apply-bob-patches.cjs exists, loads clean, reuses the exported normalization transforms, embeds the verbatim registry + converter blocks, and implements all six idempotent delta steps ready to run against the restaged 1.6.1 payload in Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local vendored tree | The 1.6.1 payload (fetched in Plan 02) crosses into the repo; integrity depends on registry immutability |
| patch script → vendored files | apply-bob-patches.cjs rewrites gsd-core/ files in place |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | apply-bob-patches.cjs write scope | medium | mitigate | Script is node-builtins-only, no network, no exec; writes only under gsd-core/; every read fails loud (swallow only ENOENT) |
| T-07-02 | Tampering | inlined canonical Bob blocks | medium | mitigate | Blocks captured byte-identical from the current vendored tree before the nuke; verified equal before Plan 02 uses the script |
| T-07-SC | Tampering | npm pack @opengsd/gsd-core@1.6.1 | high | mitigate | Immutable registry-signed tarball (D-04); package is the project's own vendored source, verdict Approved in RESEARCH Package Legitimacy Audit (not ASSUMED/SUS) — no blocking human checkpoint required |
</threat_model>

<verification>
- `node --check scripts/apply-bob-patches.cjs` passes.
- The script imports the exported transforms from fix-slash-commands.cjs and defines all six delta steps with idempotency guards.
- 07-REVENDOR-NOTES.md exists with the recorded pre-vendor baseline and the placeholder sub-sections.
- No vendored payload file has been modified in this plan (the nuke/restage is Plan 02).
</verification>

<success_criteria>
- The re-vendor tool exists and is provably a full six-delta reproducer (not the incomplete two-edit model), ready to run idempotently against a restaged pristine 1.6.1 payload.
- The pre-vendor baseline is captured so Plan 03's drift classification can attribute every failure.
</success_criteria>

<output>
Create `.planning/phases/07-gsd-core-1-6-1-sync/07-01-SUMMARY.md` when done.
</output>
