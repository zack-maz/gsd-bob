---
phase: 03-installer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/installer/args.cjs
  - src/installer/scope.cjs
  - src/installer/report.cjs
  - test/installer/scope.test.cjs
  - test/installer/args.test.cjs
autonomous: true
requirements: [INSTALL-01, INSTALL-02]
must_haves:
  truths:
    - "Arg parser accepts exactly the gsd-core-mirrored flags and rejects unknown ones"
    - "No --clean / --update flag is recognized (re-run = update; --uninstall + install = clean)"
    - "Global scope resolves through getGlobalConfigDir('bob', explicitDir); local scope is always <cwd>/.bob"
    - "BOB_CONFIG_DIR and -c <path> both override the resolved global target (tilde-expanded)"
    - "The resolved absolute target path is the value the entry prints before any write (INSTALL-01)"
  artifacts:
    - path: "src/installer/args.cjs"
      provides: "parseArgs(argv) → {scope, explicitDir, dryRun, uninstall, help}; hand-parsed process.argv"
      min_lines: 30
      exports: ["parseArgs"]
    - path: "src/installer/scope.cjs"
      provides: "resolveTarget({scope, explicitDir}) via runtime-homes.cjs; local = cwd/.bob"
      min_lines: 20
      exports: ["resolveTarget"]
    - path: "src/installer/report.cjs"
      provides: "written/skipped/removed bucket formatting via console.log"
      exports: ["printReport", "newReport"]
    - path: "test/installer/scope.test.cjs"
      provides: "global/local/-c/BOB_CONFIG_DIR resolution assertions (hermetic env injection)"
    - path: "test/installer/args.test.cjs"
      provides: "flag-parse + unknown-flag + no-clean-flag assertions"
  key_links:
    - from: "src/installer/scope.cjs"
      to: "gsd-core/bin/lib/runtime-homes.cjs"
      via: "calls getGlobalConfigDir('bob', explicitDir) — never bespoke path math"
      pattern: "getGlobalConfigDir\\('bob'"
  prohibitions:
    - "args.cjs MUST NOT use commander/yargs/oclif or any CLI framework — hand-parse process.argv only"
    - "args.cjs MUST NOT recognize a --clean or --update flag"
    - "scope.cjs MUST NOT reimplement .bob path resolution — it calls getGlobalConfigDir"
    - "No module here may require js-yaml or any third-party package (node: builtins + vendored gsd-core resolver only)"
---

<objective>
Build the dependency-free CLI plumbing the entry point composes: `args.cjs` (hand-parsed `process.argv` mirroring gsd-core's flag convention exactly), `scope.cjs` (local/global target resolution that CALLS the vendored `getGlobalConfigDir('bob', explicitDir)` rather than reimplementing path math), and `report.cjs` (the written/skipped/removed end-of-run buckets). These are pure transforms with no dependency on the manifest or staging engine, so they parallelize with Plan 01 in Wave 1.

Purpose: Lock the flag contract (INSTALL-01/02) and the scope→absolute-path resolution behind small, unit-tested modules before the entry wiring (Plan 04) and staging engine (Plan 03) consume them.
Output: `src/installer/args.cjs`, `src/installer/scope.cjs`, `src/installer/report.cjs`, and two house-style test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-installer/03-CONTEXT.md
@.planning/phases/03-installer/03-RESEARCH.md
@.planning/phases/03-installer/03-PATTERNS.md
@gsd-core/bin/lib/runtime-homes.cjs
@test/descriptor.test.cjs
</context>

## Artifacts this phase produces (Plan 02 contribution)

- New module `src/installer/args.cjs` exporting `parseArgs(argv)`.
- New module `src/installer/scope.cjs` exporting `resolveTarget({scope, explicitDir})`.
- New module `src/installer/report.cjs` exporting `newReport()` and `printReport(report)`.
- CLI flags locked: `--bob`, `--local`/`-l`, `--global`/`-g`, `--config-dir`/`-c <path>`, `--uninstall`/`-u`, `--dry-run`, `--help`. NO `--clean`/`--update`.
- New test files: `test/installer/args.test.cjs`, `test/installer/scope.test.cjs`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hand-parse process.argv into a resolved options object (args.cjs)</name>
  <files>src/installer/args.cjs, test/installer/args.test.cjs</files>
  <read_first>
    - .planning/phases/03-installer/03-PATTERNS.md (`src/installer/args.cjs` section — external convention, gsd-core selectRuntimesFromArgs() hand-parse rule; flag list; NO --clean/--update)
    - .planning/phases/03-installer/03-RESEARCH.md (Architectural Responsibility Map row "Arg parsing"; Project Constraints — mirror gsd-core flags, no framework)
    - .claude/CLAUDE.md (§"What NOT to Use" — no commander/oclif/yargs; re-run = update, --uninstall+install = clean)
  </read_first>
  <behavior>
    - parseArgs(['--bob','--local']) → { scope:'local', explicitDir:undefined, dryRun:false, uninstall:false, help:false }.
    - parseArgs(['--bob','-g']) → scope:'global'. parseArgs(['--bob','--global']) → scope:'global'.
    - parseArgs(['--bob']) (no scope flag) → scope:null (signals the entry to run the interactive prompt).
    - parseArgs(['--bob','-c','/tmp/x']) → explicitDir:'/tmp/x' (and parseArgs(['--config-dir','/tmp/x']) equivalent).
    - parseArgs(['--bob','-u']) → uninstall:true. parseArgs(['--bob','--dry-run']) → dryRun:true. parseArgs(['--help']) → help:true.
    - parseArgs(['--bob','--clean']) and parseArgs(['--bob','--update']) → throw or set help with an "unknown flag" message (these flags do NOT exist).
    - An unknown flag like '--frobnicate' is rejected with a concrete error naming the flag.
  </behavior>
  <action>
    Create `src/installer/args.cjs` (`'use strict'`, no requires beyond node builtins). Export `parseArgs(argv)` that iterates the array hand-rolled (no framework), recognizing exactly: `--bob` (accepted/ignored — runtime selector for UX parity), `--local`/`-l` → scope='local', `--global`/`-g` → scope='global', `--config-dir`/`-c` consuming the NEXT token as explicitDir, `--uninstall`/`-u` → uninstall=true, `--dry-run` → dryRun=true, `--help`/`-h` → help=true. Default scope is `null` when no scope flag present (the entry uses null to trigger the readline prompt). Reject any unrecognized token with a thrown Error whose message names the offending flag — and explicitly treat `--clean`/`--update` as unknown (a targeted negative gate proving the no-extra-flags contract). Return `{ scope, explicitDir, dryRun, uninstall, help }`. Write `test/installer/args.test.cjs` covering every <behavior> case including the assert.throws on `--clean`, `--update`, and a junk flag.
  </action>
  <verify>
    <automated>node --test test/installer/args.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/installer/args.test.cjs` passes covering every <behavior> case.
    - `grep -ciE "commander|yargs|oclif" src/installer/args.cjs` returns 0.
    - `node -e "const {parseArgs}=require('./src/installer/args.cjs'); try{parseArgs(['--bob','--clean']); process.exit(1)}catch(e){process.exit(0)}"` exits 0 (--clean rejected).
    - `npm test` stays green.
  </acceptance_criteria>
  <done>parseArgs recognizes exactly the gsd-core-mirrored flag set, defaults scope to null for the prompt, and rejects --clean/--update/unknown flags with a concrete error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve scope to an absolute target path via the vendored descriptor (scope.cjs)</name>
  <files>src/installer/scope.cjs, test/installer/scope.test.cjs</files>
  <read_first>
    - gsd-core/bin/lib/runtime-homes.cjs (getGlobalConfigDir signature lines 215-232: explicitDir → expandTilde; else descriptor-driven; bob → ~/.bob via dot-home)
    - test/descriptor.test.cjs (hermetic env injection pattern; ~/.bob default + BOB_CONFIG_DIR override + tilde expansion assertions — the scope test mirrors this)
    - .planning/phases/03-installer/03-PATTERNS.md (`src/installer/scope.cjs` section — INTEGRATION ANALOG, call never reimplement; local = path.join(process.cwd(), '.bob'))
    - .planning/phases/01-bob-capability-mapping/CAPABILITY-MAP.md (§2 — global ~/.bob, project .bob/ config home confirmed)
  </read_first>
  <behavior>
    - resolveTarget({scope:'global', explicitDir:undefined}) returns getGlobalConfigDir('bob', undefined) (i.e. ~/.bob in a default env).
    - resolveTarget({scope:'global', explicitDir:'/tmp/xbob'}) returns '/tmp/xbob' (the -c override wins, tilde-expanded by the resolver).
    - resolveTarget({scope:'local'}) returns path.join(process.cwd(), '.bob') — always cwd-relative, NOT via the descriptor.
    - The function returns an absolute path string (the value the entry prints before writing, INSTALL-01).
    - BOB_CONFIG_DIR honoring is delegated to getGlobalConfigDir (the test asserts the resolver wiring, not a reimplementation).
  </behavior>
  <action>
    Create `src/installer/scope.cjs` (`'use strict'`; require `node:path` and the vendored `../../gsd-core/bin/lib/runtime-homes.cjs`). Export `resolveTarget({ scope, explicitDir })`: for `scope === 'local'` return `path.join(process.cwd(), '.bob')`; for `scope === 'global'` return `getGlobalConfigDir('bob', explicitDir)` (which handles `-c`/`BOB_CONFIG_DIR`/tilde — do NOT reimplement). Do not handle `scope === null` here (that is resolved to local/global by the entry's prompt before calling). The returned path is what the entry prints before any write. Write `test/installer/scope.test.cjs` mirroring test/descriptor.test.cjs: require the resolver via `requireVendor('runtime-homes.cjs')` to assert the underlying behavior, and require `src/installer/scope.cjs` for the integration; assert local = cwd/.bob, global default and the -c override path. Use a hermetic approach for BOB_CONFIG_DIR (the resolver reads process.env in its public form, so assert the -c explicit-dir path which is deterministic and env-independent).
  </action>
  <verify>
    <automated>node --test test/installer/scope.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/installer/scope.test.cjs` passes covering local, global-default, and -c override cases.
    - `grep -c "getGlobalConfigDir('bob'" src/installer/scope.cjs` >= 1 (calls the resolver, not bespoke math).
    - `grep -cE "homedir\\(\\)|os\\.homedir" src/installer/scope.cjs` returns 0 (no hand-rolled home resolution).
    - `node -e "const {resolveTarget}=require('./src/installer/scope.cjs'); const p=resolveTarget({scope:'global',explicitDir:'/tmp/xbob'}); if(p!=='/tmp/xbob')process.exit(1)"` exits 0.
    - `npm test` stays green.
  </acceptance_criteria>
  <done>resolveTarget delegates global resolution to getGlobalConfigDir('bob', …), returns cwd/.bob for local, returns an absolute path, and reimplements no path logic.</done>
</task>

<task type="auto">
  <name>Task 3: End-of-run report buckets (report.cjs)</name>
  <files>src/installer/report.cjs</files>
  <read_first>
    - .planning/phases/03-installer/03-CONTEXT.md (§Specific Ideas — buckets: written, skipped (user-modified), removed (orphaned))
    - scripts/generate-support-roster.cjs (lines 80-82 — process.stdout.write summary line; the closest in-repo reporting analog)
    - .planning/phases/03-installer/03-PATTERNS.md (`src/installer/report.cjs` — no-analog; plain console.log buckets)
  </read_first>
  <action>
    Create `src/installer/report.cjs` (`'use strict'`, node builtins only). Export `newReport()` returning a mutable accumulator `{ written: [], skipped: [], removed: [] }` and `printReport(report, { dryRun })` that prints the three buckets via `console.log` with clear headers ("Written", "Skipped (user-modified, preserved)", "Removed (orphaned)") and a one-line summary tally; when `dryRun` is true the header reads "PLAN (dry-run — nothing written)". No file IO, no third-party deps. This module is consumed by the staging engine (Plan 03) and the entry (Plan 04). Keep it deterministic and side-effect-free except console output. (No dedicated test file — covered indirectly by the Plan 04 dry-run integration test asserting the planned buckets; this is glue/output formatting, so no tdd block.)
  </action>
  <verify>
    <automated>node -e "const r=require('./src/installer/report.cjs'); const rep=r.newReport(); rep.written.push('a'); r.printReport(rep,{dryRun:true})" | grep -qi "dry-run"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const r=require('./src/installer/report.cjs'); console.log(typeof r.newReport, typeof r.printReport)"` prints `function function`.
    - The verify command exits 0 (dry-run header rendered).
    - `grep -ciE "commander|chalk|ora" src/installer/report.cjs` returns 0.
  </acceptance_criteria>
  <done>report.cjs exposes newReport()/printReport(), prints written/skipped/removed buckets, marks dry-run runs, and adds no dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → installer | process.argv (and -c value / BOB_CONFIG_DIR) are user-supplied and untrusted |
| env → scope resolution | BOB_CONFIG_DIR can point the global target anywhere on disk |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | scope.cjs target via -c / BOB_CONFIG_DIR | mitigate | resolveTarget returns an absolute path; the entry (Plan 04) PRINTS it before any write (INSTALL-01) so the user sees exactly where it lands; path math is delegated to the vetted gsd-core resolver, not hand-rolled. |
| T-03-06 | Elevation of Privilege | args.cjs unknown flags | mitigate | Unknown/extra flags (incl. --clean/--update) throw a concrete error rather than being silently absorbed — no hidden behavior surface. |
| T-03-07 | Tampering | path traversal via -c value (e.g. '../../etc') | accept | The tool writes only under the user-resolved scope target the user explicitly chose and saw printed; it operates on the invoking user's own filesystem (ASVS V4 N/A). Symlink hardening on writes is noted for Plan 03/04. |
</threat_model>

<verification>
- `node --test test/installer/args.test.cjs` green.
- `node --test test/installer/scope.test.cjs` green.
- `grep -ciE "commander|yargs|oclif" src/installer/args.cjs` == 0.
- `npm test` (full suite) stays green.
</verification>

<success_criteria>
- Flag contract locked and tested (INSTALL-01/02), including the no-`--clean`/`--update` negative gate.
- Scope→absolute-path resolution delegates to the vendored descriptor resolver and returns the path the entry will print before writing.
- report.cjs ready for Plan 03/04 to populate and print.
</success_criteria>

<output>
Create `.planning/phases/03-installer/03-02-SUMMARY.md` when done.
</output>
