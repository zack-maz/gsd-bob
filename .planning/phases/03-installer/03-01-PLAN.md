---
phase: 03-installer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bob-adapter.cjs
  - src/installer/manifest.cjs
  - test/installer/unmerge.test.cjs
  - test/installer/manifest.test.cjs
autonomous: true
requirements: [INSTALL-05]
must_haves:
  truths:
    - "A manifest module computes a sha256 of the exact bytes written to disk"
    - "The manifest is the sole source of truth: a path absent from entries[] is never classified for removal"
    - "Update classification distinguishes overwrite (hash matches), skip-warn (hash differs), rewrite (missing)"
    - "unmergeCustomModes removes gsd/gsd-* slugs from custom_modes.yaml and preserves every user slug"
    - "unmergeCustomModes fails loud on a non-mapping YAML root, mirroring mergeCustomModes"
  artifacts:
    - path: "src/installer/manifest.cjs"
      provides: "sha256 hashing, manifest read/write/serialize, file-entry update classification, orphan classification"
      min_lines: 60
      exports: ["sha256", "readManifest", "writeManifest", "buildManifest", "classifyOnUpdate", "classifyOrphan"]
    - path: "src/bob-adapter.cjs"
      provides: "unmergeCustomModes sibling of mergeCustomModes"
      contains: "function unmergeCustomModes"
    - path: "test/installer/manifest.test.cjs"
      provides: "hash determinism, schema round-trip, update/orphan classification assertions"
    - path: "test/installer/unmerge.test.cjs"
      provides: "slug removal, user-slug preservation, idempotency, fail-loud assertions"
  key_links:
    - from: "src/bob-adapter.cjs"
      to: "js-yaml"
      via: "unmergeCustomModes uses the same yaml.load/yaml.dump path as mergeCustomModes (YAML confined to adapter)"
      pattern: "yaml\\.(load|dump)"
    - from: "src/installer/manifest.cjs"
      to: "node:crypto"
      via: "createHash('sha256') over the bytes-as-written"
      pattern: "createHash\\('sha256'\\)"
  prohibitions:
    - "manifest.cjs MUST NOT require js-yaml (YAML stays confined to bob-adapter.cjs)"
    - "manifest.cjs MUST NOT scan the filesystem to discover removable files — only entries[] drives classification"
    - "unmergeCustomModes MUST NOT delete custom_modes.yaml or drop any non-gsd user slug"
    - "Neither file may add a third-party dependency — node: builtins + the existing js-yaml (adapter only)"
---

<objective>
Build the two no-/sibling-analog linchpins of the installer before anything that consumes them: the manifest module (sha256 over bytes-as-written, schema read/write, update + orphan classification — the single source of truth for INSTALL-05) and the adapter's `unmergeCustomModes` (the slug-removing sibling of the existing `mergeCustomModes`, required by uninstall so YAML handling stays confined to the adapter).

Purpose: These are the genuinely net-new pieces with no in-repo analog (manifest) or a single exact sibling analog (unmerge). Building and unit-testing them in isolation in Wave 1 lets the staging engine (Plan 03) and the entry wiring (Plan 04) consume verified primitives rather than reimplement hashing or YAML logic.
Output: `src/installer/manifest.cjs`, an added `unmergeCustomModes` export in `src/bob-adapter.cjs`, and two house-style test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-installer/03-CONTEXT.md
@.planning/phases/03-installer/03-RESEARCH.md
@.planning/phases/03-installer/03-PATTERNS.md
@src/bob-adapter.cjs
@test/merge.test.cjs
</context>

## Artifacts this phase produces (Plan 01 contribution)

- New export `unmergeCustomModes(existingYamlText, ownedSlugs)` in `src/bob-adapter.cjs` (added to `module.exports`).
- New module `src/installer/manifest.cjs` exporting `sha256`, `readManifest`, `writeManifest`, `buildManifest`, `classifyOnUpdate`, `classifyOrphan`.
- Manifest dotfile literal name: `.gsd-bob-manifest.json` (define as an exported constant `MANIFEST_FILENAME`).
- Manifest schema keys: `schemaVersion`, `gsdBobVersion`, `scope`, `configHome`, `generatedAt`, `entries[]`; each entry `{ path, sha256, kind: 'file' | 'merged' }`.
- New test files: `test/installer/manifest.test.cjs`, `test/installer/unmerge.test.cjs`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unmergeCustomModes to the adapter (sibling of mergeCustomModes)</name>
  <files>src/bob-adapter.cjs, test/installer/unmerge.test.cjs</files>
  <read_first>
    - src/bob-adapter.cjs (lines 57-111 mergeCustomModes + isOwnedSlug; lines 197-204 module.exports — mirror exactly)
    - test/merge.test.cjs (house style for the sibling test: yaml.load helper, assert.throws(/regex/) fail-loud pattern, slug-preservation assertions)
    - .planning/phases/03-installer/03-RESEARCH.md (Open Question Q2 — un-merge belongs in the adapter; D-06 un-merge semantics)
  </read_first>
  <behavior>
    - unmergeCustomModes('', ['gsd']) returns YAML text with an empty customModes list (or omits the key cleanly) — nothing to remove, never throws on empty/undefined/null.
    - A custom_modes.yaml containing only a user mode 'my-mode' is returned with 'my-mode' intact and no gsd entry added.
    - A custom_modes.yaml containing user mode 'my-mode' + 'gsd' + 'gsd-legacy' → after unmerge with ownedSlugs covering gsd/gsd-*, only 'my-mode' remains; both gsd and gsd-legacy removed.
    - Idempotent: unmerge twice equals unmerge once (parsed equality).
    - Fail-loud: a sequence-root or scalar-root YAML throws a concrete error matching /mapping|non-mapping|not a mapping/i (same discipline as mergeCustomModes).
  </behavior>
  <action>
    Add `unmergeCustomModes(existingYamlText, ownedSlugs)` to `src/bob-adapter.cjs` directly beneath `mergeCustomModes`. Mirror `mergeCustomModes` structure exactly: js-yaml SAFE `yaml.load`; treat `null`/`undefined` parse result as `{}`; throw the same concrete non-mapping error for a sequence/scalar root (reuse the existing `got = Array.isArray(...) ? 'sequence' : typeof parsed` wording so both functions emit consistent errors); read `doc.customModes` defensively as an array. Removal rule: filter OUT any entry whose slug is owned. Determine ownership by the SAME `isOwnedSlug` helper already in the file (slug === 'gsd' or starts with 'gsd-') AND, if `ownedSlugs` is a non-empty array, intersect with it so the caller can scope removal; when `ownedSlugs` is omitted/empty, fall back to all `isOwnedSlug` slugs (the uninstall default). Re-emit with `yaml.dump(doc, { lineWidth: -1 })` to match `mergeCustomModes`. Keep js-yaml strictly inside this file (the installer never requires js-yaml). Add `unmergeCustomModes` to `module.exports`. Per D-06 it must NEVER delete the file and NEVER drop a non-owned user slug. Write `test/installer/unmerge.test.cjs` covering every <behavior> case, copying the house style from test/merge.test.cjs (yaml.load helper, assert.throws regex, deepEqual on parsed modes-by-slug).
  </action>
  <verify>
    <automated>node --test test/installer/unmerge.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/installer/unmerge.test.cjs` passes with at least the 5 <behavior> cases.
    - `grep -c 'unmergeCustomModes' src/bob-adapter.cjs` returns >= 2 (definition + export).
    - `grep -v '^#' test/installer/unmerge.test.cjs | grep -c "assert.throws"` >= 1 (fail-loud path covered).
    - The full suite stays green: `npm test`.
  </acceptance_criteria>
  <done>unmergeCustomModes is exported from the adapter, removes only owned slugs, preserves all user slugs, is idempotent, and fails loud on a non-mapping root.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the manifest module (sha256, schema read/write, update + orphan classification)</name>
  <files>src/installer/manifest.cjs, test/installer/manifest.test.cjs</files>
  <read_first>
    - .planning/phases/03-installer/03-RESEARCH.md (Code Examples: sha256 of bytes-as-written, classifyOnUpdate, manifest shape D-01/D-02; Pitfall 3 hash-the-bytes-not-the-source; Pitfall 4 file vs merged)
    - .planning/phases/03-installer/03-PATTERNS.md (`src/installer/manifest.cjs` section — sha256 via node:crypto, JSON via JSON.parse/JSON.stringify, manifest is SOLE source of truth D-03)
    - test/descriptor.test.cjs (house style for a pure-module unit test: hermetic, no live filesystem dependency where avoidable)
  </read_first>
  <behavior>
    - sha256(Buffer.from('abc')) returns a stable 64-char hex string equal across calls (determinism).
    - buildManifest({ scope, configHome, gsdBobVersion, entries }) returns an object with schemaVersion, gsdBobVersion, scope, configHome, generatedAt (ISO string), and entries[].
    - writeManifest then readManifest round-trips entries[] deepEqual (excluding generatedAt timestamp).
    - classifyOnUpdate(entry, absPath): missing file → 'rewrite'; on-disk bytes hash === entry.sha256 → 'overwrite'; differs → 'skip-warn'.
    - classifyOrphan(entry, absPath): on-disk hash === entry.sha256 → 'remove'; differs → 'keep-warn'; missing → 'remove' (already gone, drop entry).
    - A manifest written then re-read for an unrelated user path NOT in entries[] yields no classification (the API never invents entries from the filesystem).
  </behavior>
  <action>
    Create `src/installer/manifest.cjs` (`'use strict'`, `node:fs`/`node:path`/`node:crypto` only — NO js-yaml, NO third-party). Export: `MANIFEST_FILENAME` = `.gsd-bob-manifest.json`; `sha256(buf)` = `createHash('sha256').update(buf).digest('hex')` over the exact byte buffer (Pitfall 3 — hash bytes-as-written, never the source); `manifestPath(configHome)` = `path.join(configHome, MANIFEST_FILENAME)`; `readManifest(configHome)` = JSON.parse the dotfile, returning `null` when absent (ENOENT) and throwing loud on a corrupt/non-parseable manifest (fail-loud, never silently treat a broken manifest as empty); `writeManifest(configHome, manifestObj)` = JSON.stringify(obj, null, 2) + '\n' (byte-stable); `buildManifest({ schemaVersion=1, gsdBobVersion, scope, configHome, entries })` returning the D-01 schema object with `generatedAt: new Date().toISOString()`; `classifyOnUpdate(entry, absPath)` per D-04 (missing→'rewrite', match→'overwrite', differ→'skip-warn') reading the file back and re-hashing; `classifyOrphan(entry, absPath)` per D-05 (match or missing→'remove', differ→'keep-warn'). The manifest is the SOLE source of truth (D-03): the module exposes NO "scan a directory for removable files" function. Each entry shape is `{ path, sha256, kind }` with `kind` in `'file' | 'merged'`. Write `test/installer/manifest.test.cjs` covering every <behavior> case using a scratch tmpdir (`fs.mkdtempSync(path.join(os.tmpdir(), 'gsdbob-'))`) for read/write round-trips, mirroring the hermetic style.
  </action>
  <verify>
    <automated>node --test test/installer/manifest.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/installer/manifest.test.cjs` passes covering all 6 <behavior> cases.
    - `grep -cE "require\\('js-yaml'\\)|require\\(\"js-yaml\"\\)" src/installer/manifest.cjs` returns 0 (YAML confined to adapter).
    - `grep -c "createHash('sha256')" src/installer/manifest.cjs` >= 1.
    - `node -e "const m=require('./src/installer/manifest.cjs'); console.log(m.MANIFEST_FILENAME)"` prints `.gsd-bob-manifest.json`.
    - `npm test` stays green.
  </acceptance_criteria>
  <done>manifest.cjs computes a stable sha256 of bytes-as-written, round-trips the D-01 schema, classifies update and orphan outcomes from entries[] only, never scans the filesystem to invent entries, and requires no third-party module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → manifest read | a `.gsd-bob-manifest.json` on disk is untrusted input parsed by the installer |
| disk → custom_modes.yaml | an existing user-authored YAML file is untrusted input parsed during un-merge |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | manifest.cjs readManifest | mitigate | A corrupt/non-JSON manifest throws loud (fail-loud), never silently treated as empty (which would orphan every tracked file). Tested via assert.throws. |
| T-03-02 | Tampering | bob-adapter.unmergeCustomModes | mitigate | Non-mapping YAML root throws the same concrete error as mergeCustomModes; un-merge NEVER deletes the file or drops a non-owned user slug (D-06). Tested. |
| T-03-03 | Information Disclosure | manifest.cjs sha256 | accept | sha256 is used for ownership/integrity detection only, not as a security primitive; node:crypto built-in, not hand-rolled (V6). |
| T-03-04 | Tampering | js-yaml arbitrary-tag execution | accept | js-yaml v4 default SAFE schema does not execute tags (verified Phase 2 control); YAML stays confined to the adapter. |
</threat_model>

<verification>
- `node --test test/installer/unmerge.test.cjs` green.
- `node --test test/installer/manifest.test.cjs` green.
- `npm test` (full suite, Phase 2 golden tests must stay green).
- `grep -cE "require\\('js-yaml'\\)" src/installer/manifest.cjs` == 0.
</verification>

<success_criteria>
- unmergeCustomModes exported, slug-scoped removal, user-slug preservation, idempotent, fail-loud — proven by test/installer/unmerge.test.cjs.
- manifest.cjs is the single-source-of-truth primitive (sha256 of bytes, schema read/write, update/orphan classification) with zero third-party deps — proven by test/installer/manifest.test.cjs.
- INSTALL-05 manifest mechanism foundation in place for Plan 03 (staging) and Plan 04 (uninstall) to consume.
</success_criteria>

<output>
Create `.planning/phases/03-installer/03-01-SUMMARY.md` when done.
</output>
