---
phase: 02-runtime-foundation-artifact-translation
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - test/text-mode-golden.test.cjs
  - test/fixtures/text-mode/config.json
  - .planning/ACCEPTANCE-CHECKLIST.md
  - SUPPORT-ROSTER.md
autonomous: true
requirements: [TRANS-03, RUNTIME-01, RUNTIME-02, RUNTIME-03, RUNTIME-04, TRANS-01, TRANS-02, TRANS-04, TRANS-05]
must_haves:
  truths:
    - "With workflow.text_mode forced true for the bob runtime, a configured interactive flow asks via a numbered text list and captures a validated answer in the Claude runtime (no AskUserQuestion)"
    - "text_mode is satisfied by reusing gsd-core's existing config+workflow seam — NO prompt-rewriting in the converters"
    - "Every Phase 2 requirement appends a device-runnable AC step (AC-05+) to the consolidated .planning/ACCEPTANCE-CHECKLIST.md using the Cmd/Expect/Confirms/Result schema"
    - "A generated support roster records any unsupported-on-Bob artifact so gaps are loud"
  artifacts:
    - path: "test/text-mode-golden.test.cjs"
      provides: "TRANS-03 golden — flow asks + captures validated answer with text_mode on"
    - path: ".planning/ACCEPTANCE-CHECKLIST.md"
      provides: "AC-05+ device-runnable steps for each Phase 2 requirement (appended)"
      contains: "AC-05"
    - path: "SUPPORT-ROSTER.md"
      provides: "generated unsupported-on-Bob roster"
  key_links:
    - from: "test/fixtures/text-mode/config.json"
      to: "gsd-core/bin/lib/config-loader.cjs"
      via: "workflow.text_mode:true flows into init JSON; workflows swap AskUserQuestion for numbered text"
      pattern: "text_mode"
    - from: ".planning/ACCEPTANCE-CHECKLIST.md"
      to: "Phase 6 on-device pass"
      via: "AC-05+ appended per Phase 1 D-07 convention; Phase 6 runs the whole file"
      pattern: "AC-0[5-9]"
---

<objective>
Satisfy TRANS-03 by reusing gsd-core's existing `text_mode` seam (default it `true` for the bob runtime; verify the configured flow degrades `AskUserQuestion` to numbered text and captures a validated answer in the Claude runtime — no converter rewriting). Then close the phase: run the full `node:test` suite as the phase gate, append the device-runnable AC-05+ steps for every Phase 2 requirement to the consolidated acceptance checklist, and emit the generated support roster.

Purpose: TRANS-03 plus the cross-cutting phase-close obligations (full-suite gate + D-11/Phase-1-D-07 acceptance-checklist append). This plan depends on the runtime spine (02-01) and the converters/adapter (02-02) being in place.
Output: `text-mode-golden.test.cjs`, appended `AC-05`+ steps in `.planning/ACCEPTANCE-CHECKLIST.md`, and `SUPPORT-ROSTER.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-runtime-foundation-artifact-translation/02-CONTEXT.md
@.planning/phases/02-runtime-foundation-artifact-translation/02-RESEARCH.md
@.planning/ACCEPTANCE-CHECKLIST.md
</context>

<artifacts_this_phase_produces>
Symbols/paths CREATED by this plan (exclude from drift verification):
- `test/text-mode-golden.test.cjs` and `test/fixtures/text-mode/config.json` (a `.planning/config.json`-shaped fixture with `workflow.text_mode:true`)
- Appended steps `AC-05`..`AC-NN` in `.planning/ACCEPTANCE-CHECKLIST.md` (one+ per Phase 2 requirement, Cmd/Expect/Confirms/Result schema)
- `SUPPORT-ROSTER.md` (generated unsupported-on-Bob roster at project root)
- The bob-runtime `workflow.text_mode:true` default convention (install-written config, option a per RESEARCH Pattern 4)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: text_mode degradation for the bob runtime (TRANS-03) golden test</name>
  <read_first>
    - .planning/phases/02-runtime-foundation-artifact-translation/02-RESEARCH.md §"Pattern 4" (text_mode is runtime-config, NOT a converter; option (a) — set text_mode:true in the install-written config — is preferred over a config-loader override), §"Assumptions Log" A3
    - .planning/phases/02-runtime-foundation-artifact-translation/02-CONTEXT.md D-09 (reuse text_mode seam; bob defaults workflow.text_mode:true; NO prompt-rewriting; golden test asserts the flow asks + captures a validated answer with text_mode forced on)
    - ~/.claude/gsd-core/bin/shared/config-defaults.manifest.json line 38 (`"text_mode": false` global default under `workflow`)
    - ~/.claude/gsd-core/bin/lib/config-loader.cjs lines 107, 557, 663 (where text_mode flows into init JSON)
    - ~/.claude/gsd-core/workflows/plan-phase.md line 160 + 380/462 (the workflow-layer branch: TEXT_MODE true → replace every AskUserQuestion with a numbered text list and capture the typed choice)
  </read_first>
  <files>test/text-mode-golden.test.cjs, test/fixtures/text-mode/config.json</files>
  <behavior>
    - With a `.planning/config.json`-shaped fixture carrying `workflow.text_mode:true`, the config loader projects `text_mode:true` into the init JSON (the value the workflow markdown reads)
    - The configured flow, under text_mode, presents a numbered text list and captures+validates a typed answer (asserted via the seam, NOT via a rewritten converter)
    - No converter is involved in the degradation (TRANS-03 is config+workflow, not artifact transform) — assert that the bob converters from 02-02 do NOT contain prompt-rewriting logic for AskUserQuestion
  </behavior>
  <action>
    Author `test/fixtures/text-mode/config.json`: a minimal `.planning/config.json`-shaped object with `workflow.text_mode: true` (the bob-runtime default per D-09 option a). Author `test/text-mode-golden.test.cjs` that loads the fixture through the vendored `config-loader.cjs` (via test/_helpers/vendor.cjs) and asserts the resolved init projection has `text_mode === true` (proving the seam carries the bob default into the value workflows read at line 160). Add an assertion that simulates the workflow-layer degradation contract: given text_mode true, the expected presentation is a numbered text list and a validated typed-answer capture (assert against the documented seam behavior — a small harness that, given text_mode:true + a question spec, produces a numbered list and validates a numeric choice). Add a guard assertion that the bob converters (`convertClaudeCommandToBobSkill`/`convertClaudeCommandToBobCommand`) do NOT rewrite AskUserQuestion (verify by checking the converter source slice contains no AskUserQuestion-rewriting branch) — confirming D-09's "no prompt-rewriting in converters". Build any forbidden-token check programmatically so the test file does not self-trip.
    Record the bob-runtime `workflow.text_mode:true` default decision (option a) in the SUMMARY so Phase 3's installer writes it into the bob `.planning/config.json`.
  </action>
  <verify>
    <automated>node --test test/text-mode-golden.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/text-mode-golden.test.cjs` exits 0
    - The fixture's `workflow.text_mode:true` projects to `text_mode === true` in the resolved init JSON via the vendored config-loader
    - The degradation harness produces a numbered text list and captures/validates a typed numeric choice with text_mode on (asserting the flow asks + captures a validated answer in the Claude runtime)
    - The bob converters contain NO AskUserQuestion prompt-rewriting branch (D-09 reuse-the-seam, not rebuild)
  </acceptance_criteria>
  <done>text-mode-golden.test.cjs proves that with workflow.text_mode forced true for the bob runtime the existing seam degrades AskUserQuestion to a numbered text list and captures a validated answer, with no converter rewriting — satisfying TRANS-03 by reuse.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite phase gate + emit generated support roster</name>
  <read_first>
    - .planning/phases/02-runtime-foundation-artifact-translation/02-RESEARCH.md §"Validation Architecture" → "Sampling Rate" (per-wave merge = `node --test test/`; phase gate = full suite green before verify)
    - src/bob-adapter.cjs (Task 3 of 02-02 — gateArtifact/buildSupportRoster — the roster source)
    - .planning/phases/02-runtime-foundation-artifact-translation/02-CONTEXT.md D-10 (the unsupported marker string `unsupported on Bob: <reason>` in a generated support roster)
  </read_first>
  <files>SUPPORT-ROSTER.md</files>
  <action>
    Run the full test suite `node --test test/` and confirm all eight test files (descriptor, backend-neutrality, planning-bytecompat from 02-01; skill-golden, command-golden, merge, unsupported-gate from 02-02; text-mode-golden from this plan) pass — this is the phase gate before verification.
    Generate `SUPPORT-ROSTER.md` at the project root from the `bob-adapter` gate: run the representative candidate set through `gateArtifact`/`buildSupportRoster` and write the resulting roster (each unsupported entry as `unsupported on Bob: <reason>`, supported entries listed as emitted). For Phase 2 this roster proves the mechanism on the representative case(s); the full-roster generation rides with Phases 4-5. Include a header noting the comment-loss caveat of the YAML merge (js-yaml drops comments on re-emit — Pitfall 5) and that the roster is regenerated by the installer/emitter, not hand-maintained.
  </action>
  <verify>
    <automated>node --test test/ && node -e "const fs=require('fs'); const r=fs.readFileSync('SUPPORT-ROSTER.md','utf8'); if(!/unsupported on Bob:/.test(r))throw new Error('roster missing marker'); console.log('roster OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/` exits 0 (all eight Phase 2 test files green)
    - `SUPPORT-ROSTER.md` exists at the project root and contains at least one `unsupported on Bob: <reason>` line for the representative unsupported case
    - The roster header documents the YAML comment-loss caveat and that it is generated, not hand-maintained
  </acceptance_criteria>
  <done>The full Phase 2 test suite passes as the phase gate and SUPPORT-ROSTER.md is generated from the bob-adapter gate, recording the representative unsupported-on-Bob case loudly.</done>
</task>

<task type="auto">
  <name>Task 3: Append device-runnable AC-05+ steps for every Phase 2 requirement (D-11 / Phase-1 D-07)</name>
  <read_first>
    - .planning/ACCEPTANCE-CHECKLIST.md (the append target — read the existing schema: stable AC-ID + `Cmd:`/`Expect:`/`Confirms:`/`Result: [ ] pass [ ] fail`; the Safety invariant T-01-SC — every Cmd MUST be read-only/side-effect-free; existing steps run to AC-04)
    - .planning/phases/02-runtime-foundation-artifact-translation/02-CONTEXT.md D-11 (each requirement appends device-runnable AC steps AC-05+), and the canonical_refs note that these are the live-Bob steps deferred from dev-time verification
    - .planning/REQUIREMENTS.md (the 9 Phase 2 requirement IDs each AC step Confirms)
    - .planning/phases/01-bob-capability-mapping/01-CONTEXT.md D-07 (the append convention)
  </read_first>
  <files>.planning/ACCEPTANCE-CHECKLIST.md</files>
  <action>
    APPEND (never overwrite) new steps starting at `AC-05` to `.planning/ACCEPTANCE-CHECKLIST.md`, one or more per Phase 2 requirement, using the EXACT existing schema (`Cmd:` / `Expect:` / `Confirms:` / `Result: [ ] pass  [ ] fail`). Each `Cmd:` MUST be read-only / side-effect-free per the file's Safety invariant T-01-SC — these run unattended on a real Bob machine in Phase 6. Cover the live-Bob confirmations deferred from dev-time golden verification:
    - RUNTIME-01/02: on a Bob machine, run the shim with BOB_CONFIG_DIR overridden and read-only confirm gsd_run resolves the bob home (e.g. `BOB_CONFIG_DIR=/tmp/xbob node gsd-core/bin/gsd-tools.cjs query state.load` observed to read from the overridden path; and unset → `~/.bob`). Confirms RUNTIME-01, RUNTIME-02.
    - TRANS-01/02: load the emitted `.bob/commands/<name>.md` and `.bob/skills/<name>/SKILL.md` in Bob and read-only confirm the command appears with its description and the skill is recognized (description present so it is not ignored). Confirms TRANS-01, TRANS-02.
    - TRANS-03: run a GSD interactive flow under Bob and read-only confirm it degrades to a numbered text question and captures a typed answer. Confirms TRANS-03.
    - TRANS-04: read-only inspect that an unsupported skill is absent from the emitted set and listed in SUPPORT-ROSTER.md. Confirms TRANS-04.
    - TRANS-05: read-only confirm that after a (separate, Phase-3) install, a pre-seeded user mode in `~/.bob/custom_modes.yaml` is still present and the `gsd` slug appears once. Confirms TRANS-05. (The Cmd here is a read-only `cat`/listing — the install itself is a Phase 3/6 action, not this Cmd.)
    - RUNTIME-03: read-only golden-diff a `.planning/` artifact produced under Bob against the committed claude-produced golden. Confirms RUNTIME-03.
    - RUNTIME-04: read-only grep the bob core paths for model-name literals (expect none). Confirms RUNTIME-04.
    Number them AC-05, AC-06, … sequentially after AC-04. Do NOT renumber or modify AC-01..AC-04.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('.planning/ACCEPTANCE-CHECKLIST.md','utf8'); if(!c.includes('## AC-05'))throw new Error('missing AC-05'); const reqs=['RUNTIME-01','RUNTIME-02','RUNTIME-03','RUNTIME-04','TRANS-01','TRANS-02','TRANS-03','TRANS-04','TRANS-05']; for(const r of reqs){ if(!c.includes(r))throw new Error('no AC step confirms '+r);} if(!/AC-01/.test(c)||!/AC-04/.test(c))throw new Error('clobbered existing steps'); console.log('AC append OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ACCEPTANCE-CHECKLIST.md` contains a new `## AC-05` (and subsequent) section appended AFTER AC-04; AC-01..AC-04 are untouched
    - Every Phase 2 requirement ID (RUNTIME-01..04, TRANS-01..05) appears in at least one appended AC step's `Confirms:` field
    - Every appended `Cmd:` is read-only / side-effect-free (no install/write/delete/move/copy) per the Safety invariant T-01-SC
    - Each appended step uses the exact `Cmd:` / `Expect:` / `Confirms:` / `Result: [ ] pass  [ ] fail` schema
  </acceptance_criteria>
  <done>The consolidated acceptance checklist gains read-only AC-05+ steps covering all nine Phase 2 requirements' live-Bob confirmations, appended without disturbing AC-01..AC-04, ready for the Phase 6 unattended on-device pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install-written config → workflow | `workflow.text_mode:true` in `.planning/config.json` controls whether interactive flows degrade — a config value steering runtime prompt behavior |
| acceptance-checklist Cmd → on-device shell | every `Cmd:` line runs unattended on the user's real Bob machine in Phase 6 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | An acceptance-checklist `Cmd:` that mutates the user's machine when run unattended in Phase 6 | mitigate | Safety invariant T-01-SC enforced: every appended `Cmd:` is read-only (listing / `echo $VAR` / read-only `gsd_run query` / read-only `cat`); the verify gate and acceptance criteria assert no install/write/delete/move/copy. |
| T-02-09 | Repudiation | text_mode silently NOT degrading, so an interactive flow stalls under Bob | mitigate | text-mode-golden.test.cjs asserts the seam carries `text_mode:true` into init JSON and the flow asks + captures a validated answer; the bob runtime default is recorded for Phase 3 to write. |
| T-02-10 | Information Disclosure | Support roster going stale / silent (gap not surfaced) | mitigate | SUPPORT-ROSTER.md is generated from the gate (not hand-maintained) and asserted to contain the `unsupported on Bob:` marker; header documents regeneration. |
</threat_model>

<verification>
- `node --test test/` green (all eight Phase 2 test files) — the phase gate
- `.planning/ACCEPTANCE-CHECKLIST.md` has AC-05+ covering all nine requirements; AC-01..AC-04 intact; every appended Cmd read-only
- `SUPPORT-ROSTER.md` present with the unsupported marker
</verification>

<success_criteria>
- TRANS-03: with text_mode forced true for bob, the flow degrades AskUserQuestion to numbered text and captures a validated answer (golden) — by reuse, no converter rewriting
- Phase gate: full node:test suite green
- D-11 / Phase-1 D-07: device-runnable AC-05+ steps appended for every Phase 2 requirement; support roster emitted
</success_criteria>

<output>
Create `.planning/phases/02-runtime-foundation-artifact-translation/02-03-SUMMARY.md` when done.
</output>