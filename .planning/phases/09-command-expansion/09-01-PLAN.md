---
phase: 09-command-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/new-milestone.md
  - commands/gsd/complete-milestone.md
  - commands/gsd/milestone-summary.md
  - commands/gsd/quick.md
  - commands/gsd/fast.md
  - commands/gsd/ship.md
  - commands/gsd/explore.md
  - commands/gsd/spec-phase.md
  - commands/gsd/mvp-phase.md
  - commands/gsd/map-codebase.md
  - commands/gsd/ui-phase.md
  - commands/gsd/secure-phase.md
  - commands/gsd/extract-learnings.md
  - commands/gsd/docs-update.md
  - commands/gsd/health.md
  - commands/gsd/stats.md
  - commands/gsd/resume-work.md
  - commands/gsd/pause-work.md
  - commands/gsd/code-review.md
  - commands/gsd/debug.md
  - commands/gsd/audit-fix.md
  - commands/gsd/audit-uat.md
  - SUPPORT-ROSTER.md
  - test/fixtures/quality-gates/code-review.command.expected.md
  - test/fixtures/quality-gates/code-review.skill.expected.md
  - test/fixtures/quality-gates/debug.command.expected.md
  - test/fixtures/quality-gates/debug.skill.expected.md
  - test/fixtures/quality-gates/audit-fix.command.expected.md
  - test/fixtures/quality-gates/audit-fix.skill.expected.md
  - test/fixtures/quality-gates/audit-uat.command.expected.md
  - test/fixtures/quality-gates/audit-uat.skill.expected.md
autonomous: true
requirements: [CMD-01, CMD-02, CMD-03]

must_haves:
  truths:
    - "commands/gsd/ holds exactly 28 pristine command sources, all on gsd-core 1.6.1 (18 new + 4 re-synced + 6 already-identical), each byte-identical to the @opengsd/gsd-core@1.6.1 tarball package/commands/gsd/ source (D-01, D-02, F-01, F-03)"
    - "SUPPORT-ROSTER.md (repo root) lists all 28 gsd-<stem> commands as Supported plus the 2 curated skip fixtures, regenerated by the script — never hand-edited (D-08, F-05)"
    - "The 8 quality-gate golden fixtures byte-match the vendored converter's output over the re-synced 1.6.1 sources, so quality-gate-equivalence.test.cjs is green (Q5, Pitfall 1)"
  artifacts:
    - "commands/gsd/{new-milestone,complete-milestone,milestone-summary,quick,fast,ship,explore,spec-phase,mvp-phase,map-codebase,ui-phase,secure-phase,extract-learnings,docs-update,health,stats,resume-work,pause-work}.md (18 new pristine sources)"
    - "commands/gsd/{code-review,debug,audit-fix,audit-uat}.md (4 re-synced to 1.6.1)"
    - "SUPPORT-ROSTER.md (regenerated, 28 supported + 2 skip)"
    - "test/fixtures/quality-gates/*.{command,skill}.expected.md (8 regenerated)"
  key_links:
    - "commands/gsd/*.md -> scripts/generate-support-roster.cjs -> SUPPORT-ROSTER.md (directory-derived, drift-proof)"
    - "re-synced commands/gsd/*.md -> convertClaudeCommandToBobCommand/Skill -> regenerated test/fixtures/quality-gates/*.expected.md -> quality-gate-equivalence.test.cjs"
---

<objective>
Grow the vendored command-source set from 10 to 28 by dropping in the 18 curated pristine sources and re-syncing the 4 drifted ones — the purely-additive "product change" of Phase 9. This is a SOURCE-ONLY vendoring plan: it adds and re-syncs `commands/gsd/*.md` files and regenerates two derived artifacts. It touches ZERO machinery — no edit to `src/installer/stage.cjs`, `src/bob-adapter.cjs` (`gateArtifact`/`buildSupportRoster`/`neutralizeModelReferences`), or the vendored `gsd-core/bin/lib/runtime-artifact-conversion.cjs` converters. The directory-derived staging loop, gate, and roster generator auto-consume the additions (F-05).

The plan splits along the two decisions with different consequences:
- Task 1 (D-01, additive): vendor the 18 NEW pristine sources from the immutable `@opengsd/gsd-core@1.6.1` tarball (F-01/F-02), byte-verify against the tarball, and regenerate `SUPPORT-ROSTER.md` (D-08) to the full 28-supported set. The 18 are net-new (no existing fixtures), so the full suite stays green after this task — including the Phase 8 NEUTRAL-03 invariant which directory-derives and auto-covers them.
- Task 2 (D-02, version reconciliation): re-sync the 4 DRIFTED sources (`code-review`, `debug`, `audit-fix`, `audit-uat`) to their 1.6.1 tarball versions so all 28 land on one consistent version (SYNC-01 spirit, F-03), then regenerate the 8 quality-gate golden fixtures in the SAME task (the trailing-blank-line drift makes them go stale — Q5/Pitfall 1) so the suite ends green. Fixture regen is bound tightly to the re-sync inside one task: the suite is never observed red at any task or plan boundary.

Every one of the 18 classifies Supported by construction — the gate is name/`requires[]`-based, not tool-inspecting, and derived candidates carry `requires: []`; the only skip vector is the single `BOB_SKIP_LIST` entry `gsd-autonomous`, which is not among the 18 (D-03, D-04, F-04, Q2/Q3). No gate edit is possible or needed.

Purpose: satisfy CMD-01 (28 sources vendored, installer auto-emits), CMD-02 (roster reflects all 28), and the source/fixture foundation for CMD-03 (proven in Plan 09-02).
Output: 22 vendored `commands/gsd/*.md` sources on one consistent 1.6.1 version, a regenerated 28-command `SUPPORT-ROSTER.md`, and 8 regenerated quality-gate golden fixtures.
</objective>

<artifacts_produced>
## Artifacts this plan produces

NEW source files (18 pristine vendored, verbatim upstream 1.6.1):
`commands/gsd/new-milestone.md`, `complete-milestone.md`, `milestone-summary.md`, `quick.md`, `fast.md`, `ship.md`, `explore.md`, `spec-phase.md`, `mvp-phase.md`, `map-codebase.md`, `ui-phase.md`, `secure-phase.md`, `extract-learnings.md`, `docs-update.md`, `health.md`, `stats.md`, `resume-work.md`, `pause-work.md`.

RE-SYNCED source files (4, 1.5.0 -> 1.6.1):
`commands/gsd/code-review.md`, `debug.md`, `audit-fix.md`, `audit-uat.md`.

REGENERATED artifacts:
- `SUPPORT-ROSTER.md` (repo root) — full 28-command Supported roster + 2 curated skip lines, produced by `node scripts/generate-support-roster.cjs`.
- `test/fixtures/quality-gates/{code-review,debug,audit-fix,audit-uat}.{command,skill}.expected.md` (8 files) — re-frozen from the vendored converter over the re-synced 1.6.1 sources.

This plan creates NO new source symbols in `src/` and edits NO code in `src/` or `gsd-core/`. It only vendors markdown sources and regenerates derived artifacts.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-command-expansion/09-CONTEXT.md
@.planning/phases/09-command-expansion/09-RESEARCH.md
@.planning/phases/09-command-expansion/09-PATTERNS.md

# The vendor target dir + a representative pristine analog (colon form + full frontmatter)
@commands/gsd/progress.md
# The roster generator (directory-derived; run, never edit)
@scripts/generate-support-roster.cjs
# The fixture consumer + the exact converter calls that produce the 8 fixtures
@test/quality-gate-equivalence.test.cjs
# The hermetic vendored-converter resolver
@test/_helpers/vendor.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the 18 new pristine 1.6.1 sources + regenerate the roster</name>
  <files>commands/gsd/new-milestone.md, commands/gsd/complete-milestone.md, commands/gsd/milestone-summary.md, commands/gsd/quick.md, commands/gsd/fast.md, commands/gsd/ship.md, commands/gsd/explore.md, commands/gsd/spec-phase.md, commands/gsd/mvp-phase.md, commands/gsd/map-codebase.md, commands/gsd/ui-phase.md, commands/gsd/secure-phase.md, commands/gsd/extract-learnings.md, commands/gsd/docs-update.md, commands/gsd/health.md, commands/gsd/stats.md, commands/gsd/resume-work.md, commands/gsd/pause-work.md, SUPPORT-ROSTER.md</files>
  <read_first>
    - `commands/gsd/progress.md` — the pristine-source analog: colon command form (`name: gsd:progress`) and full frontmatter (`effort`, `allowed-tools`, `requires`). Every vendored source MUST retain this verbatim shape; the converter strips/rewrites at emit (F-02, PATTERNS "Vendor-as-source, transform-at-emit").
    - `scripts/generate-support-roster.cjs:32-38` — the directory-derived candidate set; it maps each `<stem>.md` to `{ name: 'gsd-'+stem, requires: [] }` and appends the 2 curated skip fixtures. Run it; do NOT edit it (D-08).
    - 09-RESEARCH.md Q1 (fetch sequence) and Q3 (why adding Supported commands is purely additive — the gate never fires for a `requires: []` derived candidate).
  </read_first>
  <action>
    Fetch the immutable tarball into a scratch dir OUTSIDE the repo tree and copy the 18 new sources in pristine — do NOT pre-transform (F-02, Pitfall 5). Sequence: create a scratch dir with `mktemp -d`; inside it run `npm pack @opengsd/gsd-core@1.6.1`; extract with `tar xzf opengsd-gsd-core-1.6.1.tgz` (yields `package/commands/gsd/*.md`). Copy each of the 18 new stems from `<scratch>/package/commands/gsd/<stem>.md` to `commands/gsd/<stem>.md` using `cp` (or `node:fs` cpSync). The 18 stems are exactly: new-milestone, complete-milestone, milestone-summary, quick, fast, ship, explore, spec-phase, mvp-phase, map-codebase, ui-phase, secure-phase, extract-learnings, docs-update, health, stats, resume-work, pause-work — no more, no less (the deferred long-tail `transition`/`ai-integration-phase`/autonomy cluster stay OUT, per CONTEXT Deferred Ideas).

    Then assert byte-identity: for each of the 18 stems run `diff -q commands/gsd/<stem>.md <scratch>/package/commands/gsd/<stem>.md` and confirm zero drift (this is the supply-chain tamper gate, T-09-01). Any `DRIFT:` output fails the task.

    Then regenerate the roster: run `node scripts/generate-support-roster.cjs` (D-08). Because the candidate set is derived from the now-28-file `commands/gsd/` directory, this auto-produces the full 28-supported roster (+ the 2 curated skip fixtures `gsd-autonomous`, `gsd-parallel-fanout`). Do NOT hand-edit `SUPPORT-ROSTER.md`. The script prints `... (2 unsupported, 28 supported)` on success.

    Do NOT touch `code-review`/`debug`/`audit-fix`/`audit-uat` in this task — they are re-synced in Task 2. Do NOT edit any file under `src/` or `gsd-core/`.
  </action>
  <verify>
    <automated>test $(ls commands/gsd/*.md | wc -l | tr -d ' ') -eq 28 && node scripts/generate-support-roster.cjs | grep -q '28 supported' && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `commands/gsd/` contains exactly 28 `.md` files (10 prior + 18 new).
    - Each of the 18 new files is byte-identical to its `@opengsd/gsd-core@1.6.1` tarball source (`diff -q` reports no differences).
    - `node scripts/generate-support-roster.cjs` stdout contains `28 supported`; `SUPPORT-ROSTER.md` lists all 28 `gsd-<stem>` under Supported and the 2 curated skip lines under Unsupported.
    - `npm test` exits 0 — the 18 additions are auto-covered green by the directory-derived roster-capmap and NEUTRAL-03 model-neutrality suites, and the quality-gate fixtures still match (the 4 drifted sources are untouched this task).
  </acceptance_criteria>
  <done>28 pristine 1.6.1 sources present in commands/gsd/ (18 new byte-verified), SUPPORT-ROSTER.md regenerated to 28 supported, full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Re-sync the 4 drifted sources to 1.6.1 + regenerate the 8 quality-gate fixtures</name>
  <files>commands/gsd/code-review.md, commands/gsd/debug.md, commands/gsd/audit-fix.md, commands/gsd/audit-uat.md, test/fixtures/quality-gates/code-review.command.expected.md, test/fixtures/quality-gates/code-review.skill.expected.md, test/fixtures/quality-gates/debug.command.expected.md, test/fixtures/quality-gates/debug.skill.expected.md, test/fixtures/quality-gates/audit-fix.command.expected.md, test/fixtures/quality-gates/audit-fix.skill.expected.md, test/fixtures/quality-gates/audit-uat.command.expected.md, test/fixtures/quality-gates/audit-uat.skill.expected.md</files>
  <read_first>
    - `test/quality-gate-equivalence.test.cjs:39,58,64-73` — how the fixtures are produced and consumed: `const conv = requireVendor('runtime-artifact-conversion.cjs')`, then `conv.convertClaudeCommandToBobCommand(source, 'gsd-'+stem)` -> `<stem>.command.expected.md` and `conv.convertClaudeCommandToBobSkill(source, 'gsd-'+stem)` -> `<stem>.skill.expected.md`. Regeneration must produce EXACTLY what this test reads.
    - `test/_helpers/vendor.cjs` — `requireVendor` / `repoRoot`: resolve the converter through the VENDORED gsd-core (carries the `bob` runtime), never the global install.
    - 09-RESEARCH.md Q5 — the full 1.5.0->1.6.1 diff of the 4 drifted sources: only added frontmatter keys (`requires`, `type: prompt`, all stripped by the converter) and removed trailing blank lines; no Bob-hostile construct, no model directive. The blank-line removal is what changes the emitted body and forces the fixture regen.
  </read_first>
  <action>
    Re-sync the 4 drifted sources from the SAME immutable tarball (D-02, F-03). Fetch fresh into a `mktemp -d` scratch dir (`npm pack @opengsd/gsd-core@1.6.1` + `tar xzf ...`) — do not rely on a scratch dir from Task 1 (shell state does not persist between task runs). Copy each of `code-review`, `debug`, `audit-fix`, `audit-uat` from `<scratch>/package/commands/gsd/<stem>.md` to `commands/gsd/<stem>.md` pristine. Assert byte-identity with `diff -q commands/gsd/<stem>.md <scratch>/package/commands/gsd/<stem>.md` for all 4 (zero drift, T-09-01).

    Then, TIGHTLY in the same task, regenerate the 8 quality-gate golden fixtures so the suite is never left red across the plan boundary (Q5/Q6 hard ordering, Pitfall 1). There is no fixture-generator script — produce the fixtures by running the vendored converter on the re-synced sources. Write a one-shot Node step (a throwaway inline `node -e` or a scratch script under the OS tmpdir, NOT a persistent file in the repo — CLAUDE.md: no unnecessary scripts) that, for each of the 4 stems: reads `commands/gsd/<stem>.md`, resolves the converter via `test/_helpers/vendor.cjs` `requireVendor('runtime-artifact-conversion.cjs')`, calls `convertClaudeCommandToBobCommand(source, 'gsd-'+stem)` and writes it to `test/fixtures/quality-gates/<stem>.command.expected.md`, and calls `convertClaudeCommandToBobSkill(source, 'gsd-'+stem)` and writes it to `test/fixtures/quality-gates/<stem>.skill.expected.md`. This mirrors exactly how `quality-gate-equivalence.test.cjs` computes the expected output, so the regenerated fixtures byte-match what the (unchanged) test recomputes.

    Do NOT hand-edit any fixture. Do NOT edit `test/quality-gate-equivalence.test.cjs` — it stays unchanged and simply goes green once the fixtures reflect the 1.6.1 output. Do NOT regenerate `SUPPORT-ROSTER.md` again (the re-sync changes file CONTENT, not stems, so the roster is unchanged from Task 1). Do NOT edit any file under `src/` or `gsd-core/`.
  </action>
  <verify>
    <automated>node --test test/quality-gate-equivalence.test.cjs test/roster-capmap.test.cjs test/model-neutrality.test.cjs && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 4 re-synced files is byte-identical to its `@opengsd/gsd-core@1.6.1` tarball source (`diff -q` reports no differences) — the 28 sources are now provably one consistent version.
    - The 8 regenerated `test/fixtures/quality-gates/*.expected.md` byte-match the vendored converter's output over the re-synced sources; `node --test test/quality-gate-equivalence.test.cjs` exits 0 (all per-stem byte-golden + strip-key + neutralization assertions green).
    - `node --test test/roster-capmap.test.cjs test/model-neutrality.test.cjs` exits 0 (roster still traces to the gate; emission still zero-literal).
    - `npm test` exits 0 — the full suite is green at the end of this task (no red across the plan/wave boundary).
  </acceptance_criteria>
  <done>The 4 drifted sources re-synced to 1.6.1 (byte-verified), all 28 sources on one consistent version, 8 quality-gate fixtures re-frozen, full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> repo tree | The `@opengsd/gsd-core@1.6.1` tarball crosses into `commands/gsd/`; a tampered or hand-edited source must not slip in. |
| vendored source -> emitted `.bob/` artifact | Model-brand literals in a source body must not survive the converter + Phase 8 post-pass into an emitted artifact (backend-neutrality). |
| gate/roster authority -> loadable set | A command Bob cannot support must never be silently emitted; every skip is recorded loud. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | vendored `commands/gsd/*.md` sources | medium | mitigate | Fetch only from the immutable `@opengsd/gsd-core@1.6.1` tarball; assert byte-identity with `diff -q` against the tarball for all 22 sources before commit (Task 1 + Task 2 gates). |
| T-09-02 | Information disclosure | emitted `.bob/` converted set | medium | mitigate | The Phase 8 NEUTRAL-03 invariant (`test/model-neutrality.test.cjs`) directory-derives and re-scans the full 28-command emission for zero model literals; re-run green in both tasks. |
| T-09-03 | Repudiation | `gateArtifact` / `SUPPORT-ROSTER.md` | low | mitigate | Regenerated roster records any unsupported command as a loud `unsupported on Bob: <reason>` line; all 18 verified Supported (Q2), so no silent parity gap. |
| T-09-SC | Tampering | `npm pack` fetch (supply chain) | low | accept | No new packages are installed. `npm pack @opengsd/gsd-core@1.6.1` fetches the project's OWN already-vendored, already-vetted upstream (Phase 7 `[VERIFIED]`); the immutable pinned version + the `diff -q` byte gate (T-09-01) is the mitigation. No `[ASSUMED]`/`[SUS]` package, so no legitimacy checkpoint applies. |
</threat_model>

<verification>
- `ls commands/gsd/*.md | wc -l` == 28.
- All 22 vendored/re-synced sources byte-identical to the `@opengsd/gsd-core@1.6.1` tarball (`diff -q` zero drift).
- `node scripts/generate-support-roster.cjs` reports `28 supported`; `SUPPORT-ROSTER.md` lists all 28 under Supported.
- `npm test` exits 0 at the end of the plan — quality-gate-equivalence (regenerated fixtures), roster-capmap, and model-neutrality all green over the 28-command set.
</verification>

<success_criteria>
- CMD-01 (foundation): 18 new sources vendored + 4 re-synced -> `commands/gsd/` holds exactly 28 pristine 1.6.1 sources, byte-verified.
- CMD-02 (roster): `SUPPORT-ROSTER.md` regenerated to the full 28-command Supported set via the script (D-08); all 18 Supported by construction (D-03/D-04).
- CMD-03 (foundation): the 8 quality-gate golden fixtures re-frozen to the 1.6.1 output; the model-neutrality invariant stays green over the 28-command emission.
</success_criteria>

<output>
Create `.planning/phases/09-command-expansion/09-01-SUMMARY.md` when done.
</output>
