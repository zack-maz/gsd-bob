---
phase: 09-command-expansion
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - test/command-expansion.test.cjs
autonomous: true
requirements: [CMD-01, CMD-02, CMD-03]

must_haves:
  truths:
    - "A scratch stage() run emits exactly 28 .bob/commands/gsd-*.md and 28 .bob/skills/gsd-*/SKILL.md — the emitted surface equals the source count and is pinned to 28 in exactly one CMD-01 guard (D-07, Q4b, Pitfall 4)"
    - "Each of the 28 sources converts to a structurally Bob-conformant command + skill: non-empty description, argument-hint kept iff the source declared one, effort/allowed-tools/agent/type/requires stripped, skill frontmatter is name + description only, bodies carry no Claude config-home path / no colon dialect and carry the .bob home + hyphen form (D-05, Q4a)"
    - "Every emitted converted command/skill over the full 28-command set carries zero model literals per scanModelLiterals, and the Phase 8 NEUTRAL-03 invariant stays green at 28 (D-06, F-05)"
    - "SUPPORT-ROSTER.md reflects the full 28-command Supported set — every commands/gsd stem appears as gsd-<stem> under Supported (CMD-02)"
  artifacts:
    - "test/command-expansion.test.cjs (new directory-derived structural equivalence + count==28 + roster-reflects-28 suite)"
  key_links:
    - "commands/gsd/*.md -> stage() -> .bob/commands + .bob/skills (count==28, D-07)"
    - "commands/gsd/*.md -> convertClaudeCommandToBobCommand/Skill -> scanModelLiterals -> zero literals (CMD-03)"
    - "commands/gsd/*.md -> SUPPORT-ROSTER.md Supported section (CMD-02)"
---

<objective>
Prove the existing roster-agnostic gates hold at 28 commands — the "verification" half of Phase 9. Author ONE new directory-derived test suite (`test/command-expansion.test.cjs`) that iterates the full `commands/gsd/*.md` directory rather than any hardcoded name list (D-05), so it auto-covers this and every future addition. The suite composes three real in-repo harness idioms (PATTERNS "highest-value mapping"): the per-stem structural + programmatic-forbidden-token loop from `test/quality-gate-equivalence.test.cjs`, the `scanModelLiterals` scan from `test/model-neutrality.test.cjs`, and the scratch-tmpdir `stage()` emission harness from `test/model-neutrality.test.cjs`. Then run the full invariant family green over the 28-command emission as the phase gate.

No `src/`, `gsd-core/`, or existing-test edits — the roster-capmap and model-neutrality suites are already directory/emission-derived and auto-cover the 18 additions (F-05); this plan RUNS them, it does not rebuild them (D-06). The only NEW artifact is the one test file.

The headline gate (D-07): a downstream reader confirms the phase in one command — 28 sources -> 28 emitted commands + 28 skills, roster shows 28 Supported, zero model literals. The literal `28` is pinned in exactly ONE place (the CMD-01 guard); every other count is derived from `commands/gsd/*.md` length so the suite can never go stale (Pitfall 4).

ACCEPTED DECISION — two-roster divergence (RESEARCH Open Question, Option A): CMD-02 is verified against the directory-derived repo-root `SUPPORT-ROSTER.md` (regenerated in Plan 09-01) plus the `count==28` emission gate, NOT the installed `.bob/SUPPORT-ROSTER.md` that `stage.cjs renderRoster` builds from its hardcoded 5-entry representative list. Group D reads the repo-root roster only; `stage.cjs renderRoster` is deliberately left untouched (staging-engine edits are out of scope for this source-only phase). The installed roster continuing to show a 5-entry representative set is a known, accepted divergence — not a silent gap — and does NOT block CMD-02 as written (Option B, making renderRoster directory-derived, would touch the staging engine and needs separate blessing; do not do it here). Asserting `28` against the installed roster is explicitly forbidden (Pitfall 3).

Purpose: satisfy CMD-01 (emitted count == 28), CMD-02 (roster reflects 28), CMD-03 (per-command `.planning/` artifact contract held + model-neutral output).
Output: `test/command-expansion.test.cjs` and a fully green `npm test` over the expanded 28-command set.
</objective>

<artifacts_produced>
## Artifacts this plan produces

NEW test file:
- `test/command-expansion.test.cjs` — directory-derived structural equivalence over all 28 `commands/gsd/*.md`, an emitted-count assertion (28 commands + 28 skills via a scratch `stage()` run, `28` pinned once), a per-stem `scanModelLiterals` zero-literal check, and a roster-reflects-28 assertion against repo-root `SUPPORT-ROSTER.md`.

This plan creates NO new source symbols in `src/` and edits NO code in `src/`, `gsd-core/`, or any existing test. It adds one test file and re-runs the existing invariant suites.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-command-expansion/09-RESEARCH.md
@.planning/phases/09-command-expansion/09-PATTERNS.md

# Idiom 1: per-stem structural loop + programmatic forbidden-token construction
@test/quality-gate-equivalence.test.cjs
# Idioms 2 & 3: scanModelLiterals import + scratch-tmpdir stage() harness (baseOpts/freshManifest/scratch)
@test/model-neutrality.test.cjs
# Directory-derived candidate derivation to mirror (do not hardcode the 28 names)
@test/roster-capmap.test.cjs
# Hermetic vendored-converter + repoRoot resolver
@test/_helpers/vendor.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author test/command-expansion.test.cjs (structural equivalence + count==28 + roster-reflects-28)</name>
  <files>test/command-expansion.test.cjs</files>
  <behavior>
    - Structural (per stem, all 28): each source converts to a command whose frontmatter has a non-empty description, keeps argument-hint IFF the source declared one, and strips effort/allowed-tools/agent/type/requires; the skill frontmatter carries name + non-empty description only; both bodies contain no Claude config-home path form and no colon command dialect, and at least the conversion carries the .bob home + hyphen form.
    - Neutrality (per stem, all 28): scanModelLiterals over each converted command and skill returns [] (zero model literals).
    - Count (CMD-01): a scratch stage() run emits N .bob/commands/gsd-*.md and N .bob/skills/gsd-*/SKILL.md where N === number of commands/gsd/*.md sources; and one explicit guard pins that source count === 28.
    - Roster (CMD-02): repo-root SUPPORT-ROSTER.md lists every commands/gsd stem as gsd-<stem> under Supported.
  </behavior>
  <read_first>
    - `test/quality-gate-equivalence.test.cjs:32-53` (imports + programmatic forbidden-token construction via `.join('')`) and `:76-111` (the per-stem frontmatter/body structural assertions to adapt — non-empty description, argument-hint-iff-declared via `/^argument-hint:/m.test(source)`, strip effort/allowed-tools/agent). Extend the strip set to also assert `type:` and `requires:` are absent (Q4a).
    - `test/model-neutrality.test.cjs:31-50` (`scanModelLiterals` import from `src/bob-adapter.cjs`, `scratch()` helper) and `:67-90` (`freshManifest` + `baseOpts` — the exact `stage()` options object with `scope`/`configHome`/`report`/`manifest`) and `:211-231` (emitted-path shape `commands/gsd-<stem>.md` + `skills/gsd-<stem>/SKILL.md`; the scratch `stage()` count pattern).
    - `test/roster-capmap.test.cjs:53-67` (`deriveCandidates` — the directory-derived enumeration to mirror; never hardcode the 28-name list).
    - `test/_helpers/vendor.cjs` — `requireVendor`, `repoRoot`.
    - 09-PATTERNS.md "test/command-expansion.test.cjs (NEW test)" — the composed-from-three-idioms recipe with the exact copyable excerpts.
  </read_first>
  <action>
    Create `test/command-expansion.test.cjs` as a `node:test` + `node:assert/strict` suite that iterates the FULL directory (the drift-proof spine, D-05) — never a hardcoded list. Enumerate stems via `fs.readdirSync(path.join(repoRoot,'commands','gsd')).filter(f => f.endsWith('.md')).map(f => path.basename(f,'.md'))`, mirroring `roster-capmap.test.cjs:53-67`. Resolve the converter with `requireVendor('runtime-artifact-conversion.cjs')` and `scanModelLiterals`/`stage` from `src/bob-adapter.cjs` and `src/installer/stage.cjs` via `repoRoot`, exactly as the two analog suites do (hermetic — vendored gsd-core, never the global install).

    Build the forbidden and required tokens PROGRAMMATICALLY with `.join('')` (the Claude config-home path form, the colon command dialect, the .bob home, the hyphen form) exactly as `quality-gate-equivalence.test.cjs:50-53` does, so this file's own prose never contains the literal tokens it forbids. Do NOT write those literals verbatim anywhere in the file.

    Group A — structural equivalence (per stem, drop the byte-golden diff, keep the structural + programmatic-token assertions from `quality-gate-equivalence.test.cjs:76-111`): command frontmatter has a non-empty `description`; `argument-hint` present IFF `/^argument-hint:/m.test(source)`; `effort`/`allowed-tools`/`agent`/`type`/`requires` absent from command frontmatter; skill frontmatter has `name` + non-empty `description` and no `argument-hint`; neither converted body contains the config-home path form or the colon dialect; the conversion carries the .bob home + hyphen form.

    Group B — neutrality (per stem): `assert.deepEqual(scanModelLiterals(command), [])` and the same for the skill (reuse the shared detector, D-06; do not author a second neutrality regex).

    Group C — emitted count (CMD-01, D-07): run one scratch `stage()` into a `mkdtempSync` target using `baseOpts`/`freshManifest` shaped from `model-neutrality.test.cjs:67-90`; read `commands/gsd-*.md` and `skills/gsd-*/SKILL.md` from the target; assert both counts equal the source-stem count (derived, not magic); then add exactly ONE guard `assert.equal(stems.length, 28, 'CMD-01: commands/gsd/ holds exactly 28 sources')` — the single pinned `28` (Pitfall 4). All scratch writes stay under `os.tmpdir()`.

    Group D — roster reflects 28 (CMD-02): read repo-root `SUPPORT-ROSTER.md`; for every stem assert its `gsd-<stem>` line appears under the Supported section. This ties the generated roster (regenerated in Plan 09-01) to the source set.

    Do NOT author 11 bespoke real-answer goldens (Q4 forbids it) — the existing Phase-4 real-answer guard in `test/core-loop-equivalence.test.cjs` still runs and covers the text_mode-capture contract for the core loop. Do NOT add byte-golden fixtures for the 18. Do NOT edit any existing test, `src/`, or `gsd-core/`.
  </action>
  <verify>
    <automated>node --test test/command-expansion.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `test/command-expansion.test.cjs` exists and `node --test test/command-expansion.test.cjs` exits 0.
    - The stem list is derived from `fs.readdirSync('commands/gsd')` (no hardcoded 28-name list); the literal `28` appears exactly once (the CMD-01 source-count guard).
    - Group C asserts emitted `.bob/commands/gsd-*.md` count === emitted `.bob/skills/gsd-*/SKILL.md` count === source-stem count, via a real scratch `stage()` run under `os.tmpdir()`.
    - Group A/B assertions run for all 28 stems (structural contract + `scanModelLiterals(...) === []`).
    - Group D confirms every `gsd-<stem>` appears under Supported in repo-root `SUPPORT-ROSTER.md`.
    - The file contains no verbatim Claude config-home path / colon-dialect / model-brand literal (tokens built via `.join('')`).
  </acceptance_criteria>
  <done>test/command-expansion.test.cjs proves, directory-derived, that all 28 sources convert to a structurally-conformant, zero-literal Bob command+skill, emit exactly 28 commands + 28 skills, and appear Supported in the roster.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full invariant family green over the 28-command emission</name>
  <files>(no file changes — verification-only phase gate)</files>
  <read_first>
    - 09-RESEARCH.md Q4c/Q6 and the "Validation Architecture" Sampling Rate: the phase gate is `npm test` full green; the per-req map ties CMD-01/03 to `command-expansion.test.cjs`, CMD-02 to `roster-capmap.test.cjs`, and the neutrality invariant to `model-neutrality.test.cjs`.
    - `package.json` `scripts.test` — `node --test "test/**/*.test.cjs"`.
  </read_first>
  <action>
    Run the full suite as the phase gate and confirm the pre-existing directory/emission-derived invariants auto-covered the 18 additions and stay green over the 28-command set (D-06, D-07): `npm test`. Confirm specifically that `test/model-neutrality.test.cjs` (NEUTRAL-03 zero-literal invariant over the full real emission), `test/roster-capmap.test.cjs` (every command Supported / every skip traces to the gate), `test/quality-gate-equivalence.test.cjs` (regenerated 1.6.1 fixtures), and the new `test/command-expansion.test.cjs` (count==28 + structural + roster) all pass. This task adds no code — it is the headline "confirm the phase in one command" gate (D-07). If any suite is red, do not paper over it: the fix belongs in the responsible plan/task (fixtures -> 09-01 Task 2; structural/count -> Task 1 here), not in a new workaround.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 — the entire suite is green over the expanded 28-command set.
    - The run includes green results for `command-expansion`, `model-neutrality`, `roster-capmap`, and `quality-gate-equivalence`.
    - No existing test, `src/`, or `gsd-core/` file was modified to make the suite pass.
  </acceptance_criteria>
  <done>Full suite green over the 28-command emission: count==28, roster reflects 28, per-command contract held, zero model literals — the phase's headline gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored source -> emitted `.bob/` artifact | The verification boundary: the new suite + NEUTRAL-03 confirm no model-brand literal crosses the converter into an emitted artifact over the full 28-command set. |
| source set -> emitted/loadable set | The count/roster boundary: exactly the 28 Supported sources must emit; no phantom or missing command, no silently-dropped source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02 | Information disclosure | emitted `.bob/` converted set (28) | medium | mitigate | `test/command-expansion.test.cjs` runs `scanModelLiterals` per converted command/skill (Group B) and `test/model-neutrality.test.cjs` re-scans the full real emission — both must return zero literals. |
| T-09-04 | Spoofing/Repudiation | emitted count vs source set | low | mitigate | Group C asserts emitted command count === skill count === source-stem count via a real scratch `stage()` run, with `28` pinned once — catching any silently dropped or phantom-emitted command. |
| T-09-03 | Repudiation | `SUPPORT-ROSTER.md` reflects 28 | low | mitigate | Group D asserts every source stem appears Supported in the regenerated roster; `roster-capmap` proves every skip line traces to the gate — no silent parity gap. |
| T-09-SC | Tampering | test dependencies (supply chain) | low | accept | No packages installed; the suite uses `node:test`/`node:assert` builtins and the already-vendored converter resolved through `test/_helpers/vendor.cjs`. No `[ASSUMED]`/`[SUS]` package. |
</threat_model>

<verification>
- `node --test test/command-expansion.test.cjs` exits 0 (structural over 28 + count==28 + roster reflects 28 + per-stem zero-literal).
- `npm test` exits 0 — model-neutrality (NEUTRAL-03), roster-capmap, quality-gate-equivalence, and the new suite all green over the 28-command set.
- Emitted `.bob/commands/gsd-*.md` count == 28 and `.bob/skills/gsd-*/SKILL.md` count == 28 via a scratch `stage()` run.
</verification>

<success_criteria>
- CMD-01: a real scratch `stage()` run emits exactly 28 commands + 28 skills; the source count is pinned to 28 in one guard.
- CMD-02: `SUPPORT-ROSTER.md` reflects the full 28-command Supported set; `roster-capmap` confirms every command Supported and every skip traces to the gate.
- CMD-03: each of the 28 sources holds the structural `.planning/` artifact contract and emits model-neutral output; NEUTRAL-03 stays green over the 28-command emission.
</success_criteria>

<output>
Create `.planning/phases/09-command-expansion/09-02-SUMMARY.md` when done.
</output>
