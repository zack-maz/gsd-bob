---
phase: 06-on-device-acceptance-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-on-device-acceptance-verification/06-COVERAGE-MATRIX.md
  - .planning/ACCEPTANCE-FOLLOWUPS.md
  - .planning/ACCEPTANCE-CHECKLIST.md
  - test/acceptance-coverage.test.cjs
autonomous: true
requirements: [VERIFY-01, VERIFY-02]

must_haves:
  truths:
    - "VERIFY-01: every v1 success criterion across Phases 1-5 (28 canonical requirement IDs) is provably confirmed by >=1 AC step — a hermetic test fails on any orphan SC or orphan AC"
    - "VERIFY-02: a user can run the full accumulated checklist unattended on a real Bob machine and record an unambiguous pass/fail per item from a single at-a-glance roll-up, in a dependency-safe order, without interpreting or improvising"
    - "VERIFY-02 SC#3: a root-anchored follow-up log exists, pre-seeded with the watch-list assumption rows, so a refuted on-device assumption becomes a tracked enhancement rather than a silent gap"
    - "The checklist modification is INSERT-only: no `## AC-NN` block body is edited; the read-only-by-default safety invariant (T-01-SC) is preserved"
  artifacts:
    - path: ".planning/phases/06-on-device-acceptance-verification/06-COVERAGE-MATRIX.md"
      provides: "SC->AC traceability table (28 requirement IDs -> AC-01..26, zero orphans), derived-from-source blockquote"
      contains: "Requirement ID (canonical SC)"
    - path: ".planning/ACCEPTANCE-FOLLOWUPS.md"
      provides: "Root-anchored fixed-schema wrong-assumption log, pre-seeded with SPIKE-01/02/04 watch-list rows"
      contains: "Proposed enhancement"
    - path: ".planning/ACCEPTANCE-CHECKLIST.md"
      provides: "Run scaffolding (How to Run preamble + execution order + results roll-up) wrapped around frozen AC-01..26"
      contains: "How to Run"
    - path: "test/acceptance-coverage.test.cjs"
      provides: "Hermetic traceability assertions (no orphan SC / no orphan AC) + followups-presence assertion"
      contains: "VERIFY-01"
  key_links:
    - from: "test/acceptance-coverage.test.cjs"
      to: ".planning/REQUIREMENTS.md"
      via: "parses v1 requirement IDs above the `## v2 Requirements` boundary, minus VERIFY-* (the canonical SC source of truth)"
      pattern: "REQUIREMENTS\\.md"
    - from: "test/acceptance-coverage.test.cjs"
      to: ".planning/ACCEPTANCE-CHECKLIST.md"
      via: "matchAll family regex over each `Confirms:` line (anti-drift: no frozen ID list)"
      pattern: "SPIKE\\|RUNTIME\\|TRANS\\|INSTALL\\|CORE\\|QUAL\\|UP"
    - from: ".planning/ACCEPTANCE-CHECKLIST.md"
      to: ".planning/ACCEPTANCE-FOLLOWUPS.md"
      via: "preamble references the follow-up log by path for refuted-assumption recording"
      pattern: "ACCEPTANCE-FOLLOWUPS"
---

<objective>
Prove, structure, and seed the on-device acceptance gate for gsd-bob — without writing any runtime/adapter/installer/converter code (D-07). This phase does NOT author new on-device AC steps; the checklist (`.planning/ACCEPTANCE-CHECKLIST.md`, AC-01..AC-26) is already complete. It (1) proves every v1 success criterion across Phases 1-5 is covered by >=1 AC step (VERIFY-01) via a coverage matrix + a hermetic `node:test`; (2) wraps the frozen AC bodies with run scaffolding so a user can run them unattended and record an unambiguous pass/fail (VERIFY-02); and (3) stands up a pre-seeded follow-up log so a wrong-on-device assumption becomes a tracked enhancement, never a silent gap (VERIFY-02 SC#3).

Purpose: Compensate for the total absence of a local Bob throughout development with a runnable, complete, self-recording artifact set plus a mechanical completeness proof — the single empirical gate the user runs once on real hardware.
Output: One new coverage matrix doc, one new root-anchored follow-up log, insert-only run scaffolding added to the existing checklist, and one new hermetic test holding the traceability + presence assertions.

## Artifacts this phase produces
- `.planning/phases/06-on-device-acceptance-verification/06-COVERAGE-MATRIX.md` (NEW) — the SC->AC traceability proof (VERIFY-01, per D-01)
- `.planning/ACCEPTANCE-FOLLOWUPS.md` (NEW, root-anchored sibling) — the wrong-assumption log, seeded with FU-01/FU-02/FU-03 (+ optional FU-04) (VERIFY-02 SC#3, per D-05/D-06)
- `.planning/ACCEPTANCE-CHECKLIST.md` (MODIFIED, insert-only) — How to Run preamble + execution order + results roll-up around frozen AC-01..26 (VERIFY-02, per D-03/D-04)
- `test/acceptance-coverage.test.cjs` (NEW) — hermetic completeness + followups-presence assertions; test symbols name `VERIFY-01` / `VERIFY-02` (per D-02/D-06)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-on-device-acceptance-verification/06-CONTEXT.md
@.planning/phases/06-on-device-acceptance-verification/06-RESEARCH.md
@.planning/phases/06-on-device-acceptance-verification/06-PATTERNS.md
@.planning/REQUIREMENTS.md
@.planning/ACCEPTANCE-CHECKLIST.md
@test/roster-capmap.test.cjs
@test/_helpers/vendor.cjs
@SUPPORT-ROSTER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the coverage matrix (VERIFY-01 / D-01)</name>
  <read_first>
    - .planning/phases/06-on-device-acceptance-verification/06-RESEARCH.md (the VERIFIED "SC->AC Coverage Matrix" table — 28 canonical SCs -> AC-01..26, zero orphans; transcribe, do NOT re-derive)
    - .planning/phases/06-on-device-acceptance-verification/06-PATTERNS.md (the COVERAGE-MATRIX.md section: SUPPORT-ROSTER.md header analog)
    - SUPPORT-ROSTER.md (the "DERIVED from a named source / guarded against drift" blockquote analog, lines 1-6)
    - .planning/REQUIREMENTS.md (the 28 v1 Phase 1-5 requirement IDs being mapped)
  </read_first>
  <files>.planning/phases/06-on-device-acceptance-verification/06-COVERAGE-MATRIX.md</files>
  <action>
    Create the standalone matrix at `.planning/phases/06-on-device-acceptance-verification/06-COVERAGE-MATRIX.md` per D-01 (standalone-file option; keeps the checklist body untouched per Pitfall 2). Open with a `# Phase 6 Coverage Matrix — SC -> AC traceability` title and a blockquote (mirroring SUPPORT-ROSTER.md) stating the file is DERIVED from `.planning/ACCEPTANCE-CHECKLIST.md` `Confirms:` lines + `.planning/REQUIREMENTS.md` v1 IDs and is guarded against drift by `test/acceptance-coverage.test.cjs` (VERIFY-01) — "do not hand-curate the mapping."

    Transcribe the verified table from 06-RESEARCH.md §"SC->AC Coverage Matrix" VERBATIM (do NOT re-derive or re-discover — Pitfall 1). Columns: `Phase | Requirement ID (canonical SC) | Confirmed by AC | Notes`. All 28 rows: SPIKE-01..04 -> AC-01..04; RUNTIME-01/02 -> AC-06, RUNTIME-03 -> AC-05, RUNTIME-04 -> AC-12; TRANS-01..05 -> AC-07..11; INSTALL-01 -> AC-13/AC-16, INSTALL-02/03 -> AC-13, INSTALL-04 -> AC-14, INSTALL-05 -> AC-15/AC-16; CORE-01..05 -> AC-17..21; QUAL-01 -> AC-22, QUAL-02 -> AC-23, QUAL-03 -> AC-24/AC-25; UP-01/02 -> AC-26.

    Below the table add a short "Tallies" note stating: 28 canonical SCs (4 SPIKE + 4 RUNTIME + 5 TRANS + 5 INSTALL + 5 CORE + 3 QUAL + 2 UP), 26 AC steps, zero orphan SC and zero orphan AC, and the multi-ID AC lines (AC-06, AC-13, AC-26). State that the join key is the `(FAMILY)-\d{2}` requirement ID, NOT the phase-local ` / SC#N` suffix (which drifts). Add a one-line pointer that this matrix is referenced from the checklist's run preamble (built in Task 3).

    Do NOT invent any AC-27+ step, do NOT edit the checklist here, and do NOT introduce the deprecated colon command form anywhere — use the hyphen `gsd-<cmd>` form if a command name appears.
  </action>
  <acceptance_criteria>
    - File `.planning/phases/06-on-device-acceptance-verification/06-COVERAGE-MATRIX.md` exists.
    - It contains a blockquote naming `ACCEPTANCE-CHECKLIST.md` as the derived source and `test/acceptance-coverage.test.cjs` as the drift guard.
    - The table contains all 28 canonical requirement IDs (SPIKE-01..04, RUNTIME-01..04, TRANS-01..05, INSTALL-01..05, CORE-01..05, QUAL-01..03, UP-01..02) each mapped to >=1 AC-NN.
    - `grep -c 'gsd:' .planning/phases/06-on-device-acceptance-verification/06-COVERAGE-MATRIX.md` returns 0 (no colon command form).
    - No `## AC-NN` step is authored in this file (it is a matrix, not a checklist extension).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/06-on-device-acceptance-verification/06-COVERAGE-MATRIX.md && grep -q 'Requirement ID (canonical SC)' .planning/phases/06-on-device-acceptance-verification/06-COVERAGE-MATRIX.md && grep -q 'test/acceptance-coverage.test.cjs' .planning/phases/06-on-device-acceptance-verification/06-COVERAGE-MATRIX.md && for id in SPIKE-01 SPIKE-04 RUNTIME-04 TRANS-05 INSTALL-05 CORE-05 QUAL-03 UP-02; do grep -q "$id" .planning/phases/06-on-device-acceptance-verification/06-COVERAGE-MATRIX.md || { echo "MISSING $id"; exit 1; }; done && test "$(grep -c 'gsd:' .planning/phases/06-on-device-acceptance-verification/06-COVERAGE-MATRIX.md)" = "0" && echo OK</automated>
  </verify>
  <done>The coverage matrix transcribes the verified 28-SC -> AC-01..26 mapping with a derived-from-source blockquote and zero-orphan tallies; no AC step authored, no colon command form.</done>
</task>

<task type="auto">
  <name>Task 2: Stand up the pre-seeded follow-up log (VERIFY-02 SC#3 / D-05, D-06)</name>
  <read_first>
    - .planning/phases/06-on-device-acceptance-verification/06-RESEARCH.md (the "Follow-up log schema + seed rows" section: the exact column order, the FU-01/02/03(+04) seed rows, the SPIKE-04 v2-ID GAP note — Pitfall 5)
    - .planning/phases/06-on-device-acceptance-verification/06-PATTERNS.md (the ACCEPTANCE-FOLLOWUPS.md section: root-anchored fixed-schema doc analog)
    - .planning/ACCEPTANCE-CHECKLIST.md (lines 1-15 — the root-anchored declaration + `**Schema:**` field-order block to mirror)
    - .planning/REQUIREMENTS.md (§v2 — confirm PAR-01 and NATIVE-01 exist; confirm NO config-home-override v2 ID exists)
  </read_first>
  <files>.planning/ACCEPTANCE-FOLLOWUPS.md</files>
  <action>
    Create `.planning/ACCEPTANCE-FOLLOWUPS.md` at the planning ROOT (sibling of ACCEPTANCE-CHECKLIST.md — NOT nested under a phase dir; per D-06). Mirror the checklist header conventions: a `# On-Device Acceptance Follow-Ups` title, an Established/Run-target line, and a paragraph stating it is the single root-anchored wrong-assumption log, referenced from the checklist preamble, shipping pre-seeded but empty of refutations (its existence + schema + watch-list rows ARE the VERIFY-02 mechanism; population is the user's on-device act).

    Declare the fixed column schema (D-05 + Research) in this exact order: `| ID | Status | Assumption | Observed on-device | Impact | Proposed enhancement | Links |`. State the `Status` seed value is `unconfirmed`, flipped by the user to `confirmed-as-assumed` or `refuted` after the pass; and that a refuted lower-bound (Bob actually richer than assumed) is the good-surprise case that must become tracked v2 work.

    Pre-seed the watch-list rows exactly (transcribe from 06-RESEARCH.md §"Follow-up log schema"):
    - FU-01 / unconfirmed / SPIKE-01: no isolated subagents -> sequential inline / Links -> PAR-01 + NATIVE-01, AC-01, CAPABILITY-MAP SPIKE-01.
    - FU-02 / unconfirmed / SPIKE-02: no structured prompts -> text_mode / Links -> NATIVE-01, AC-02, CAPABILITY-MAP SPIKE-02.
    - FU-03 / unconfirmed / SPIKE-04(b): config home ~/.bob fixed, no env override / Links -> a DESCRIPTIVELY-NAMED proposed enhancement ("descriptor config-home-override enhancement — v2 candidate, no ID yet"), AC-04 + AC-06, CAPABILITY-MAP SPIKE-04(b). DO NOT cite a non-existent v2 ID for this row (the GAP is real and flagged — Pitfall 5).
    - FU-04 (optional, include it) / unconfirmed / SPIKE-04(c): IDE-vs-Shell via BOB_SHELL_CLI_IDE_SERVER_PORT / Links -> NATIVE-01, AC-04, CAPABILITY-MAP SPIKE-04(c).

    Fill the `Observed on-device`, `Impact`, and `Proposed enhancement` cells with the conservative-default-was-refuted description from Research (what becomes possible if Bob proves richer). This is doc-only; no test/runtime code here. Use hyphen `gsd-<cmd>` form only if any command appears; never the colon form.
  </action>
  <acceptance_criteria>
    - File `.planning/ACCEPTANCE-FOLLOWUPS.md` exists at the planning root (NOT under any phase directory).
    - The header row contains all six required columns: `ID`, `Assumption`, `Observed on-device`, `Impact`, `Proposed enhancement`, `Links` (plus the `Status` column).
    - The three primary watch-list tokens are present in the table body: `SPIKE-01`, `SPIKE-02`, `SPIKE-04`.
    - The FU-01 row links `PAR-01` and `NATIVE-01`; the FU-02 row links `NATIVE-01`.
    - No invented v2 ID is asserted for the SPIKE-04 config-home row (it links a descriptive proposed enhancement, not an existing ID).
    - `grep -c 'gsd:' .planning/ACCEPTANCE-FOLLOWUPS.md` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/ACCEPTANCE-FOLLOWUPS.md && ! test -f .planning/phases/06-on-device-acceptance-verification/ACCEPTANCE-FOLLOWUPS.md && for col in 'ID' 'Assumption' 'Observed on-device' 'Impact' 'Proposed enhancement' 'Links'; do grep -q "$col" .planning/ACCEPTANCE-FOLLOWUPS.md || { echo "MISSING COL $col"; exit 1; }; done && for tok in SPIKE-01 SPIKE-02 SPIKE-04 PAR-01 NATIVE-01; do grep -q "$tok" .planning/ACCEPTANCE-FOLLOWUPS.md || { echo "MISSING $tok"; exit 1; }; done && test "$(grep -c 'gsd:' .planning/ACCEPTANCE-FOLLOWUPS.md)" = "0" && echo OK</automated>
  </verify>
  <done>The root-anchored follow-up log exists with the fixed seven-column schema and is pre-seeded with FU-01/02/03(+FU-04) watch-list rows; the SPIKE-04 row links a descriptive proposed enhancement (no invented v2 ID).</done>
</task>

<task type="auto">
  <name>Task 3: Add insert-only run scaffolding + write the hermetic traceability/presence test (VERIFY-01, VERIFY-02 / D-02, D-03, D-04, D-06)</name>
  <read_first>
    - .planning/ACCEPTANCE-CHECKLIST.md (FULL — the frozen header/schema/safety block at lines 1-15, the AC-01..26 bodies that MUST NOT be edited, and the `## AC-NN` + `Confirms:` line shapes)
    - .planning/phases/06-on-device-acceptance-verification/06-RESEARCH.md (the "Unattended run structure", "Mutating-step dependency order", "Traceability test shape", "How Confirms lines encode SC identity", and "Code Examples" sections — the family regex, the AC-15-uninstall-last teardown order, the canonical-SC parse boundary)
    - .planning/phases/06-on-device-acceptance-verification/06-PATTERNS.md (the test header pattern + repo-root resolution + the ACCEPTANCE-CHECKLIST.md insert-only section)
    - test/roster-capmap.test.cjs (the exact parse-and-assert-against-frozen-contract analog: `'use strict'`, node:test, node:assert/strict, JSDoc naming the reqs, derive-don't-freeze)
    - test/_helpers/vendor.cjs (import `{ repoRoot }` — never hand-roll path.resolve)
  </read_first>
  <files>.planning/ACCEPTANCE-CHECKLIST.md, test/acceptance-coverage.test.cjs</files>
  <action>
    PART A — Insert-only checklist scaffolding (D-03/D-04). Make ALL additions BEFORE `## AC-01` (line 19) and AFTER the existing safety-invariant block / `---` (line 17). Do NOT edit, reflow, or reorder any text inside a `## AC-NN` block — a `git diff` touching any line from `## AC-01` onward (other than pure insertion above it) is a failure (Pitfall 2). Add three new sections:
      1. A `## Results Roll-Up` table near the top: columns `| AC-ID | pass/fail | notes |`, one row per AC-01..AC-26 — the single at-a-glance record the user reports. Keep (do not remove) the per-step inline `Result: [ ] pass [ ] fail` (D-04: both).
      2. A `## How to Run` preamble: prerequisites (a real Bob install; Node >=22.15.0; a throwaway/scratch workspace); the install step `npx -y --package=@opengsd/gsd-bob@latest -- gsd-bob --bob --local`; and the explicit instruction to run each `Cmd:` line verbatim, type numbered answers when prompted, mark `fail` if observed output does not match `Expect:`, and — if a fail concerns a watch-list assumption — record it in `.planning/ACCEPTANCE-FOLLOWUPS.md` (reference that log BY PATH here, the D-06 linkage). Reaffirm T-01-SC: read-only by default; the only mutating commands are the already-marked install/loop/gate runs inside AC-13..AC-25; the scaffolding adds NO new `Cmd:` line.
      3. An `## Execution Order` section: read-only AC-01..AC-12 (+ AC-26) first (safe in any order); then the mutating steps in this exact dependency order — AC-13 install -> AC-14 re-run -> AC-16 dry-run (no-op) -> AC-17..AC-21 core loop in sequence -> AC-22..AC-25 gates -> AC-26 docs -> AC-15 uninstall LAST as teardown (it destroys the install AC-17..25 need; state this explicitly so an unattended run never hits a step whose prerequisite was torn down).

    PART B — The hermetic test `test/acceptance-coverage.test.cjs` (D-02/D-06). Clone the shape of test/roster-capmap.test.cjs EXACTLY: `'use strict';` on line 1; a leading JSDoc block naming `VERIFY-01 / VERIFY-02 / D-02 / D-06`; `require('node:test')`, `require('node:assert/strict')`, `require('node:fs')`, `require('node:path')`, and `const { repoRoot } = require('./_helpers/vendor.cjs');`. Place it directly in `test/` (kebab-case `.test.cjs`) so the existing `test/**/*.test.cjs` glob auto-includes it — no package.json edit.

    Implement two parse sources of truth, derived at test time (anti-drift — never freeze a hand ID list):
      - canonical SCs: read `.planning/REQUIREMENTS.md`, take requirement IDs UNDER `## v1 Requirements` and ABOVE `## v2 Requirements`, and DROP the `VERIFY-*` IDs (this phase's own reqs). Extract with the family regex.
      - checklist refs: read `.planning/ACCEPTANCE-CHECKLIST.md`, pair each line starting `Confirms:` to its preceding `## AC-NN` header, and extract every requirement ID per `Confirms:` line via `matchAll` of the family regex `/\b(SPIKE|RUNTIME|TRANS|INSTALL|CORE|QUAL|UP)-\d{2}\b/g` (collect ALL matches so AC-06 yields RUNTIME-01+02, AC-13 yields INSTALL-01+02+03, AC-26 yields UP-01+02; de-dup into a Set). Key on the ID only — IGNORE the ` / SC#N` suffix, the parentheticals, and all prose (Pitfall 3); never `assert.equal` on a full `Confirms:` line.

    Write the assertions, one `test('<REQ-ID>: <prose>', ...)` each, with messages naming the offending item:
      - `VERIFY-01: every v1 SC (Phases 1-5) is referenced by >=1 AC Confirms line (no orphan SC)` — setdiff(canonical, referenced) is empty; message `orphan SC: ${id} has no AC`.
      - `VERIFY-01: every AC step references >=1 canonical requirement ID (no orphan AC)` — each parsed AC block has >=1 ID match; message names the orphan AC.
      - `VERIFY-02: the follow-up log exists with the required columns and the watch-list rows (D-06 presence)` — `.planning/ACCEPTANCE-FOLLOWUPS.md` exists; its header row contains `ID`, `Assumption`, `Observed on-device`, `Impact`, `Proposed enhancement`, `Links`; and the table body contains the tokens `SPIKE-01`, `SPIKE-02`, `SPIKE-04`. Keep this STRUCTURAL only — do NOT assert any row is still `unconfirmed` (the user flips it post-pass), and do NOT assert a non-existent v2 ID exists for the SPIKE-04 row (Pitfall 5).

    Construct any command-name string via the hyphen form (e.g. `['gsd','-'].join('')`) so the test prose carries no colon-dialect literal, exactly as roster-capmap.test.cjs:45-46 does.
  </action>
  <acceptance_criteria>
    - `node --test test/acceptance-coverage.test.cjs` exits 0 with the three named tests passing (no orphan SC, no orphan AC, follow-up presence).
    - `npm test` still exits 0 (the new file is auto-globbed; nothing else regresses).
    - `.planning/ACCEPTANCE-CHECKLIST.md` contains the literal section headers `## Results Roll-Up`, `## How to Run`, and `## Execution Order`, and references `.planning/ACCEPTANCE-FOLLOWUPS.md` by path in the preamble.
    - INSERT-ONLY proof: every pre-existing `## AC-NN` block body is byte-unchanged — `git diff` shows only insertions above `## AC-01` (verify with: the count of `## AC-` headers is still 26, and no AC body line was modified).
    - The checklist still has exactly 26 AC steps (no AC-27+ authored) and 26 `Confirms:` lines.
    - `test/acceptance-coverage.test.cjs` line 1 is `'use strict';`, imports `{ repoRoot }` from `./_helpers/vendor.cjs`, and contains no `assert.equal` on a full `Confirms:` line.
    - `grep -c 'gsd:' test/acceptance-coverage.test.cjs` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/acceptance-coverage.test.cjs && for h in '## Results Roll-Up' '## How to Run' '## Execution Order'; do grep -qF "$h" .planning/ACCEPTANCE-CHECKLIST.md || { echo "MISSING HEADER $h"; exit 1; }; done && grep -q 'ACCEPTANCE-FOLLOWUPS.md' .planning/ACCEPTANCE-CHECKLIST.md && test "$(grep -c '^## AC-' .planning/ACCEPTANCE-CHECKLIST.md)" = "26" && test "$(grep -c '^Confirms:' .planning/ACCEPTANCE-CHECKLIST.md)" = "26" && head -1 test/acceptance-coverage.test.cjs | grep -q "use strict" && grep -q "require('./_helpers/vendor.cjs')" test/acceptance-coverage.test.cjs && test "$(grep -c 'gsd:' test/acceptance-coverage.test.cjs)" = "0" && git diff --unified=0 .planning/ACCEPTANCE-CHECKLIST.md | grep '^-' | grep -v '^---' | grep -q '.' && { echo "INSERT-ONLY VIOLATED: deletions found"; exit 1; }; echo OK</automated>
  </verify>
  <done>The checklist carries an insert-only Results Roll-Up + How to Run + Execution Order (read-only first, AC-15 uninstall last) referencing the follow-up log by path, with all 26 AC bodies byte-unchanged; the new hermetic test proves zero orphan SC, zero orphan AC, and follow-up-log presence, and `npm test` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 6 produces markdown docs + a read-only `node:test` that parses repo-local trusted files. No authentication, session, access-control, cryptography, network, or untrusted-input surface is introduced (06-RESEARCH §Security Domain). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `.planning/ACCEPTANCE-CHECKLIST.md` insert edit | mitigate | INSERT-only constraint; Task 3 acceptance criteria + verify gate prove no `## AC-NN` body line is deleted/modified (`git diff` shows only insertions above AC-01); preserves the frozen D-05 schema and the T-01-SC read-only invariant. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install occurs — Phase 6 is doc + Node built-ins (`node:test`/`node:assert/strict`/`node:fs`/`node:path`) + repo-local `test/_helpers/vendor.cjs` only (D-07; 06-RESEARCH §Package Legitimacy Audit "not applicable"). No `[ASSUMED]`/`[SUS]` packages exist, so no legitimacy checkpoint is required. |
| T-06-02 | Information Disclosure | run preamble loosening read-only posture | mitigate | The How to Run / Execution Order text reaffirms read-only-by-default, partitions read-only (AC-01..12, AC-26) from mutating (AC-13..25), adds NO new `Cmd:` line, and orders AC-15 uninstall LAST as teardown (Pitfall 4 / T-01-SC). |
</threat_model>

<verification>
Phase-level checks (all hermetic — no live Bob, per the test-deferred principle):
- `npm test` exits 0 (the new `test/acceptance-coverage.test.cjs` is auto-globbed; no existing suite regresses).
- `node --test test/acceptance-coverage.test.cjs` exits 0: zero orphan SC, zero orphan AC (VERIFY-01), follow-up-log presence (VERIFY-02 SC#3).
- `.planning/phases/06-on-device-acceptance-verification/06-COVERAGE-MATRIX.md` exists with all 28 canonical IDs and a derived-from-source blockquote.
- `.planning/ACCEPTANCE-FOLLOWUPS.md` exists at the planning root with the seven-column schema and FU-01/02/03(+04) seed rows.
- `.planning/ACCEPTANCE-CHECKLIST.md` carries `## Results Roll-Up`, `## How to Run`, `## Execution Order`, references the follow-up log by path, and is INSERT-only (26 AC headers, 26 `Confirms:` lines, no AC body deletion/modification in `git diff`).
- No colon command form (`gsd:`) introduced in any new/modified artifact.
</verification>

<success_criteria>
- VERIFY-01 proven: a coverage matrix transcribes the verified 28-SC -> AC-01..26 mapping (zero orphans) and a hermetic test mechanically re-proves it from REQUIREMENTS.md + the checklist `Confirms:` lines at test time.
- VERIFY-02 satisfied: the checklist is run-structured (preamble + dependency-ordered execution + at-a-glance roll-up) for an unambiguous unattended pass, and a pre-seeded root-anchored follow-up log + its presence test stand up the wrong-assumption mechanism.
- D-07 honored: no runtime/adapter/installer/converter code touched; output is docs + one hermetic test only; the frozen AC bodies and the T-01-SC read-only invariant are preserved.
</success_criteria>

<output>
Create `.planning/phases/06-on-device-acceptance-verification/06-01-SUMMARY.md` when done.
</output>
